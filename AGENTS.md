# VCFM — 会话记忆点

> **最高设计原则：游戏机制与数据表现应尽可能接近现实足球。** 球员、俱乐部、国家队、赛事、转会、财政和比赛模拟优先采用现实中可解释且彼此一致的规则；避免为了得到预设结果而添加只影响某个界面的隐藏能力或特殊加权。同一事实应由同一份数据驱动，必要的性能取舍也要保留真实因果关系。

> 仓库：https://github.com/as7er/VCFM.git · `master`（2026-08-31 起 GitHub 已改为大写 `VCFM`，小写地址仍会重定向）  
> 预览：`python -m http.server 8765 --bind 127.0.0.1`  
> 缓存：**vcfm-v245**（peelB 引擎未动；表现层 A 包：640px 列 / 广播跟镜 / 定格收紧，见「🅰️ 表现层 A 包」）

## 🅰️ 表现层 A 包落地（2026-09-05 上午，ZCode 起草 + 接手会话收尾；画幅/镜头/定格，引擎零改动）

> **一句话**：A1/A2/A3 已落地。**A2 首版公式是错的**——推导假设「scale 关于左上原点」，
> 真实 CSS 是 `transform-origin: 50% 50%` 且 `.mp-camera` 左右内缩 5.5%、垂直满高，
> **横纵两轴窗口不同型**；实机表现为球被恒推离屏心 ~11%、贴左/上边线时出画、
> 贴右/下边线时露 ~18% 场外。已按真实几何重推，`scripts/_camera-framing-geometry-probe.mjs`
> 逐格证伪/验证（484 格：球出画 123→0，露场外 364→0，居中偏移 −12.5%→0）。
> **动镜头公式前必须先跑这个探针。**

- **A2 镜头**（`js/match-broadcast.js`）：follow 档 scale 1.015~1.055（≈永远全球场）→
  1.28 / 1.3(deep) / 1.34(boosted) / 1.45 / 1.5(box)；居中球 t=−50·s·o，窗口钳回草皮
  [−2,102] → |t| ≤ 52s−50/0.89（横）、52s−50（纵）——**横纵不同型，不能共用界限**
  （首版第二个错误）。常量 `CAMERA_WIDTH_FRAC`/`GRASS_MARGIN` 已 export，审计 import 不抄。
  附带修掉存量 falsy-zero：`Number(ball?.x) || 50` 把合法的 0 当缺省——球贴上/左
  边线时镜头误判居中；cameraFraming 与 crowdAtmosphere 两处都改 isFinite 判缺省。
- **A2 收尾**（`js/matchview.js`）：死球/重启镜头不再泵回全球场。旧档 scale≈1.03 时
  `_updateCameraTarget` 的 !canAIAct→回中不可察觉；广播档 1.28 下每次重启都是一次
  1.28→1.0→1.28 的 zoom 泵。现在开赛后死球保持上一目标（球已摆好，镜头原地歇），
  开赛前仍回全球场看阵型。分叉标记 `_everPlayed` 在 `update()` 的 livePlay 处置位
  （**不能只放在 `_updateCameraTarget` 里——simDrive 分支不经过它**）。
- **A1 画幅**（`css/style.css` + `js/matchview.js`）：FMM 列宽 480→640px（窄屏 <640 仍
  100%，手机零变化），画布球员半径上限 10→12px（与列宽同比）。**max-height 56vh→76vh
  一节是死代码已回退**：`.fmm-match .mp-field { max-height:none }`（match-layout 恒带
  fmm-match）以更高特异性覆盖基类，比赛屏球场高一直由 flex 给（1440×1000 实测 957px）
  ——首版「56vh 是半屏元凶」的模型不成立，真正杠杆只有列宽。
- **A3 定格**（`js/main.js` handleSimLiveEvent holdMap）：save 1100→800、chance 850→600、
  woodwork 950→700、corner 2200→1300、offside 900→600。依据：快速高光 61% 墙钟画面
  无人移动；文案可读性由 KEY_EVENT_MS（底栏）保证，与画面定格解耦。**行为级复测待做**
  （近静止% 口径），数值可再调。
- **审计换代**（`scripts/match-broadcast-audit.mjs`）：旧断言 scale<1.04、|pan|≤2.4/2.8
  是按旧 scale≈1.03 手调的，换成几何不变量（居中球 tx=ty=0；pan ≤ 草皮钳制跨度；
  falsy-zero 回归断言）。browser-e2e 的三条相机断言与新设计兼容，未动。
- **目验**（`scripts/_presentation-a1a2a3-visual-verify.mjs`，playwright 1440×1000）：
  列宽 640 ✓、球场高 957 ✓、**scale 断言用 p90（≥1.27）不用 min**——开赛摆位与启动
  缓动（1→1.28）是合法低帧；box 档是短暂事件且 kZoom 缓动爬不满就回落，峰值断言
  只要求 ≥1.32（推近有迹象）。球心逐帧出画 0.81%→**0.00%**（死球收尾后）。
- **缓存 v244→v245** 五处联动齐（sw.js / index.html×6 / main.js import×5 / 本文件头）。
- 教训：**表现层的「简单公式」也要按真实 CSS 几何建模验证**——transform-origin 与
  元素内缩能把直觉等式整个推翻，而 11% 偏心在静态截图上肉眼读不出来；探针要断言
  「球在画内」「窗口 ⊆ 草皮」这类几何不变量，不是魔法常数。

## 交接（2026-09-05 深夜，ZCode 接管 bug#1 双轨标定：深度杠杆家族三重证伪，wingRotate 落地被审计拒绝并回退）

> **一句话留给下一个接手者**：目标纵深类杠杆（wingRotate/runBehind/release）在今天的
> peelB 引擎上**全部无解**——不是参数没调对，是被「越位带 × 进球门 × 禁区三 gate」
> 两头夹死。**「压低禁区循环先行」第三次被独立数据证明是前置条件**，下一个动引擎的人
> 应该做禁区循环（boxSeconds 1046s、留区倒脚、蹲守 7.25s 那条线），不是继续扫深度参数。
> 引擎已回退到 `fb8d981`（peelB）原样，工作区干净，缓存仍 vcfm-v244（净行为零变化，不升）。
> ZCode 侧提交：`4b43e41`（验收指标换代）、`f595e65`（标定工具+证伪证据链）。

### 🎯 bug#1 验收指标换代（4b43e41，本 session 最重要的方法论产出）

用户肉眼主诉「中锋接球后其他人原地看戏」与探针「该跑没跑仅 1.5~1.6%」的矛盾已裁决：
**(A) 正确，(B) 排除**。ZCode 对渲染画面的 10Hz 采样（播放段内 42~54% 球员 <1m/s，FM 同口径
16.2%）与探针 43~45% 互证——病根是**决策层目标分布没有纵深**（最后三区无球目标领先球
中位 **−16.22m**、越过越位线仅 **2.9%**、距防线中位 19.45m），静止只是影子。
**验收主指标 = 探针 [2c] 四列**（领先≥5m 占比 / 领先中位 / 越过线占比 / 距防线中位）；
近静止% 降为次要（对跑位杠杆只响应 2pp）。符号自检内置于探针启动（草案曾把列1/2 写反，
home 好目标 y=25、球 y=30 会被判 −5.25m——公式落地前必须代具体数字验一遍）。

### ⚰️ 深度杠杆家族判决书（12 场 ×种子 372000..，peelB 引擎，全部证据在 f595e65）

| 档位 | 越位 | 进球 | boxSec | 判决 |
|---|---|---|---|---|
| control（=peelB 引擎） | 1.71★ | 3.17 | 1046 | 基线（射门 14.71 离真实带 11.9~13.8，存量问题） |
| release8（复现） | 55.79 | 3.00 | 443 | ⛔老证伪复现 |
| **runBehind4/8**（线后纵深直给） | 3.42/2.63 | **3.67/3.92⚠** | 1143/1103 | ⛔进球破顶+boxSec反升+直塞仅+0.17 |
| **wingD 9/14/16 ≡ 11/15/16 ≡ 12/16/17** | 2.88⚠ | 3.08 | 1132 | ⛔越位破带；三档**逐场同分** |
| **wingRotate 7/13/16**（曾落地） | 1.71★ | 3.17 | 1046 | 探针最优但 **audit 红了两道**（下节） |

- **wingD 三档逐场同分的机制**：管线夹取 `legalY=min(offY,ball.y)` 把 back≥9 的深度
  全部吸到越位线锚点——**深度旋钮在夹取下游不可控**（已写进探针注释）。
- **runBehind 机制教训**：目标拉向防线 → 射门/禁区停留涨（boxSec +97s）、转化跟不上，
  重演「循环与产出是同一个现象」。目标=线的肩膀站位这个形态本身没错，错在禁区循环经济
  承受不了更多禁区停留。

### ⛔ wingRotate 落地与回退的完整记录（不要重做）

wingRotate 7/13/16 曾按标定档落地引擎（engine.js wing-home 默认分支，**「先照原逻辑算+
跑 _clampOffside 再覆写」模式保随机数逐位一致**——这个落地模式本身是可靠资产，
忠实性校验：落地后 control 与探针包装档 [1]/[2]/[2b]/[2c] 十六个数逐位相同）。
落地后六道审计：standard 24 ✅（referenceDelta 九项全容差内，goals 2.83 不变）、
box-defending ✅（crowdedPairs 2.67→4.17，限 6）、corner-structure ✅、
offside-integrity ✅，但——
- **match-realism 24 background ❌**：点球 **0.58/场破上限 0.5**（真实 ~0.28，不是过期断言）；
- **box-possession-sampling ❌**：未标记近距机会 **62.33 破 58**（基线 51.83）。
机制：fromGoal(7)/(13) 都在禁区**里面**（16.5m≈15.7 单位），边锋从「半宽站桩」变成
「常驻禁区抢点」→ 禁区拥挤 → 盒内犯规与漏人同步上涨（整体犯规反而 −3.66，是挪进了盒子）。
**已 `git checkout` 回退。** 若未来禁区循环压下来后重试此档：降深度没有用（wingD 三档
同分），正解是**触发式**——平时保持半宽，球真在飞行/传中时才插后点（跑位时间差原语的
第三个应用场景，与 bug#2 角球共用同一原语）。

### 🔜 下一步（优先级）

1. **禁区循环压降**（主线前置，从未动过）：boxSeconds 1046s、boxSec占最后三区 36%、
   留区倒脚、蹲守 7.25s。它与越位/进球/禁区三 gate 的关系见上文三重证伪——**先做它**，
   深度杠杆才有落地空间。杠杆候选见「🔑 主线诊断（2026-09-02）」与留档四~七。
2. bug#2 角球（落点跷跷板 + 跑位时间差原语）——runBehind 证伪同时给角球 `runs` 原语
   立了规矩：**触发型**（球出手才启动）可行，**常驻型**（一直钉在深处）会撞同一批 gate。
3. 表现层 A 包（画幅/镜头/定格，不碰引擎）仍未做，指令书在 ZCode 会话里。

### 🔧 本 session 工具增量（都在库里，探针用法）

`node scripts/_final-third-movement-calibration-probe.mjs 12 "control,runBehind,wingD"`
—— 第二参数=档位名子串过滤。[2c]=验收主指标；[比分]=落地忠实性校验（引擎内嵌版 control
必须逐场等于探针包装档）；wingDepths={back,near,mid} 旋钮供 wingRotate 重标定。

## 交接(2026-09-04 傍晚,换机/换会话前补记:角球标定快照在 d0eef32 引擎上已过期)

> **一句话留给下一个接手者**:「🅰️ 角球真实参照值总表」第九节留档的
> ★`slots + delivery` 候选数字(每角球射门 39.3% / 角球进球占比 10.3% / 每角球进球 7.1% /
> 进球 3.25)是 **4ddac36 时代引擎**的量产。自那以后引擎改了 7 次(含 `fdbb455` 门将出球、
> `a254017` 直塞 0.20),**在今天的 d0eef32 上复现不出来**。换机后先跑一次标定曲线拿新基线,
> 再决定动不动引擎。本补记只是测量/文档,仓库代码一行未改、无新增文件,缓存仍 vcfm-v243,
> 不升缓存。

### 📐 状态存档(换机前)

- HEAD = origin/master = `d0eef32`,工作区干净,无未推送提交、无 stash。
- `verify --full`(本机 2026-09-04 15:02 → 15:44,**EXIT=0**,79 套件 + 标准档 24 场 + 后台档 24 场):
  标准档 `referenceDelta` 九项**全 0**(fdbb455 的冻结快照没过期);后台档 `profileDelta` 在容差内
  (extraStepSharePct 31.8 / 上限 32)。`box-defending-audit` crowdedPairs **1.42/场**(上限 6)。

### ⚠️ 复测结果:★39.3% 没了(换机前最重要的一条)

同一探针 `scripts/_corner-rework-calibration-probe.mjs` **逐字节未变**(`git diff 4ddac36..HEAD`
对该文件为空),同 12 场种子 372000..372011、标准档、0.1s 步长,自检「control 与未打包装引擎
逐场同分」通过。但两个关键档与留档(4ddac36 时代)对不上:

| 档位 | 每角球射门 | 每角球进球 | 角球进球占比 | 进球/场 | 射门/队场 | 角球/场 | 传中占比 |
|---|---|---|---|---|---|---|---|
| control 留档(第九节) | 23.2% | 1.8% | 2.9% | 2.92 | 13.75 | 4.67 | 4.21% |
| control 今天 | 23.5% | 2% | 2.9% | 2.83 | 13.46 | 4.25 | 4.72% |
| ★`slots+delivery` 留档 | **39.3%** | 7.1% | **10.3%** | 3.25 | 14.67 ⚠ | 4.67 | 4.39% |
| `slots+delivery` 今天 | **23.7%** | 3.4% | **5.4%** | 3.08 | 13.75 | 4.92 | 4.71% |

**结论**:不是探针问题(文件没动、自检过),是**引擎在标定之后动过、留档没标有效期**。
「两半单独做都比不做更差」的结构性结论仍成立(`delivery` 单独 = 落点吊进无人区、出脚时距目标
10.7m;`slots` 单独 = 角球零进球却漏球,今天进球 3.00 vs control 2.83),但 **「39.3% 对上真实
38.5%」这条最强信号、以及 10.3% 进带、7.1% 超真 1.7 倍——都是旧引擎的产物**,今天不再成立。
`gkOnLine` 归因(门将被提到门线上约占 +0.66 的一半、退回引擎原值却救不回护栏)是机制性结论,
量级同样待复测。完整 12 场输出在本机 `/tmp/corner-cal-12.txt`(16:22,不进库)。

**换机后第一件事**(接手者,~15 分钟):
`node scripts/_corner-rework-calibration-probe.mjs 12` —— 重跑标定曲线,以今天的数字为准。
沿用第九节纪律:`slots+delivery` 采用前必须过 `match-realism-audit 24` / `box-defending-audit` /
`corner-structure-audit` 三关;当年进球余量只剩 0.05,今天连射门率那条最强信号都没了,
**在没有新基线前不要动角球引擎**。

## 交接(2026-09-04 清晨,换设备:公司/家里那台 → 新机器)

> **本次完成:用户肉眼报的四件事里,两件画面/行为问题修完并各自单独提交(`fdbb455` 门将出球、
> `d33fbf6` 画面瞬移);两件引擎深账(最后三区跑位塌陷、角球 slots+delivery)经实测确认
> 本次无法收口,根因与反证全部留档在本节。工作区干净、**只差 push**。缓存仍是 vcfm-v243
> (门将改动已升;本次两提交都不需要再动缓存——越位那两条软规则实验**已全部回滚,引擎没留下
> 任何改动**)。两个未入库探针(`scripts/_offside-awareness-calibration-probe.mjs`、
> `scripts/_offside-ship-gate-probe.mjs`)都不进库、不阻塞出货,新机器不需要它们。**

### ✅ 换机后第一步(拿到新机器)

```
git pull                      # 取 fdbb455 + d33fbf6(若 push 已完成则直接拉到)
git status --short            # 应干净(只剩那两个 ?? 探针,可不入库)
node --check js/sim/engine.js && node --check js/matchview.js
node scripts/offside-event-integrity-audit.mjs 8   # 越位 3.75/队场并告警(见下,引擎=fdbb455 原样)
```

**⛔ 引擎现在是 fdbb455 原样**,越位 3.75 是未修状态、`offside-event-integrity-audit` 会告警
(它只告警不失败,见文件头)。**这不是回归**,是本会话把越位实验全部回滚后的正确状态。

### 📝 本设备改动 = 2 个提交,已过各自的全部护栏

| 提交 | 改了什么 | 护栏状态 |
|---|---|---|
| `fdbb455` | 门将出球三处根因(压迫阈值 6.5→4、大脚落点改有方向 [30,55]/[45,70]、符号×2) | `verify --full` 全绿(193 套件 + 两项 24 场标定);快照九项刷到新基线 |
| `d33fbf6` | **画面瞬移**:引擎 `_restart` 单 tick 把球+全员搬到重启点,画面照画即瞬移;修法=相邻帧超物理上限位移时显示坐标缓动 0.7s,纯表现层 | 不碰引擎/不动任何出货门;`node --check` 过;落地轻弹误触发一并修掉 |

`fdbb455` 的 `verify --full` 全绿是上一会话跑的,输出在 `/tmp/verify-full4.txt` 末行 `EXIT=0`,
开跑时间晚于全部源文件 mtime,覆盖的就是那次提交。`d33fbf6` 是纯表现层、引擎一行未改,
`d33fbf6` 之后的引擎与那次全绿的引擎**逐位相同**,不需重跑 `--full` 就能出货。

### ⚠️ 引擎级反证:越位软规则在真实出货门上无解(本次最重要的一段留档)

用户「都修啊」四件事里,第 3 件(最后三区跑位塌陷)被越位挡着;越位这条线本次推到头了,
结论是**没有任何一档越位软规则能同时满足真实出货门全部九项**。完整反证链:

1. **出货门两格个位数都在真实门槛失败**(24 场 equal,种子 165000..,审计本体):
   `0.45/0.40`:进球 **2.42 破底**、转化 **8.5% 破下限 9**;`0.30/0.45`:进球 2.96/转化 10.4 都进带、
   越位进带,但 **点球 0.08/场 破下限 0.1**。
2. **点球塌是「消费共享随机流」造成的漂移假象,不是机制**:越位软规则每次触发各吞一个
   `Math.random()`,把整条后续判罚流(抢断/犯规/门将)向右推;关掉软规则点球回 10/24、
   开 `0.30/0.45` 掉到 2/24。`0.35/0.45`、`0.25/0.50` 也都复现同一点球塌。
3. **改成确定性哈希(不再漂移判罚流)后**:点球回 9/24、越位进带(2.38 略高)、
   **但进球暴跌 2.08、转化 7.4%** —— 哈希让「越位者让球过去」在高意识下过于有效、把进攻拆了。
4. **试过的、都不行**:禁区内无门将时外场抢断不判犯规(出货门转化仍破 9)、
   6.5s 控球保护在进攻三区改 2.0s(逐位无差)、`0.35/0.45`(点球仍塌)、`0.25/0.50`(点球仍塌)。

**⛔ 不要再在越位软规则参数网格上花时间**——它被「点球下限 0.1」和「转化率下限 9」两头夹死,
继续扫只是拟合种子噪声。越位线的**真正解法**仍是 AGENTS.md 老账里那两条:
(a) 跑位时间差(出脚瞬间不越位、球到时已越过)——这同时是第 3 件事(跑位塌陷)的解;
(b) 与「压低禁区循环」成对做。**本次会话没有触碰这两条。**

### 🔜 待做(按优先级)

1. **先跑 `verify --full`**(约 50 分钟,含标准档+后台档两项 24 场标定)拿一份全绿存档;
   至少跑 `node scripts/match-realism-audit.mjs 24`(标准档,~5 分钟)确认 fdbb455 基线。
   越位软规则实验已回滚,引擎无残留,预期直接绿。
2. **用户原话的四件事还剩两件**:第 3 件跑位塌陷(被越位卡,见上)、第 4 件角球 slots+delivery
   (尚未开始)。角球那条不需要越位,是最可能先动的一件。
3. 本设备两个探针未入库:**`_offside-ship-gate-probe.mjs` 是全新工具**(动态 import 真审计跑
   逐格出货门,~2 分钟/格,值不值得留库由新机器决定)。`_offside-awareness-calibration-probe.mjs`
   是实验留档,同上。


## 交接（2026-09-03 深夜，同一台机器换会话）

> **本次完成：用户肉眼报的三个画面问题里，门将出球那条已改完并过出货门；越位老账重测完，
> 结论推翻了 v241。缓存 vcfm-v242 → v243。`verify --full` 全绿，已提交。**

### ✅ `verify --full` 全绿（判决对应当前这次提交）

上一会话末尾那跑其实跑完了，只是会话结束得早、没看到退出码。输出在
`/tmp/verify-full4.txt`：**开始 22:46:58 → 结束 23:30:31（约 44 分钟），
末行 `VCFM verification passed`、`EXIT=0`**，含 193 个套件 + `--full` 专有的
两项 24 场标定（标准档 `:1826`、后台档 `:1945`）。
**开跑时间晚于本次最后一次引擎改动（`js/sim/engine.js` 22:32:56），
之后再没碰过任何文件**，所以这份绿覆盖的就是本次提交的全部内容。

途中单独跑过、结论一致：
- `node scripts/match-realism-audit.mjs 24 background` → **进球 3.08，退出码 0**，
  对刷新后快照九项全部在容差内（余量最小的两项：空门机会 0.54/0.65、门将对抗 2.29/3）。
- `node scripts/box-defending-audit.mjs` → 绿，`crowdedPairs` **1.42/场**（上限 6）。
- `node scripts/cache-audit.mjs` → 绿（cache vcfm-v243 / 8 处查询串 / 98 资源 / 92 可达模块）。

**教训（值得留着）：50 分钟的全量要重定向到文件再读，别指望会话还在。**
判断一份历史输出能不能算数，看它的 Birth 时间是否晚于所有源文件的 mtime——
这次就是靠这条免掉一次重跑。

### 📝 本次提交的改动（6 个文件，1 个新增）

| 文件 | 改了什么 |
|---|---|
| `js/sim/engine.js` | `_gkDistribute` 三处，见下节 |
| `scripts/match-realism-audit.mjs` | **有意刷新** `STANDARD_PROFILE_REFERENCE_24` 九项 + 写清依据的注释 |
| `scripts/_offside-path-fix-calibration-probe.mjs` | 新增传球成功率/停滞两列、`crossBan` 档、换种子窗口与档位过滤参数；删掉 `peelAll` 两档 |
| `scripts/_gk-kick-and-ball-jump-probe.mjs` | **新增**：门将落点 + 球瞬移探针 |
| `sw.js` / `index.html` / `js/main.js` / `AGENTS.md` | 缓存版本五处联动升到 v243 |

### 🥅 门将出球：三处根因已修，过出货门

**用户原话**：「守门员开大脚 100% 落在中场中路对方球员脚下。」
实测（`scripts/_gk-kick-and-ball-jump-probe.mjs` 2 场）：**现象为真但成因不同**。

1. **压迫阈值太宽（主因）**：`underHeavyPressure` 用 `nearestOpp < 6.5` 格，
   而门球时对方中锋固定站在 (50, 83.5)/(50, 16.5)、离门将 6.0~6.3 格——
   **禁区线在 y=84/16，他站在线外，这个高位逼抢合法且真实**。于是 113 次出球
   **112 次判为贴身压迫，短传分支（`:2035`）一次都没执行过**。→ 阈值收到 **4** 格。
2. **落点被硬夹在中三区**：`targetY` clamp 在 `[38,62]`，场地纵向 100 格，
   所以 **113/113 落在中路盒里、43 次（38.1%）还落在本方半场**。
   → 改成有方向的 `[30,55]`（主队）/`[45,70]`（客队）。
3. **两处符号写反**：`:2053` 与 `:2078` 是 `dir * -X`，与瞄接应人那处的 `dir * +2` 相反，
   等于把球开向本方半场。→ 修正。另加一条「接应人必须已推到中线附近」的筛选，
   否则本方半场里最居中的中场就能当选、落点被 clamp 拉回来。

**❌ 证伪的两条**：不是站位违规（那个站位合法）；不是「100% 到对方脚下」——
改前落地后本方先控住 65.5%、对手 19.5%、散球 15%。用户的印象来自**落点永远在中圈那片人堆**。

**效果（2 场探针）**：短传/大脚 0/113 → 38/63；落在本方半场 38.1% → **3.2%**；
落点朝前推进中位 12（贴边界）→ 10.2；大脚后本方保有率 65.5% → 36.8%
（**这个方向是对的**，真实大脚第二球多半是丢的，65% 反而偏高）。

**⚠️ 意外发现**：`_bestPass` 未被调用的次数里有一半是 `bypassBuildUp`——
门将有「发动反击」习惯时按 62% 概率直接开大脚。合成球队也带这个习惯，
说明它由属性推出而非显式配置。这是正常行为，没动。

### ⚖️ 两个旋钮各自绿、合并破顶：**+0.66 是交互项，不是相加**

24 场后台档，同一批固定种子：

| 变体 | 进球 | 射门 | 传球 | 出货门 |
|---|---|---|---|---|
| 都不改 | 3.00 | 29.67 | 1022.42 | 绿 |
| 只改压迫阈值 | **2.67** | 29.00 | 1053.92 | 绿 |
| 只改落点（近端 22） | **3.00** | 27.58 | 1020.79 | 绿 |
| 两处都改（近端 22） | **3.33** | 29.04 | 1045.17 | ❌ 进球破顶 3.3 |
| 两处都改（近端 30） | 3.08 | 28.75 | 1043.13 | ✅ 采用 |
| 两处都改（近端 36） | 3.21 | 28.08 | 1048.04 | — |

- 单独跑：压迫阈值 **−0.33** 球，落点 **0.00** 球且把射门 29.67 压到 27.58（更近真实带）。
  **合起来 +0.33。** 机制：阈值改完后门将只在真被扑上来时才开大脚，而这些大脚现在又都朝前，
  于是剩下的每一脚都变成从受压状态直接发起的快速反击；单独开任何一个这个组合都不成立。
- **⛔ 别再拿近端去凑进球**：22/30/36 → 3.33/3.08/3.21 **不单调**，
  那是固定种子下的混沌重掷、不是响应曲线，拧它等于拟合种子噪声。教训已写进 `engine.js` 注释。

### 🔄 有意刷新了冻结快照的传球成功率（用户点头才做的）

近端 30 之后进球回到 3.08 进带，但换成 `passCompletionPct` 破线：
**79.2% vs 快照 82.4%，偏差 −3.2、容差 ±2**。这不是失真——大脚现在飞到中线一带的争抢区、
不再落进本方中场脚下，完成率必然下降；**78.6% 仍在护栏 72~88 内，也贴近真实（英超约 80%）**。
按 `match-realism-audit.mjs` 自己立的规矩（引擎被有意改过就该回来刷这份数），
**九项一起按同一次标准档跑刷新**，避免半新半旧：

`goals 3.08→2.67 · shots 30.13→28.88 · passes 1094.63→1108.04 · passCompletionPct 82.4→78.6 ·
fouls 26.17→27.29 · openGoalShots 0.83→0.75 · gkClaims 17.71→17.46 · gkChallenges 8.46→7.25 ·
strongPoints 1.79→1.88`

**⚠️ 这是刷探测器基线，不是改断言。** 依据已写在那个常量上方的注释里。
以后的意外漂移从这组新值起算。

### ✅ 越位老账已清（2026-09-05，`peelB` 单档落地，三关全绿）

**结局：`peelB` 已写进引擎（`_resolvePossession` 选接管者的循环，`engine.js:5400` 后一条
`continue`），越位从 4.5 → 2.1~2.5/队场，三关全绿。这一节以下的旧表全部作废——它们跑在
门将改动前的引擎上，今天不复现。**

今天两窗口 12 场重测（门将改动后的 c5182aa 引擎）逐条推翻了下面的旧结论：

| 档位 | 窗口A 越位/进球 | 窗口B 越位/进球 | 旧断言 → 今天 |
|---|---|---|---|
| control | 4.25 / 2.83 | 4.63 / **1.58破底** | control 本身 12 场就抖 1.25 球，判决只能靠 24 场 |
| `peelB` 单档 | 2.29 / 2.5 | 2.79 / 2.67 | ⛔旧「破底 1.58、绝不能单独上」→ ✅**两窗都不破底**，反把窗口B破底的 control 救回 |
| `hardA` | 3.42 / 3.08 | 4.38 / 2.0 | 越位降得不稳 |
| `crossBan` | 2.5 / 2.33破 | 2.46 / 2.75 | 旧无害 → 今天单独上会打崩进球 |
| `peelB`+`hardA` | 2.29 / 2.5 | 2.42 / **2.17破** | 旧最优组合 → 今天窗口B进球破底，**反不如单 peelB 稳** |

- **为什么门将改动翻了盘**：`peelB` 当年单独破底的机理是「越位者不碰球、附近没别人，
  直塞白白滚走」。门将改动改变了球权流转，那个副作用消失——今天单 `peelB` 传球成功率没掉
  （79.7~80%）、停滞 0、直塞没崩。
- **落地后五份审计全过（带 peelB 的引擎源码，非探针包装；vcfm-v244）**：
  - `match-realism-audit 24` **标准档**：进球 2.79 / 停滞 0 / 成功率 78.7% / 强弱队 54.2%，
    与冻结基线 delta 极小（进球 +0.12、成功率 +0.1）。
  - `match-realism-audit 24 background` **后台档**（进攻杠杆约束面）：进球 3.13 / 停滞 0 /
    extraStepSharePct 31.6（上限 32，余量 0.4 极紧但没破，peelB 未恶化它）。
  - `offside-event-integrity-audit 8`：35/35 事件=调用一致、零重复；判罚率 2.19/队场
    （仅告警、非失败，就是 peelB 后的新水平，比改前 4.5 大幅改善）。
  - `corner-structure-audit`：硬护栏全过（间距/主罚者在弧顶/Law 17/落点记录）；
    12 项还原度缺口是 bug#2 待办、只告警不阻断。
  - `box-possession-sampling-audit` boxSeconds 1087（基线 1092）；
    `box-defending-audit` crowdedPairs/场 2.67（上限 14）。全 `passed`。
- **没压进 1.4~2.1 真实带（落在 2.1~2.5 上沿）是有意为之**：地基先打到「可采用」，
  留的越位余量正好给后面「最后三区放开纵深」去消耗——放开跑位必然拉高越位，此时有预算接。
- **`peelB` 的设计原则复核**：它让**已越位的非接球人**不去争这个飞行球（预定接球人 receiverId
  不动、仍会去接并在既有判罚处照常被吹）。纯几何、不消费随机数、不改判罚路径——是让 AI 认
  越位线，不是留漏洞。符合最高设计原则。

<details><summary>⛔ 以下为门将改动前的旧记录，已作废，仅留作追溯（原「v241 结论已被推翻」节）</summary>

我在上一份交接里写「配平条件可能变了，值得重测」——重测了，**但不是我猜的方向**。
新增 `crossBan` 档（传中选中的接球人已越位就放弃这一脚，`_bestCross` 只能在**返回值**上判，
因为它每个候选消耗两次 `random()`，提前剔候选会移动随机数流；`_bestCutback` 不用管，
它只找身后 4~32 格的队友，身后不构成越位）。

窗口 A（种子 372000）与窗口 B（372100），各 12 场，**都跑在门将改动之前的引擎上**：

| 档位 | 窗口A 越位/进球 | 窗口B 越位/进球 |
|---|---|---|
| control | 4.00 / 2.67 | 4.29 / 3.33 ⚠破顶 |
| `peelB` | 2.88 / **1.58** ⚠破底 | 2.67 / **1.92** ⚠破底 |
| `hardA` | 3.92 / 2.83 | — |
| `crossBan` | 2.83 / 2.83 | — |
| `peelB`+`hardA` | 2.33 / 2.67 | — |
| `peelB`+`crossBan` | 1.13 / 3.00 | **0.79 / 2.67**（射门 13.79 **进真实带**、直塞 3.50 **首超真实**、零告警） |
| `hardA`+`crossBan` | 2.58 / 2.92 | — |
| 三条全上 | **0.42** / 2.67 | — |

- **✅ 推翻 v241「修好越位会把进球顶出护栏」**：`peelB`+`hardA` 现在进球 2.67 = control，
  当年那条「每少吹一次越位多 0.15 球」的弹性**归零**了。旧表算作罪状的「射门 15.13 更远离真实带」
  也不成立：合并档 14.21 = control 14.21。
- **✅ `peelB` 单独打崩进球不是噪声**：两个独立窗口同向同量级（−1.09 / −1.41），
  而**传球成功率没动（81.9~82.2%）、停滞全 0** → 不是「球白白滚走」，是射门转化率掉
  （9.4% → 5.8%）。**`peelB` 绝不能单独上。**
- **⛔ 硬开关的任何组合都跳过 1.4~2.1 真实带**：2.33 → 1.13 中间没有档位。
  要落在带内只能给规则留一个**可调的失误率**。而且——
- **🔑 更要紧的机制话**：真实的 1.70 次越位来自前锋抢跑的时间差误判，
  而引擎的越位只来自**传球决策的接线**。全堵是 0.42，留缝调到 1.70 也办得到，
  但那个 1.70 是**故意留的漏洞凑出来的**，不是跑位时间差挣来的。
  真做对要动跑位时间差（见「🔑 主线诊断」与 `:1109-1111`），那是另一条大工程。
- **⚠️ 12 场只能当预警**：两个窗口的 **control 本身就差 0.66 球**（2.67 vs 3.33，后者还破顶）。
  判决只能靠 24 场出货门。**这两张表都是门将改动前的引擎，与现在的引擎不能直接拼，要重跑。**

</details>

### 🎬 球瞬移：是引擎行为，不是渲染 bug

2 场里球单 tick 位移 > 6 格（物理不可能，0.1s 最多约 4.4 格）的帧 **98 次**（约 49 次/场），
位移中位 20.3 格、最大 49.4 格。**其中 90 次带重启类型**（门球 67、越位 13、角球 6、点球 1、任意球 1）
——**引擎把球搬到重启点，画面只是照着坐标画**。
剩 8 次不带重启类型（`dead→held` 7、`held→held` 1、`shot→held` 1），疑似门将捡死球，**未查**。
**修法两条都不动判罚**：表现层补一段「球被捡回摆好」的过渡，或引擎给重启加一个短暂摆球阶段。

### 🅾️ 用户报的第 2 条还没动：前锋单干、其他人原位看戏

**这就是「最后三区跑位塌陷」那笔老账**，不是新问题（见「🔑 主线诊断（2026-09-02）」）：
最后三区占无球决策 54.5%，其中 67% 是三处**刻意**写死的「钉在固定点」分支，
无球目标点离球员本人只有 2.08m（组织阶段 9~10m）。
**它与越位同结**：唯一实测有效的解法是放开越位夹取、允许把防线身后当目标
（`boxSeconds` −57%、均速 +35%），但越位被打到真实值的 24~37 倍。**越位这关过了才轮到它。**

### 🔧 顺手修掉的一条：缓存版本是**五处联动**，`e5f0648` 只改了一处

`cache-audit` 检查五处：`sw.js` 的 `CACHE`、`index.html` 的 `CURRENT_CACHE`、
`index.html`+`js/main.js` 里全部 `?v=NNN`（8 处）、`index.html` 的 `vcfm-sw-reloaded-vNNN`（2 处）、
**AGENTS.md 顶部第一个 `vcfm-vNNN`**。`e5f0648` 升 sw.js 到 v242 时其余四处仍是 241，
**所以 master 上这条审计一整天都是红的没人知道**——上一份交接说的「全量绿」有效期只到 `a254017`。
**教训：连纯字符串改动也要跑一次全量，缓存审计专门查的就是这种不一致。**

### 🔜 下一步（我的建议顺序）

1. **越位那两张表要在新引擎上重跑**（门将改动动了球权流转）。最有希望的一档是
   `peelB`+`crossBan`：窗口 B 里射门首次进真实带、直塞首超真实、零告警，
   **唯一问题是越位 0.79 太低**——它需要的正是那个「可调失误率」。
2. 用户报的第 2 条（跑位塌陷）与第 1 条剩下的 8 次无重启瞬移。
3. 角球第二条主线（`slots + delivery`）仍一点没动。

⚠️ 余量变紧的三项，动引擎前先看：`extraStepSharePct` **31.8/上限 32**、
空门机会对快照 **0.54/0.65**、门将对抗 **2.29/3**。

## 交接（2026-09-03 17:50，从公司电脑换到家里的机器）

> **本次完成：坏断言回退干净 + 主线第一次真正落地。**
> 三个提交在 master 上（`2648943` → `d5e1014` → `a254017`），工作区干净，
> **缓存 vcfm-v241 → vcfm-v242**（引擎行为变了，这次必须升）。
> `node scripts/verify.mjs --full` 全绿。

### ✅ 今天做完的三件事

**1. 回退了一个「把守护断言改成要求 bug 继续存在」的提交（`2648943`）**

`cce4eba` 的 audit 那一半是残缺的：它加的三条断言引用
`SIM.SEPARATION_MIN_DISTANCE_M`，而那个常量**从未被加进 `engine.js`**，
于是 `box-defending-audit` 红在 `:228`（`undefined m`），`verify` 跑不到底。
第一版处置（`e6efc6c`，已回退）把断言改成 `goalwardGap ≈ 2.99` —— 而
2.99 = 2.85 格 × 1.05 m/格，这个数存在的唯一原因就是格数被当成米用，
等于把防回归的绊线**反转成锁死回归**。教训见「⚠️ `cce4eba` 的 audit 那一半是残缺的」。

**2. 按路线 2 处理掉（`d5e1014`）**：移除那三条断言与 `solveGap` 夹具，
**恢复被 `cce4eba` 顺手删掉的 `assert.match(/const need = minD;/)`**（「全场单一半径」
当时成了覆盖空洞）。断言原文与夹具用 `git show cce4eba -- scripts/box-defending-audit.mjs`
取回。⛔ **恢复它时不要照抄那条 `[1.5, 2.2]` 的带**：同一批 24 场实测证明 1.94
自己就破三条护栏，而 1.94 正落在带内。

**3. 主线落地：直塞第 2 门 `advance` 0.35 → 0.20（`a254017`）**

**直塞 0.63~0.73 → 2.29~2.42 次/场**（真实 3.38 的 68~72%）。
强队分/场 1.79 → **2.08**、净胜球 +12 → **+22**——技术优势被放大而不是抹平。
射门几乎不动（28.67 → 29.3），涨的是转化率：改的是机会**质量**不是数量。
完整曲线、四个口径的护栏实测、以及**约束面在后台档不在标准档**这个关键发现，
全部见「🅱️ 直塞第 2 门 `advance`」那一节。

### 🔍 换机后的起步动作

```
git pull                                           # 取本交接 + 三个提交
node scripts/box-defending-audit.mjs               # 应绿（约 95s，crowdedPairs 1.58/上限 6）
node scripts/match-realism-audit.mjs 24 background # 应绿（进球 3.00，这是出货门）
node scripts/_through-pass-gate-calibration-probe.mjs 6 372000
                                                   # 探针自检：control 必须与未打包装引擎逐场同分
```

⚠️ **`a254017` 的全量绿是在「引擎逻辑与现在完全相同」时跑的**，
之后只追加了两处**不影响模拟**的改动：`engine.js` 里那段依据注释（纯注释）
与 `sw.js` 的缓存字符串。三个文件都过 `node --check`。
若要一份逐位对应的绿，家里那台重跑一次 `node scripts/verify.mjs --full` 即可（约 50 分钟）。

⚠️ `npm test` = `verify.mjs` **不带** `--full`，**不含** 24 场标定，
而这次采用的全部依据都在 `--full` 那两项里。**任何动引擎的改动都必须跑 `--full`。**

### 🔜 下一步（按我的优先级排序，理由都在正文里）

1. **越位那笔老账**（最该做）：现 4.3~4.9 次/队场，真实 1.70。
   `peelB`+`hardA` 的 4.54 → 2.04 预算仍在、从未采用。它当年被「进球升到 3.67」卡住，
   **而现在直塞这条路已经把进攻产出补上了一部分，配平条件可能变了——值得重测。**
2. **查「后台档为什么对直塞比标准档敏感」**：转化率涨 +1.0pp vs 标准档 +0.3pp。
   那可能是后台档（0.3s 步长 + 4 趟分离）的保真度缺陷。
   **修它比继续在门槛上折衷更值**——直塞现在只有真实的 68~72%，剩下的缺口正被它挡着。
3. **角球第二条主线一点没动**（`slots + delivery` 那三件事仍未做，见「角球」那节）。
4. 192 场把进球 SE 压到 0.12 那一跑，**未做**（48 场分辨不出这一列，见留档）。

### ⛔ 不要重做的（今天证伪或已完成）

- 落点门 `okOffside` **单独**放宽近惰性：通过率 38%→76%，直塞几乎不动，
  因为上游第 2 门只放过来 120~166 个候选。
- `combined`（advance15 + offTol6）：两窗口直塞 7.33 / 7.50，**真实值的 2.2 倍**。
- `advance15`：标准档全绿（进球 2.75、强队 2.00、净胜球 +19）但**后台档进球 3.46 破顶 3.3**。
- `advance25`：被 `0.20` 双向占优（出货门余量 0.09 vs 0.30，直塞 2.29 vs 2.42）。
- **别再用「24 场 → 48 场」当复现**：审计种子是 `165000 + match`，
  48 场窗口**包含**24 场窗口，那是同一批比赛算两遍。

### 📌 留在公司电脑上、不随 git 走

- 本机 Claude 记忆目录（`~/.claude/projects/F--VCFM/memory/`）：今天新增一条
  `vcfm-red-audit-is-a-finding`（审计红了是发现、要报路线，不要改断言让它绿）。
  **换机后靠本节与「⚠️ `cce4eba`」那节即可，没有只存在于记忆里的实质结论。**
- 后台任务的原始输出在临时目录的 `tasks/*.output`，
  **不需要它们**——每条结论的数字都已写进 AGENTS.md，同种子可复现。

## 交接（2026-09-02 18:40，从公司电脑换回家里的机器）

> **本次完成：两条线各推进一大步，但引擎一行未改，缓存仍是 vcfm-v241。**
> 五个提交已在 master 上（`bdfab18` → `ac24bf1`），工作区干净。
> 今天全部是**测量与留档**，没有任何一档达到「可以直接改引擎」的状态——
> 每一档都写清了还差什么，见下。

### 主线（最后三区无球跑位）：杠杆收窄到位，但目标点这条路封死了

**结论链（按顺序读「🔑 主线诊断（2026-09-02）」那一节）**：
三条起步基线逐位复现（越位 4.56 / boxSeconds 1092.12 / 进球 2.88）→
**跑位塌陷只发生在最后三区**（占无球决策 54.5%，目标距球员 2.08m，而组织阶段是 9~10m，
v240 那个 0.31m 是全场合并值、把分区现象读成了全局）→ 最后三区 67% 的决策是三处
**刻意**写下的「钉在固定点」分支 → **改这些目标点公式造不出快速通路**
（`wingRotate` 54090 次覆写只换 2pp 位移，却把进球顶到 5.67）→
**但放开越位夹取（`release{R}`）证明了机制**：允许把防线身后当目标，
`boxSeconds` −57%、禁区触球 −61%、均速 +35%，而进球 2.67 仍在护栏内——
**四次失败之后第一次做到**，唯一障碍是越位被打到 **24~37 倍**。

**⛔ 已证伪，不要再花时间碰**：
1. `wingRotate`（最后三区边锋前后点）：进球 5.67 破顶，位移只动 2pp——它是多放一个后点蹲守者。
2. `midLate`（第二线锚在禁区弧顶）：**全部指标反向**，近静止 43.2%→48.6%、进球 2.17 破底。
3. `releasePass`（只在传球飞行中放开纵深）：**空档，0 次覆写**。
   `engine.js:1721` 对预定接球人有一条更早的提前返回，飞行段本来就不经过 `_clampOffside`。
4. 第一版 `depthRelease`（`legalY` 丢掉 `min(offY, ball.y)` 的球项）：按 `min` 的单调性只会**更严**。
   它那组近惰性数字**不能读作「放开纵深无效」**。

**✅ 这个杠杆已经做完了（2026-09-03 下午），不要重做**——见「🅱️ 直塞第 2 门 `advance`」。
结论：瓶颈全在第 2 门（`advance > 0.35` → **0.20**），落点那道门 `okOffside` 单独动近惰性。
直塞 0.63~0.73 → **2.29~2.42**（真实 3.38 的 68~72%），全量绿，缓存 vcfm-v242。
**越位那笔老账仍未动**（现 4.3~4.9 次/队场 vs 真实 1.70），`peelB`+`hardA` 的
4.54 → 2.04 下调预算仍在，留给下一次。

> **🔬 2026-09-05 复测 + 钉死病根：塌陷是「决策层目标点太近」，不是移动层腿软。**
> 给探针加了一刀诊断（把「近静止」按离目标距离切成「已到位 <1.5m」vs「该跑没跑 ≥1.5m」，
> 并记录该跑没跑者的 `role|fsm`）。control 现状：近静止 44.8% 里 **已到位 43.3%、该跑没跑仅 1.6%**。
> **结论：球员没有"腿软冲不到"，他们是忠实跑到那个近目标（中位 4.4m）就停了**——44.8% 的静止
> 几乎全是「服从了决策、到位即停」。反证：`release8` 把目标点纵深拉到 6.5m，近静止立刻掉到 35.7%、
> 均速 +32%，**目标一变远球员马上动**。所以这轮一度怀疑的 `engine.js:4713 slowR` 到达减速**不是**
> 运动战塌陷的主因（该跑没跑仅 1.6%，可忽略）；`slowR` 只在**角球抢点**那种「目标点=落点、要全速冲进」
> 的特殊场景才咬人。运动战的真身仍是这一节的老结论：**最后三区目标点太近太密（2.08m/4.4m
> vs 组织阶段 9~10m），肉眼看就是"接球后大家在原地附近小碎步、不前插"。** 修法方向＝给最后三区
> 无球目标点补纵深/前插意图，但不能像 `release8` 粗暴放开（越位 58）或 `wingRotate` 只改边锋还过冲
> （进球 3.58 破顶）。诊断输出留在本机 `/tmp/final-third-diag-12.txt`（不进库）。

### 角球（用户当面指出的还原度问题）：三步全走完，`slots + delivery` 是候选

第 1 步取到**有来源的真实参照值**（官方 PL/Pulselive `corner_taken` 四季 + The Analyst 落点分区
+ premierleague.com 十八季进球占比），第 2 步新增 `scripts/corner-structure-audit.mjs`
（4 条硬断言 + 12 项告警，已挂进 `verify.mjs`），第 3 步跑完标定曲线。
详见「🅰️ 角球真实参照值总表」第一~九节。

**差距分解**：数量只有真实 59%（3.02 vs 5.14 次/队场）× 每角球威胁 52%（2.15% vs 4.1%）
≈ 31%，正好对上「角球进球占全部进球 4.5% vs 十八季带 9.8~14.2%」。**两半都要修。**

**★ 候选档 `slots + delivery`**：每角球射门 **39.3%** 对上真实 **38.5%**、
角球进球占比 **10.3%** 落进 9.8~14.2%、进球 3.25 仍在护栏内。
**⛔ 但还不能上，三件事没做**：
1. ~~**`slots` 单独档多漏的那 +0.66 个非角球进球未归因**（3.58 vs 2.92，而角球进球占比 0%）。
   嫌疑是「进攻方留守 2 / 防守方留前场 1」改变了角球后的转换。**先加一档隔离它。**~~
   **→ 已隔离，嫌疑证伪。见「❌ 负结果留档十」。+0.66 仍未归因，但可以确定不在这两处，
   下一个该验的是门将位置（`slots` 把守门员从离门线 5.25m 提到 1.58m）。**
2. 进球 3.25 **只剩 0.05 余量**，且射门 14.67/队场比现状 13.75 **更远离**真实带 11.9~13.8。
3. 三关都没跑：`match-realism-audit 24`、`box-defending-audit`
   （`crowdedPairs` 上限 14、干净基线 12，**余量只有 2，而这档往小禁区放了 5 名防守者**）、
   `corner-structure-audit`（12 项告警应逐条清空）。

**🔑 `runs` 跑动原语机械上通了**——抢点者退 3m 起跑，出脚那一刻距目标只剩 **1.16m**、
峰值 **2.79 m/s**。这是项目第一次做出「有触发 / 有时长 / 有到达点」的跑动。
**但威胁顶到真实 3.7 倍**（每角球进球 15.3%、射门率 67.8%、进球 4.17），要加阻尼；
三个可调量（`RUN_BACK` 现 3.0m、盯人者提前量、门将出击）**按留档二的教训不要一次动两个**。

**⚠️ 两条会误导下一个人的事实**：
- **`_bestCross` 在角球上 20 次调用全部返回 null**（`ahead >= 4` 永远过不了，主罚者就在球门线上），
  所以**角球落点现在是常量兜底吊传，不是决策，主罚者的 `crossing` 完全不影响球吊到哪**。
  真要落地 `delivery`，正解是给角球一条**自己的**落点选择，不要在 `_bestCross` 里开特例
  （会把运动战传中一起带偏）。
- **不要用 `cornerShapeUntil` 检测角球**：`engine.js:6577` 让人墙任意球复用了同一字段。
  第一版这么写，量出「站位重叠 1.02m」「Law 17 只剩 1.57m」两个**假缺陷**。改用 `ball.state`。

### 换机后的起步动作

```
git pull                                          # 取本交接 + 五个提交
node scripts/corner-structure-audit.mjs           # 角球结构基线（6 场，约 50s，12 项告警是验收清单）
node scripts/offside-event-integrity-audit.mjs 8   # 越位基线，应报 4.56 并告警
node scripts/box-possession-sampling-audit.mjs     # 禁区循环基线（boxSeconds 1092.12）
```
⚠️ **今天没跑 `node scripts/verify.mjs` 全量 75 项**（40 分钟以上，`ecosystem-audit` 单项就十几分钟）。
理由：**引擎代码一行未改**，改动只有新增/修改的审计脚本与文档。
已单独跑过并通过的是 `set-piece-presentation-audit` 与新增的 `corner-structure-audit`，
六个改动过的脚本都过 `node --check`。**下次动引擎前请补一次全量。**
⚠️ `verify.mjs` 默认**不含** 24 场标定（`match-realism-audit` 在独立的 `fullChecks` 里），
任何动引擎的改动都要另跑 `node scripts/match-realism-audit.mjs 24`。
⚠️ `verify.mjs` 不要接 `| tail`——`tail` 要等管道关闭才吐字，后台会看着像卡死。

### 留在公司电脑上、不随 git 走

- `.tmp-baseline/*.log`：今天所有基线与标定曲线的原始输出（`.gitignore` 的 `*.log` 挡着）。
  **不需要它们**——每一条结论的数字都已写进 AGENTS.md，重跑探针也能复现（同种子）。
- `C:\Users\WXX\.claude\projects\F--VCFM\memory\`：本机记忆文件，不随仓库走。
  今天没有任何只存在于记忆里的实质结论，**换机后靠 AGENTS.md 即可**。
- 家里那台机器上的视频素材仍在原处（`C:\Users\WXX\Desktop\2D比赛画面对比\`），
  但 `cmp8_ball.py` 已以负结果关闭，**不阻塞任何工作**。

## 表现层修复：主裁瞬移与官员标识（2026-09-02 晚，家里的机器）

> 只动表现层（`js/matchview.js`、`css/style.css`），**引擎一行未改**，缓存仍是 vcfm-v241。
> 触发点：用户看比赛高光时指出「主裁移动怎么和球一样同步或者瞬移」，并要求圆点里加字母。

### `_updateOfficials` 的四处缺陷（读代码找到，三处有实测）

讽刺的是原代码自己的注释写着「平滑系数刻意比球员慢：裁判是跟着跑的，**不该和球同步瞬移**」——
意图写对了，实现三处违背它：

1. **`const side = bx >= 50 ? -1 : 1`**：球每次横穿中线，目标 x 从 `bx+11` 跳到 `bx-11`，**一次跳 22 格**。
2. **`ty = by + (by >= 50 ? -7 : 7)`**：同一写法在纵向，过半场线**跳 14 格**。而 y 是进攻轴，每次转换都过。
3. **目标点是球瞬时位置的刚性偏移**：长传飞行、死球摆位瞬移到角旗，全被原样继承。
4. **`if (gap < 6)` 是死代码**：偏移恒为 ±11/±7，`Math.hypot(11, 7)` 恒等于 13.04，永远不小于 6。
   而且直接对格数取 hypot 是**混单位**（x 一格 0.68m、y 一格 1.05m）——
   正是第六节「顺手修掉一处混单位断言」点名过的写法，官员这处漏了。

**改法**：引入 `_playCentre` 局面重心，**先低通球位再算目标**（飞行中系数降到 0.02，几乎不追球）——
输入连续，输出就不可能瞬移；两处符号翻转换成 `-Math.tanh((play.x - 50) / 12)`，中线平滑过零、
两侧仍饱和到原来的 ±11 / ±7，几何意图不变；最小间距兜底改成按米算（`MIN_REF_GAP_M = 5`），
tanh 之后偏移在中线附近趋于 0，这条守卫才真的会成立；再加**单帧位移封顶**
`MAX_OFFICIAL_STEP_M = 0.7`（7 m/s × 0.1s，官员每 sim 帧更新一次），边裁跟越位线同样受它保护。

### 实测（`scripts/_referee-motion-probe.mjs`，4 场，soft 档 = 高光走的那条路径）

| 指标 | 改前 | 一版（锚真实主裁） | **二版（锚引擎球员）** |
|---|---|---|---|
| 峰值速度 | **55.46 m/s** | 7.00 | **5.00** |
| p99 速度 | 16.66 | 7.00 | **5.00** |
| p90 速度 | 8.00 | 4.83 | **4.09** |
| 中位速度 | 2.81 | 2.08 | **1.91** |
| 每场跑动 | **20.88 km** | 13.34 km | **11.49 km** |
| 单帧目标跳变最大 | **45.87 m** | 9.37 m | 8.90 m |
| 与球间距中位 | 10.71 m | 11.07 m | 12.99 m |

`gap < 6` 的命中数改前 **0**、改后 5622——4 场 × 54000 步 = 216000 次求值里旧分支一次都没成立，
**死代码的实测证明**，不只是推导。

### ⛔ 一版的锚点选错了，这是本次最值得记住的一条

一版把上限锚在**真实主裁**的冲刺峰值 6~7 m/s 上。看起来有据可依,实际是错的——
用户当场就看出来了：「球去到哪，**主裁跑的比追球的球员还快**」。

补量了引擎自己的球员速度才知道他说得对：

```
追球者 中位 2.10 m/s / p90 4.82        全体外场 中位 2.25 / p90 5.42 / p99 5.72
一版主裁 中位 2.08     / p90 4.83  ← 与追球者逐项持平，p99 7.00 反而超过全体 p99
```

**在一个球员只跑 2 m/s 的世界里，按真实人类封顶 7 m/s 的裁判就是火箭。**
判据必须是**引擎自己的球员**，不是现实世界——现实参照只有在引擎本身已经对齐现实时才可用，
而这个引擎的球员速度中位 1.91 m/s 对 FM26 的 2.75 m/s，差着 30%。

二版按档位曲线扫了五档（`track` / `k` / `cap` 三个量同向缩放），取 C 档
`track 0.035 / k 0.10 / cap 0.50`：中位、p90、p99 逐项低于上面那组，
**顺带把一版留下的「每场 13.34 km 偏高 11%」修掉了——11.49 km 落进真实的 10~12 km 带。**
再慢一档（D/E）距球中位涨到 14.6/16.5m，主裁开始明显落后于局面，不取。

⚠ 探针里 `追球者峰值 821.57 m/s`、`全体峰值 13.99` **不可信**：定位球会把球员瞬移回阵型，
那不是跑动。只读中位与 p90/p99。

### 官员标识：主裁 R / 边裁 A

`_spawnOfficials` 给三个圆点加 `textContent`（主裁 `R`、两名边裁都是 `A`），
CSS 加 flex 居中 + 900 字重 + 深色描边，主裁 10.5px、边裁 9.5px。
**保留 `aria-hidden="true"`**：官员是不可点击的装饰标记，把字母暴露给读屏只会在 22 名球员之间
插三个无意义朗读点。
肉眼一点：`R` 在 18px 圆点里偏挤，主裁那圈径向渐变左上角偏亮，对比度不如边裁的 `A`——
能读，要更清楚可把主裁字号提到 11px 或压暗渐变。

### ✅ 浏览器验证（新增 `scripts/_officials-visual-verify.mjs`，走真实代码路径）

探针量的是**照抄一遍**的公式，证明不了仓库里那份 `_updateOfficials` 真的这么跑，所以补了浏览器验证：
Playwright 走「新档 → 循环推进到比赛日 → 快速高光」（`#btn-sim-fast`，正是用户看到瞬移的那条
`playSimTimeline` → `_updateOfficials(true)` 路径），然后在页面内用 rAF 逐帧记录
`.mp-official.referee` 的 `style.left/top`。

- 字母：主裁 `R` / 边裁 `A`×2，字号 10.5 / 9.5px，`display: flex`，`inkWidth` 16 / 14（文字真被排版），
  `aria-hidden="true"` 保留，尺寸 18×18 / 16×16，页面无报错。
- 运动：1200 个采样帧里 **1097 帧有位移，单帧最大位移 0.700 m**——恰好等于 `MAX_OFFICIAL_STEP_M`。
  **上限在真实路径上生效**，不只是探针里生效。断言放宽到 2.5m（一个 rAF 间隔偶尔挤进两次更新），
  旧实现单帧目标跳变最大 45.87m，任何回归都会远远撞穿。

⚠ 写这个脚本踩的三个坑，重写前先读：
1. **块注释里不要出现 `.tmp-*/` 这种字符串**——`-*/` 会提前终止 `/** */`，
   `node --check` 报的是几行之后一个莫名的语法错，很难反推。
2. `browser-e2e.mjs` 等的哨兵键名还是 `vcfm-sw-reloaded-v235`（缓存已到 v241），
   无头模式下等不到。真正的就绪信号是 `window.vcfmMainApi`。
3. 一次 `#btn-advance` 推不到比赛日（引导步骤本身吃掉天数），要循环推进。
4. 比赛屏打开时是赛前状态，**官员不更新**——不点 `#btn-sim-fast` 的话量到的是静止画面
   （第一版就量出「有位移帧 0」）。

### ⚠ v240 那份「已验证」是假安全感

v240 条目引用的 `scripts/_officials-visual-verify.mjs` **当时从未提交**（`git log --all` 查无此文件），
而且它断言的是「主裁中位距球 12.6%、范围 [6.1, 14.6]」——**旧实现偏移恒为 ±11/±7、
距离由构造保证恒 13.04 格，那条断言怎么测都过**。它从没测速度，也没测连续性，
所以四处缺陷一个都没暴露。本次重建的同名脚本只断言**速度与连续性**。
`verify.mjs` 至今没有任何一项覆盖 officials——要挡住回归，应把这个脚本挂进去（需要 Playwright）。

## `_separateAgents` 椭圆半轴：载荷，不是笔误（2026-09-03 上午）

> 交接里定位的下一个瓶颈。`minD = 2.85` 是**格数**，而距离用 `Math.hypot(dx, dy)`
> 直接对格数取模——x 一格 0.68m、y 一格 1.05m，所以这条几何下限在真实空间里是个
> **椭圆**：横向 2.85×0.68 = **1.94m**，沿球门方向 2.85×1.05 = **2.99m**。
> 防守者站在被盯者与球门之间，两人连线基本沿 y，撞的正是 2.99m 那一侧，
> 而实测最近防守者距离中位 2.76~2.83m **正好压在上面**。

### ⛔ 看起来像混单位，实测证明是载荷

做了两个各向同性的米制档，24 场标定（种子 165000..）：

| minD | 进球 | 转化率 | 直塞 | 强队分/场 | 强队净胜球 |
|---|---|---|---|---|---|
| 盯人地板那棵树（**旧椭圆**） | 3.08 | 10.2% | 0.67 | 1.79 | +12 |
| 米制 **1.94**（纯横向半轴） | **2.29** ❌ | **7.9%** ❌ | **0.25** ❌ | 1.54 | +6 |
| 米制 **1.10** | 3.04 | 10.8% | **0.21** ❌ | **1.29** ❌ | **−4** ❌ |

**两个方向都破线，破法还不一样**：
- 1.94（横向半轴）：纵向下限从 2.99 压到 1.94 后，防守者可以坐进纵向传球线，
  进攻产出整条塌掉——进球 −0.79、转化率 −2.3pp、直塞 −0.42，三条都出护栏。
- 1.10：进球和转化率好看，但**身体能贴到 1.1m 时弱队光靠贴身就能捂住强队**，
  24 场胜率 33.3%、净胜球 +12 → −4、强队积分 1.79 → 1.29 分/场（下限 1.5）。
  传球线也一起没了（直塞 −0.46）。

合起来只有一个解释：**那个椭圆是载荷，不是笔误。引擎的进攻产出依赖
「防守者沿球门方向被挡在 2.99m 外」**，而贴身防守被同一个数挡着（实测最近防守者
距离中位 2.76~2.83m 正好压在 2.99m 上）。

12 场档位曲线（`_separation-mind-calibration-probe.mjs`，种子 372000..）也
印证了方向分离：禁区指标在 1.94 与 0.90 之间只差 0.15m（最近防守者中位 2.10 → 1.95、
>5m 完全无人 15.1% → 13.8%）——**收益几乎全部来自「改成各向同性」本身，不是量级**。

### 📝 参数化但不抹平

**动它之前必须先补上进攻产出**（把球打到防线身后那条路，见下一节「主线」），
不能单改。所以现在只参数化（常量 `SIM.SEPARATION_MIN_DISTANCE_UNITS` +
引擎选项 `separationMinDistanceUnits`），让那次配对可以直接扫它，默认值仍是 2.85。
参数化时也把另一处混单位修掉了——相向速度的法向投影：`a.vx/vy` 是格/秒
（见 `SIM.MAX_PLAYER_SPEED` 的注释写着 %/秒），和米制单位法向直接点积同样是混单位。

🔧 在 `engine.js:4631` 把它改写成「先换成米/秒 → 点积 → 回写时按轴换回格/秒」，
单独把这一处改对后重跑 `set-piece-presentation-audit` 不变——说明当前标定不依赖这个
bug，修掉不改变行为。（旧注释只写了「距离与推开量」，实际有两处。）

### ⚠️ `cce4eba` 的 audit 那一半是残缺的，已回退（2026-09-03 下午）

`cce4eba` 给 `box-defending-audit` 加了三条断言，但**它们引用的
`SIM.SEPARATION_MIN_DISTANCE_M` 从未被加进 `engine.js`**，于是 audit 直接红在
`:228`（`undefined m`），`verify.mjs` 跑不到底。而且 commit message 写着
"keep the ellipse"，断言却要求各向同性——**意图与实现互相打架**。

处理（`2648943` + 本次提交）：
- 移除那三条断言与 `solveGap` 夹具，**恢复被 `cce4eba` 顺手删掉的
  `assert.match(/const need = minD;/)`**——它是「全场单一半径」的唯一守护，
  当时打算由新断言接管，新断言没落地就成了覆盖空洞。
- 断言原文与夹具留在 `cce4eba` 里，用
  `git show cce4eba -- scripts/box-defending-audit.mjs` 取回。
  夹具本身是好东西（把其余 20 人标 `sentOff`，否则会量到「三人共线的链」而误读成各向异性）。

**⛔ 恢复它时不要照抄那条 `[1.5, 2.2]` 的带。** 上面那张 24 场表证明
**1.94 自己就破三条护栏**（进球 2.29 / 转化率 7.9% / 直塞 0.25），而 1.94 正落在
`[1.5, 2.2]` 之内——这条带允许了一个同一批实测已经证伪的值。带必须连同
「补上进攻产出」之后的新标定一起重新推，不能沿用。

**教训（与 `vcfm-calibration-compensation` 同族）**：断言分两种，
「描述现状」与「描述目标」。这条是后者。**红了不要改断言去迁就代码**——
第一版处置就是把它改成 `goalwardGap ≈ 2.99`，而 2.99 = 2.85 格 × 1.05 m/格，
那个数存在的唯一原因就是格数被当成米用，等于把防回归的绊线反转成锁死回归。
已回退（`2648943`）。

### 🔍 这次证明了「第一版探针只量进球/射门会漏掉真实瓶颈」那条教训

第一版档位曲线只有进球/射门/角球三列，**`strongVsWeak` 与 `throughPasses` 两列
不在表里**，于是 1.10 和 1.60 看起来都无害（12 场进球 1σ 约 ±0.49，四个档互相都在
噪声里排不出名次）。两列已补进探针，读法改成「**先看最后两列**」。

**我自己又犯了一次 `vcfm-calibration-compensation`**——修好一处失真会把另一处顶出护栏，
同表量连带指标。放松几何下限会**同时**压掉「技术优势」和「传球线」，不只是进球和防守贴身度。

## 🅱️ 直塞第 2 门 `advance`：0.35 → 0.20，主线第一次真正落地（2026-09-03 下午）

> **主线（最后三区）的进攻产出这条路通了。** 直塞 **0.63~0.73 → 2.29~2.42 次/场**
> （真实 3.38 的 68~72%），全量 `verify.mjs --full` 绿，缓存升到 **vcfm-v242**。
> 五次尝试之后第一次有档位同时满足「抬起直塞」与「所有护栏不破」。

### 🔑 为什么是这道门，而不是跑位

`release{R}`（放开跑位目标的越位夹取）已经证明了机制，但越位被打到 24~37 倍，不可用。
关键事实是**传球飞行段本来就自由**（`engine.js:1721` 对预定接球人有更早的提前返回，
不经过 `_clampOffside`），所以缺的从来不是「能不能跑到身后」，而是**「把球打到身后」这个决定**。
决定被两道门挡着，都在 `_passCandidates` 的直塞分支里：

| 门 | 位置 | 原值 | 含义 |
|---|---|---|---|
| 第 2 门 `advance` | `:2934` | `> 0.35` | 接球人须比持球者近门 `0.35×40` = **14 单位** |
| 落点 `okOffside` | `:2943-2945` | `± 2` | 直塞落点不得越过越位线 2 单位以上 |

**实测结论：瓶颈全部在第 2 门，落点那道门单独动几乎无效。**
放宽落点容差把「落点通过率」从 38% 抬到 76%，直塞却几乎不动——因为上游第 2 门
只放过来 120~166 个候选。放宽第 2 门则把候选数抬到 5~6 倍（127→639 / 118→698）。

### 📊 标定曲线（`_through-pass-gate-calibration-probe.mjs`）

标准档 6 场 × 两窗口（种子 372000.. / 373000..），先排除极端档：

| 档位 | 越位 | 直塞 | 进球 |
|---|---|---|---|
| control | 5.50 / 4.17 | 0.83 / 0.17 | 2.83 / 3.33 |
| advance15 | 4.08 / 3.92 | 3.00 / 2.50 | 3.17 / 2.00 |
| offTol4 | 5.92 / 4.83 | 1.33 / 1.00 | 2.33 / 3.67 |
| offTol6 | 6.00 / 4.50 | 1.67 / 0.83 | 2.33 / 3.50 |
| combined(15+6) | 4.00 / 4.50 | **7.33 / 7.50** | 2.67 / 4.00 |

`combined` 两窗口都是真实值的 2.2 倍，**直接排除**。`offTol` 单独近惰性，排除。

### ⛔ 约束面不在标准档，在**后台档**

`advance15` 标准档全绿（24 场：进球 2.75、强队 2.00 分/场、净胜球 +19），
却在 `match-realism-audit 24 background` 上把进球顶到 **3.46**（护栏顶 3.3）。
后台档是 0.3s 步长 + 4 趟分离，**生涯里其余联赛的比赛全部走它**，
所以它才是这条杠杆真正的约束面。出货门（24 场 background，种子 165000..165023，
即 `verify --full` 实际跑的那一次）三点：

| gate | 进球 | 直塞 | 转化率 | 出货门 |
|---|---|---|---|---|
| 0.35 干净 | 2.88 | 0.73 | — | ✓ |
| **0.20 ★采用** | **3.00** | **2.42** | 10.1% | ✓ 余量 0.30 |
| 0.25 | 3.21 | 2.29 | 10.9% | ✓ 余量仅 0.09 |
| 0.15 | 3.46 | 3.21 | 11.4% | ❌ |

`0.25` 被 `0.20` **双向占优**（余量更小、直塞反而更少），排除。

### ✅ `advance20` 的落地实测（全量绿）

| | advance20 | 快照/护栏 | |
|---|---|---|---|
| 进球（标准 24） | 2.83 | 2.5~3.3 | ✓ |
| 进球（后台 24） | 3.00 | 2.5~3.3 | ✓ |
| **直塞** | **2.29 / 2.42** | 真实 3.38 | 68~72% |
| 转化率 | 9.7% | 9~15 | ✓ |
| 传球成功率 | 81.3% | 72~88 | ✓ |
| 传中占比 | 6.9% | 3~14 | ✓ |
| **强队分/场** | **2.08** | 1.79 → **+0.29** | ✓ |
| **强队净胜球** | **+22**（47:25） | 须为正 | ✓ |
| 胜率 | 62.5% | — | |
| stalls | 0 | 须为 0 | ✓ |
| `crowdedPairs`/场 | 1.58 | 上限 6 | ✓ |
| boxSeconds | 1109.83 | 上限 1200 | ✓ |
| 门将抱球偏离 | −3.13 | 容差 4 | ⚠ 最贴限的一项 |

**技术优势被放大而不是抹平**（1.79 → 2.08 分/场、净胜球 +12 → +22），
正是 `_separateAgents` 失效模式的**反面**。机制上说得通：直塞奖励 vision/passing/pace，
能力高的队更吃得到这条路。

**射门几乎不动（28.67 → 29.3），涨的是转化率。** 这条杠杆改的是机会**质量**不是数量
——比「让球队多射门」更符合真实足球。射门 14.6/队仍在真实带 11.9~13.8 之外，
但那是存量问题（见「冻结快照静默烂了」那节），本档还略微改善。

### ⚠️ 两个统计教训，比这次的数更值钱

**1. 嵌套窗口不是复现。** `match-realism-audit` 的种子是 `165000 + match`，
所以「24 场 → 48 场」是**把同一个窗口加长**，24 场窗口是 48 场的子集。
我曾据此说「advance15 超顶在两个样本量上同向复现」——**那个推理是错的**，
两次很大程度是同一批比赛各算了一遍。要真复现必须**换窗口起点**，
而这支审计的起点是写死的（想换得改审计）。

**2. 进球这一列在 48 场分辨不出来。** 后台档 48 场四点曲线
（0.35→2.98、0.25→3.04、0.20→2.75、0.15→3.35）**对门槛不单调**，
而 0.25 与 0.20 的直塞量几乎相同（2.27 / 2.25）进球却差 0.29。
四档散布 0.60 与 48 场 SE≈0.25 同量级——**这一列由噪声主导**。
所以采用依据不是「0.20 的进球比别人好」，而是「0.20 在出货门上留了最大余量，
且在四个独立口径下都没出护栏」。想把这一列真正定下来需要 192 场（SE≈0.12），**未做**。

**3. 探针与审计不可互换。** 探针用能力 15 / 种子 372000，审计用能力 13 / 种子 165000。
同一档位下探针后台进球 +0.04、审计后台 +0.37——**总体不同，不是抽样**。
判护栏只认 `match-realism-audit` 的口径，探针那几列只看方向与量级。

### 🔜 下一步

- **直塞仍只有真实的 68~72%**，剩下的缺口要么继续放门槛（被后台档进球挡着），
  要么去动落点那道门 + 同时补越位下调预算（`peelB`+`hardA`，4.54 → 2.04）。
  越位现在仍是 4.3~4.9 次/队场，真实 1.70，**这条老账没动过**。
- 想再放门槛，先把「后台档为什么对直塞比标准档敏感」查清楚
  （转化率 10.4% → 11.4% vs 标准 9.4% → 9.7%）。那可能是后台档的保真度缺陷，
  修它比继续在门槛上折衷更值。

## ⛔ `npm test` 不包含 24 场标定，那份冻结快照因此静默烂了（2026-09-03 上午）

> 发现过程：给禁区盯人地板跑 `node scripts/verify.mjs --full` 时最后一项红了
> ——`background shots diverged from the fixed-seed standard profile`。
> 追下去发现**这条红与本次改动无关，`a82cb54` 上就已经红了，而且红三条。**

### 🔑 两个叠在一起的事实

1. **`npm test` = `node scripts/verify.mjs`，不带 `--full`。**
   而 `match-realism-audit 24` 与 `24 background` 只在 `fullChecks` 里（`verify.mjs:121`）。
   所以历次留档里的「verify 75 项全过」**从来不包含这两项**。
2. **`STANDARD_PROFILE_REFERENCE_24` 是写死的历史快照，不是当场跑出来的标准档。**
   那条断言的名字叫「background 与标准档不一致」，实际量的是「与一份快照的偏离」。

快照上次刷新之后，引擎经历了门将站位、门框、开球等多轮**有意**标定，没人回来刷这份数：

| | 快照 | `a82cb54` 标准档 | 偏离 | 容差 |
|---|---|---|---|---|
| 射门 | 24.83 | 29.21 | **+4.38** | ≤3 ❌ |
| 门将抱球 | 12.96 | ~17 | **≈+4** | ≤4 ❌ |

`a82cb54` 上跑 `match-realism-audit 24 background` 实测 **exit 1，红三条**：
`shots +4.42`、`goalkeeperClaims +5.92`、`openGoalShots +0.79`（`strongPointsPerMatch −0.50` 刚好压线）。

**本次改动反而把它改好了**：`shots` 偏离 4.42 → 3.30、`goalkeeperClaims` 5.92 → 4.54。
但两项仍超差，因为分母是那份烂掉的快照。

### ✅ 做了两件事

1. **刷新快照**（按文件注释本来的规定：「only after an intentional standard-engine
   calibration and a fresh 24-match run」）到本轮标准档 24 场实测：
   进球 3.08、射门 30.13、传球 1094.63、传球成功率 82.4%、犯规 26.17、
   空门机会 0.83、门将抱球 17.71、门将对抗 8.46、强弱分离 1.79。
2. **给快照加自查**：标准档跑 24 场时也算同一份 delta（`referenceDelta`）并用同一组容差
   断言。刷新之后它应当逐项为 0；一旦引擎又被有意改过而没人刷这份数，
   **会在标准档这一侧立刻失败并指向正确原因**，而不是等某次 `--full` 让 background 背锅。
   失败文案里带 `the frozen STANDARD_PROFILE_REFERENCE_24 is stale; refresh it deliberately`。

### 🔜 留给下一个人的判断

自查只解决了「失败时指向对的地方」，**没有解决「没人跑 `--full`」**——两项各 6 分钟，
加进 `npm test` 会让默认门从约 6 分钟变成 18 分钟。这是个取舍，本轮**没有**替你决定。
但要知道：**在做出决定之前，标准档与 background 的一致性实际上处于无人看守状态。**
凡是动了引擎标定的一轮，收尾必须手动跑一次 `node scripts/verify.mjs --full`。

## 官员跑位进 verify：import 真实现，抄件会漂（2026-09-03 上午）

> 交接里那条「`verify.mjs` 至今没有任何一项覆盖 officials，要挡住回归应把脚本挂进去
> （需要 Playwright）」——**结论是不需要 Playwright**，运动学那一半可以纯 Node 覆盖。

### ✅ 新增 `scripts/officials-presentation-audit.mjs`（已挂进 `verify.mjs`，2 场约 17s）

做法：**`import { MatchView }` 然后 `MatchView.prototype._updateOfficials.call(替身, true)`**，
替身只带那个方法真正读到的字段（`officials` / `ball` / `ballState` / `players`），
`_applyOfficials` 替成空函数（它全文四行，只把坐标写进 `el.style`，是纯输出端），
球的轨迹来自真正的 `SimEngine`。`matchview.js` 在 Node 里能干净 import，所以不需要浏览器。

断言五组：
1. **先证明不是在读静止画面**（`vcfm-tracker-lock-on-static` 那条教训）：
   ≥50% 的帧必须有位移，样本 >1 万帧。实测 108000/108000 帧都在动。
2. **不得瞬移**：峰值 < 4 m/s。实测 **3.80 m/s**，正是 `MAX_OFFICIAL_STEP_M` 的上限；
   旧实现单帧目标跳变 45.87m（≈458 m/s）。
3. **不得比追球球员还快**：不写死 m/s，而是锚到**引擎自己那一帧离球最近的非门将**的 p90。
   实测主裁峰值 3.80 < 追球者 p90 **4.82**。引擎改球员速度时这条会跟着动。
4. **不得被球锁死距离**：离球跨度必须 >12m。实测 **60.99m**，中位 **17.84m**
   （真实主裁离球 15~20m）。旧实现偏移恒为 ±11/±7 格、`hypot` 恒 13.04。
5. **边裁各守一条边线与一半场地**：x 恒为 3 / 97，y 范围 2.24~50 / 50~97.78，且都在跑动。

### ⛔ 为什么不用已有的两支

- `_referee-motion-probe.mjs` **照抄一遍公式**，证明不了仓库里那份实现真的这么跑。
  **抄件已经漂了**：它第 34 行写 `MAX_STEP_M = 0.7`「与 matchview.js 同值」，
  而 matchview.js 现在是 **0.38**。这就是「审计通过而画面明显不对」的机制本身。
- `_officials-visual-verify.mjs` 走真浏览器是对的，但要 Playwright + python 静态服务 +
  循环推进 25 天，两三分钟起。仓库里所有浏览器审计都在 `package.json` 单列、不进 verify
  清单，所以它保持原样，改为 `npm run test:officials-browser` 可发现。
  **DOM 那一半（R/A 字母、圆点尺寸、aria-hidden）只有真排版测得出来，仍由它负责。**

### 🔑 顺带修的测量缺陷

第一版拿「离球最近者」逐帧算速度，用 id 索引上一帧位置——换人时会把跨多帧的位移
当成一帧，死球摆位又会把整队瞬移到定位球站位，实测追球者峰值 **1037.9 m/s**，
p90 被抬到 5.37。改成「同一人连续两帧都是最近者、且两帧都不在死球摆位」才计一次，
峰值回到 6.88、p90 回到 4.82。**p90 正是第 3 条断言的分母，分母被污染的断言比没有断言更糟。**

## 禁区漏人：盯人地板落地，上限改成读球队防守档（2026-09-03 上午）

> 承上一节。闸门修好之后立刻重跑这条被挡回的改动——**当初挡它的那个数字本身就是噪声**：
> 同一改动在今天的树上把 `crowdedPairs` 从每场 3.33 带到 2.67，方向与当初记的
> 「12 → 25」相反。真正拦住它的是另一条护栏，见下。

### ✅ 改了什么

`engine.js` 的 `markingLimit` 三元式外面套一条地板：

```
boxMarkingFloor = min(禁区内进攻者人数, profile.markingCount + 1)
markingLimit    = max(原三元式, boxMarkingFloor)
```

上限**读球队自己的防守档**（`profile.markingCount`：pressing≥4 给 2、否则 1）再 +1：
守自家禁区时比平时多压一个人上去，其余仍留在 shape 上保护第二落点与反击出口。
所以默认俱乐部（pressing 3）上限 2，高压球队上限 3。人数用引擎自己的
`_inOwnPenaltyArea`（x∈(18,82)、y>80）数，与门将出击闸门同一个框。
`defensiveTransition` 那一支**刻意不动**（它按设计压到 ≤1，反击回追是另一回事）。

### ⛔ 第一版写死上限 3，被转化率护栏挡回

| 24 场标定 | 进球 | 射门（双方） | **转化率** | 角球 |
|---|---|---|---|---|
| 基线（`a82cb54`） | 2.83 | 29.21 | **9.69%** | 5.96 |
| 上限写死 3 | 2.58 | 29.04 | **8.90%** ❌ 破 9% 下限 | 5.33 |
| **上限 = `markingCount + 1`** | **3.08** | 30.13 | **10.2%** ✅ | 5.75 |
| 同档 **48 场**复跑 | **3.13** | 30.15 | **10.4%** | 5.73 |

机制清楚：`_emit("shot")` 在**出脚那一刻**就记（`engine.js:3191`），禁区里多站的人把
射门挡掉——**封堵算射门不算进球**，于是转化率被压低。基线本来只有 9.69%、余量 0.69pp，
经不起这一下。

**⚠ 真正该记住的根因**：射门量 14.5/队场，比真实带 11.9~13.8 高 6~22%。
**转化率护栏因此实际上在替「射门过量」站岗，而它挡住的是防守真实度。**
想让防守继续变真实，得先把射门量压回真实带——那是主线（禁区循环）的活。

**⚠ 同样要记住的方法论限制**：24 场的进球读数 1σ 约 **±0.34**（68 球的 Poisson），
转化率 1σ 约 **±1.2pp**。所以 2.58 / 2.83 / 3.08 这三个数**彼此都在 1σ 内**，
上表只能读作「两档都没有可分辨的进球代价，而写死 3 那档的转化率跌破了固定种子门槛」，
**不能读作「上限 2 提升了进球」**。选上限 2 的理由是**战术依据**（读球队自己的防守档，
不是魔数），不是这张表的运气。48 场复跑（3.13 / 10.4%）与 24 场逐项吻合，
说明这棵树确实稳定落在进球 3.1、转化率 10.3% 附近——**能确认的是「在护栏内且有余量」，
不是「比基线更好」**。

### 📊 12 场禁区指标（`_box-marking-probe`，种子 372000..）

| | 基线 | 上限 3 | **上限 `markingCount+1`** |
|---|---|---|---|
| **>5m 完全无人（持球）** | 19.6% | 14.8% | **16.1%** |
| **>5m 完全无人（飞行中）** | 20.8% | 15.9% | **17.2%** |
| 最近防守者 p90（持球） | 6.70 m | 5.81 m | **6.03 m** |
| 最近防守者中位（持球） | 2.83 m | 2.76 m | **2.76 m** |
| 门将站到门柱之外 | 1.7% | 1.9% | **2.1%** |

`box-defending-audit`（12 场，重做后的闸门）：`crowdedPairsPerMatch` 3.33 → **1.42**（上限 6）、
`markSharePct` 11.58 → **19.5**、最近后卫均距 3.411 → **3.20 m**。

**⚠ 不要把 3.33 → 1.42 读成「地板改善了拥挤」**：窗口间摆动本身就有 2.59，这个差值在噪声里。
能说的只是**它没有让拥挤变坏**，而这正是当初唯一的阻碍。

### 🔜 中位数没动，因为下一个瓶颈还在

最近防守者**中位**只从 2.83 挪到 2.76m —— 派了更多人去盯，但盯人者贴不上去。
原因见下一节：`_separateAgents` 的几何下限沿球门方向是 2.99m，中位数正压在上面。
**>5m 完全无人 16.1% 距真实（应接近 0）还远，剩下的空间在几何下限那边。**

## `crowdedPairs` 闸门重做：绝对次数 → 每场次数，门槛由三组实测夹出（2026-09-03 上午）

> 上一轮的交接把这条列为「醒来第一件事」：那个闸门挡回了一个有效的漏人改动
> （>5m 完全无人 19.8% → 16.8%），而它自己在同一改动家族上给过 3/12/15/25。
> 它改的是审计不是引擎，不可能弄坏玩法。

### 🔑 先量出来的三件事

| | 每场次数 | 逐场 |
|---|---|---|
| **基线**（审计的种子窗口 165000..165011） | **3.33** | 6,5,2,1,2,2,3,6,4,3,6,0 |
| 换种子窗口 166000..166011 | **0.83** | 0,5,0,0,0,0,0,3,2,0,0,0 |
| 换种子窗口 167000..167011 | **3.42** | 3,4,4,7,1,3,4,4,3,1,3,4 |
| **松弛失效**（`_separateSupportTargets` 换成空函数） | **9.25** | 12,10,16,9,5,14,8,8,5,8,8,8 |

**「窗口间摆动」才是这条固定种子断言该用的噪声模型**——换种子窗口就是换轨迹，
而任何动引擎的改动同样会从第一次死球起换轨迹。窗口内标准误只有 0.59，
但三个无辜窗口的均值落在 0.83~3.42，跨度 2.59，是窗口内标准误的 4 倍。
按窗口内标准误定门槛会重犯旧错。

### ✅ 改成什么

`box-defending-audit.mjs`：第 2 节从 4 场改成 **12 场**（约 92s，原 33s），
断言从「绝对次数 ≤ 14」改成 **`crowdedPairsPerMatch <= 6`**，
推导与三组锚点全部写在 `CROWDED_PAIRS_PER_MATCH_LIMIT` 的注释里。
无辜上界 3.42 + 3×0.59 ≈ 5.2，松弛失效下界 9.25 − 3×0.95 ≈ 6.4，6.0 落在其间。
顺带把 `boxTicks` 下限从 3000 提到 9000（实测 12.8 万）。

**⚠ 这仍是个弱检测器**，分离度只有 2.8 倍。刻意偏宽（宁可漏判也别再挡住真改进），
接应目标拥挤另有 `match-motion-integrity-audit` 在其种子上把关。想真正收紧只能换检测器。

### ⛔ 顺带发现：探针与审计的 `makeClub` 不是同一份

`_crowded-pairs-gate-probe.mjs` 的头注释写着「口径与 box-defending-audit 逐字一致」，
实际它的属性表**多写了一个 `crossing`**（审计没有）。属性表不同 → 球员数据不同 →
整场轨迹不同。对齐前后同一窗口从 2.92 变成 3.33，三个窗口的跨度从 0.75 变成 2.59——
**对齐前那组数会让人以为噪声只有实际的三分之一**。已对齐并在两支探针里写清。
新增 `scripts/_crowded-pairs-anchor-probe.mjs`（上锚，`_` 前缀不进 verify）。

这是本项目第 N 次「两把卡尺不同却声称同口径」。**下次写探针，属性表要从审计里复制，
不要凭记忆写。**

## 门将站位与禁区漏人（2026-09-03 凌晨）

> 触发点：用户报「禁区里守门员的站位和反应有问题，而且有些后卫还漏人严重」。
> ⚠ `box-defending-audit` 与 `goalkeeper-open-goal-audit` **都是过的** ——
> 两者都是人为摆位的合成场景，AGENTS.md 自己早就承认过「审计通过而画面明显不对」。

### 🔧 先补缺失的仪器：`scripts/_box-marking-probe.mjs`

`box-defending-audit.mjs:130` 有 `if (!ball.owner) continue;` —— **把球在飞行中的每一 tick
全丢掉了，也就是传中那一整段**。而且它只量「防守者→球」的距离，
**从来不量「进攻者→最近防守者」**，所以远柱边站一个没人管的人它完全看不见；
盯人断言又只是 3% 的地板（`markSharePct >= 3`）。

新探针按「球在飞行中 / 有人持球」分开，量禁区内每个进攻者到最近防守者的**米**距离。
禁区取**画面画的那个**（x∈[22,78]、主队 y>=84）——⚠ 引擎的 `_inOwnPenaltyArea` 用的是
x∈(18,82)、y>80，**比画面深 4.2m 宽 2.7m**，门将的所有出击闸门都跑在那个更大的框里。

基线（6 场）：**禁区里五个进攻者中有一个，5 米内没有任何防守者**（19.8%）。

### ✅ 已修：门将站位两处硬伤

| | 基线 | 修后 |
|---|---|---|
| **站到自家门柱之外的采样占比** | **36.7%** | **1.7%** |
| 离门线中位 | 6.26 m | 4.12 m |
| 横向偏离 p90 | 6.80 m | 4.08 m（= 门柱） |

1. `a.tx = clamp(50 + (b.x - 50) * 0.72, 40, 60)` —— **40 已经在 `GOAL_X0 = 44` 之外**。
   球从边路来时算出 29.8，被夹到 40，人就站到近柱外面，远角整个空着。
   改夹在 `[GOAL_X0, GOAL_X1]`：门将覆盖的是球门，不是球。
2. `depth = threat ? 7 : 4` 是**格数常量**（7.35m / 4.2m），与球离门多远无关。
   改成 `depthM = clamp(1.0 + dGoalM * 0.12, 1.0, 9)` 按米随球距内收。
   顺带把 `clampGkY` 的下界从 2 格收到 1 格 —— 旧值意味着他**永远碰不到自己的门线**。

**这条兜底分支覆盖所有传中、所有无主球、所有边路带球**（`underThreat` 需要真实的 `owner`，
而飞行中 `ball.owner` 是 null，且**没有传中分支**），所以它就是画面的常态。

顺带修 `_mostDangerousReceiver`：旧实现**只看 `|o.y - ownGoalY|`、没有 x 项**
（底线边的边锋排在点球点的中锋前面，且两人在 y 上交叉时身份就翻转 → 每 0.48~1.36s
重算一次不停换人），且**没排除 `sentOff`**（红牌后 `:1692` 只改 x 不改 y，
盯人者会去跟一个站在场外的人，而那个人同时吃掉 `screen` 职责与压迫者的 `shadowId`）。

### 🔑 门将站位就是「进球过量」的成因之一——24 场标定给了证据

| | 留档基线 | 只加门框 | 门框 + 门将修复 |
|---|---|---|---|
| 进球/场 | 2.88 | **3.25**（余量 0.05） | **2.83**（余量 0.47） |
| 射门（双方） | 31.13 | 30.46 | 29.21 |

门框那轮把进球顶到 3.25，我当时判断「那 0.37 大概率是重掷，真正该修的是进球过量的成因，
不是拿门框当替罪羊」——**判断对了，而成因就是门将**。修掉之后两个一起回到带内。

### ⛔ 漏人：改动有效但被 `crowdedPairs` 挡回，已回退

试的是给 `markingLimit` 补一条地板：禁区里有几个进攻者就至少派同样多的人（上限 3）。
理由很硬：普通传中点（x=15, y=88）离门 23.4m，默认 pressing=3 →
`markingLimit = 0`，**整个传中过程一个盯人都没有**（`engine.js` 的 markingLimit 三元式）。

实测有效：>5m 完全无人 **19.8% → 16.8%**，最近防守者 p90 7.18 → 6.09m。
**但 `crowdedPairs` 从 12 打到 25**（上限 14）。那个闸门本身不可信（同一改动家族给过
3/12/15，跨度是余量的 6 倍），但 25 超出了观测过的重掷区间，不能当噪声放过。
**🔜 必须先把 `crowdedPairs` 改成「每场次数 + 12 场样本」，才有资格判定这条改动。**

### 🔜 下一个瓶颈已定位：`_separateAgents` 的 `minD` 又是混单位

最近防守者距离中位 **2.81m**，而 `minD = 2.85` 是**格数** —— `Math.hypot` 在原始格上算，
沿球门方向合 **2.99m**。**中位数正好压在这个下限上**：贴身防守现在是被分离求解器
物理挡住的，而战术目标在危险区只要 1.1m（`markWeight` 那段）。
同一处的禁区内 3.35 膨胀早就因为同样理由被移除过（留档 v239），
**但格/米的混用留下来了**。今天这是第五次撞见混单位。

## 门框：立柱与横梁第一次成为可碰撞几何（2026-09-03 凌晨）

> `_resolveBounds` 过去只有 `underBar = crossZ < 2.44` 的二元判断——柱内且低于横梁就是进球，
> 否则出底线。**立柱与横梁不是几何**，所以画面上永远看不到打门框；`woodwork` 事件类型
> 与 matchview 的画法一直都在，但**空间引擎从没发过它**（只有旧的文字引擎在喂）。

### ⛔ 活球反弹试过并否掉——理由写在代码自己的注释里

第一版让球真的从门框弹回场内（恢复系数 0.55）。12 场实测：

| | 门框/场 | 进球/场 | 射门/队场 | 角球/场 |
|---|---|---|---|---|
| 基线 | 0 | 2.92 | 13.75 | 4.67 |
| 活球反弹（容差 0.5 格） | **0.92** | **3.50** | 15.33 | 5.42 |
| 死球 + 容差减半 | **0.50** | 3.83 | 14.92 | 5.17 |

三项一起涨，方向一致——反弹让球留在场内，替掉了原本的死球重启。而 `_resolveBounds`
的头注释写得很清楚：死球重启是**刻意**用来「让球彻底离开门口，从根上掐断
『射偏→门口篮板→再射』的射门画廊」的。**活球反弹等于把那个画廊请回来。**

所以采用的语义是：擦柱/中梁**不算进球**，照常走角球重启（射门方最后触球 → 角球给进攻方，
与真实的「打柱出底线」一致），画面上多一次「击中门框」。真实里打柱回到场内的情况**没有覆盖**，
那要连带重做死球重启的设计，不在这一批里。

容差：柱子 12cm + 球半径约 11cm ≈ 0.34m，但过线那一刻的插值落点本身带 0.1s 步长的
离散误差，等效容差比几何容差大——按 0.34m 取实测 0.92 次/场，减半到 `POST_TOL_X = 0.25` 格
／`BAR_TOL_Z = 0.17m` 后是 **0.50 次/场**，贴近真实的 0.4。

### 🔑 12 场分辨不出进球，24 场才能——这条比门框本身重要

同一个功能的三个变体给出进球 2.92 / 3.50 / **3.83**，跨度 **0.91**，而进球护栏带宽只有 0.8。
逻辑上门框只该让进球**变少**（擦柱进球变成角球），却读出三个都在涨——因为任何改动都会
从第一次门框事件起让**整场比赛重新掷一遍**。

`match-realism-audit 24` 的读数是 **进球 3.25、射门 30.46、角球 4.54，exit 0**——护栏内。
**12 场那个 3.83 是采样噪声，24 场才是能用的样本量。** 这个项目定 24 场是对的。

⚠ 但留档基线是 **2.88**（第 679 行），现在 3.25：**门框吃掉 0.37，护栏只剩 0.05 余量。**
与角球候选档卡在完全相同的位置。这 0.37 大概率仍是重掷而非因果，但**无论哪种，
下一个动引擎的改动几乎必然顶穿 3.3**。真正该修的是「进球过量」的成因，不是拿门框当替罪羊。

### 顺手补齐：三处高频折射的接触信号

之前只有门将指尖擦球（失败扑救的 18%）有 `_deflectPulse`。补上：
**防守者封堵**（`_emit("block")` 一直在发，但 matchview **没有 `case "block"`**）、
**门将封堵**（`gk_block`，同样没画法）、**第一脚失控**（`_miscontrolBall`，
概率 15~40%、快球最高 85%，过去**什么都不发**——这是「无缘无故拐弯」的主体）。
三处都只写一个视图读的字段，**模拟逐位相同**。

## 表现层修复：事件时钟慢一帧、门将抱球被画成扑救（2026-09-03 凌晨，家里的机器）

> 只动表现层与事件时间戳，**没有一个模拟决策被改**，缓存仍是 vcfm-v241。
> 触发点：用户看画面报了四条——球没进解说先说进球且停顿一下才进、门将没碰球先说扑救、
> **门将从来没接住过全是弹开**、球会无缘无故折射。

### ⛔ 先证伪一个我自己的假设

我最初判断是「射门结果在出脚那一刻就算完了，球再朝既定结局飞」。**错的。**
`_shoot` 里没有任何进球/扑救/偏出判定，只算瞄准点转速度；它甚至明写
`b._saveChecked = false; // 新射门允许门将掷一次扑救骰`。进球是**每 tick 真几何判定**
且带亚帧插值（`_resolveBounds`：按 `_prevY` 反解 `ratio` 求 `crossX/crossZ`），
扑救要求门将真的靠近球**这一 tick 扫过的线段**（`dPath > reach` 就不扑）。

顺带：GLM 那份 demo 的作者也猜错了方向（猜 VCFM 是「文字/事件驱动、没有球员坐标」）。
两个猜测都错，因为都没读代码。**这条记下来：这个项目的引擎是位置驱动的，别再被这个猜测带偏。**

### 🔑 真正的成因：事件时间戳用步首时钟，帧用步末时钟

`_stepOnce` 的子步循环里 `this.t = stepStartedAt + index * physicsDt`，看比赛时
`physicsSubsteps === 1`、`index === 0`，所以整步内 `this.t` 停在步首。而
`_stepBall` 已经把球推到 `T + 0.1`，随后 `_resolveBounds` / `_resolvePossession`
发出的进球/扑救事件被 `_emit` 盖上 **T**；而这一步之后录的帧盖的是 `eng.t` = **T + 0.1**。

叠加两处：`match.js:1523` 的 `cues[cueIdx].t <= t + 0.05` 再提前 0.05s；
matchview 是**先画帧再派事件**。合计提前 0.10~0.15 秒 —— 而一帧球走 4~6 米，
一脚射门整个飞行约 0.4 秒，**提前量占整段飞行的四分之一到三分之一**；
近射飞行被夹在 0.18 秒（不到 2 帧），文字能在球没离脚时就发出来。

那个「停顿」是显式写进去的：事件一发出，`main.js:9184` 立刻
`holdSimTimeline(380 / spd)` **冻结时间轴**，反复重画「球还没过线」那一帧，
然后 `rateMul = 0.55` 加导演的 0.52~0.58 把剩余飞行半速播完。1× 下进球 ≈ 0.65s、
扑救 ≈ 0.37s，且扑救还强制门将倒地 900ms、接触闪光画在**接触前**位置。

**修法**：加 `_emitTimeOffset`，`_emit` 用 `t: this.t + (this._emitTimeOffset || 0)`，
子步内置为 `physicsDt`、循环后清零。
⚠ **不要直接改 `this.t`**——所有冷却、决策节流、`settleUntil`、`deadBallUntil` 都读它，
推 0.1s 会改行为。事件时间戳没有任何玩法逻辑读取（`directResult` 是半场跑完后的纯后处理），
所以这一改**模拟逐位相同**，连随机数消耗都不变。时间戳对上之后，那 380ms 冻结自然落到
**球已进网之后**，变成正常的进球停顿。

### 🔑 门将其实会抱住，占扑救的 29.5%——是前端把结果扔了

`holdP = 0.18 + 0.12*handling + 0.16*反应时间`，取值 [0.216, 0.457]，中游门将约 0.295。
合成：**干净抱住 29.5% / 托出底线 56.4% / 拍进场内 14.1%**
（`tipOverP = clamp(0.8 + max(0, dt − SIM.DT) * 0.4, 0.8, 0.95)`，直播 dt = SIM.DT 恒为 **0.80**）。

**接住分支可达，不是死代码。** 看不见的原因是 `hold` 这个字段整个前端一次都没读过：
`matchview.js` 门将唯一的姿势分支是 `diving: pl.pose === "dive"`，**没有抱球渲染**；
`triggerDirectorMoment("save")` **无条件**强制 dive 900ms；文案永远「精彩扑救」。
用户的原话「都没有接住过，都是扑救」——**对画面成立，对模拟不成立。**

已修：`triggerDirectorMoment` 读 `opts.ev.hold`，抱住时姿势用 `"hold"`、角标「没收」、
字幕「稳稳抱住」；`adapt.js` 的文案分成「稳稳没收」/「把球扑出」
（不写「出底线」是因为 14.1% 的拍击是留在场内的，事件里没有 `tipOver` 可分辨）。

### ⚠ 中间层白名单静默丢字段——今天第三次

- `interpolateSimBall`（`match-presentation.js`）只返回 `{x,y,z,state,owner,netHit}`，
  **把 `deflect` 丢了**。引擎在门将指尖擦球处设了接触标记，注释自己写着
  「画面上就成了『球在无人接触的情况下自己拐弯』」，adapt 传了、matchview 也画了，
  而直播回放**永远**走这个插值器 —— 那个修复从写下那天起就是死的。已补。
- `adapt.js` 的 `pickFlavorEvents` 用**显式白名单**构造文案项，`hold` 不在列上，
  所以就算改了文案也读不到。已补。
- （第三次是 v240 那份 `_officials-visual-verify.mjs`，从未提交。）

**教训：这个代码库里「上游发了、下游想读」不等于字段能到——中间每一层都可能是白名单。
改表现层前先把整条链走一遍。**

### 顺手修掉：球门画宽了两格

`.mp-goal-mouth` 旧值 `left: 43%; width: 14%`，而 `left/width` 的 1% 就是一个 x 格
（球员定位同样用 `left: ${x}%`），引擎球门是 `GOAL_X0 44`~`GOAL_X1 56` = **12 格**。
画出来 14 格、每边宽 0.68m，于是「看着从柱内进了、引擎判出去」会周期性出现。
改为 `left: 44%; width: 12%`。

### ⛔ 还没做的（引擎侧真行为缺陷，都会动护栏）

1. **门框物理完全不存在**：`_resolveBounds` 只有 `underBar = crossZ < 2.44` 的二元判断，
   立柱与横梁不是可碰撞几何，球高过横梁直接变门球。`woodwork` 事件类型存在、
   matchview 有画法，但**空间引擎从来没发过它**。
2. **高频折射不发信号**：只有门将指尖擦球（失败扑救的 18%）有 `_deflectPulse`。
   防守者封堵（`engine.js:5087`，且触发半径 `d > 3.2` 是**原始格数**，纵向 3.4m 就折射
   ——又一处混单位）与第一脚失控（`:1115`，概率 15~40%、快球最高 85%）**什么都不发**。
   后者是「无缘无故拐弯」的主要来源。
3. `tipOverP` 恒为 0.80（扑救八成变角球）、扑救里的 `lateral > 7.2`
   与 `_goalOpportunity` 的 `lateralCover > 12`（**都比整个球门还宽**）。

## 交接（2026-09-02 14:15，上一次换机，内容仍有效；其「换机后已完成」一节记的就是今天公司电脑这半天）

> **本次完成：越位主线从「有个可疑着力点」推进到「修法已标定完成，但被进攻过量卡住」。**
> 提交 `d33c16f` 已推送 master，工作区干净（只剩故意不入库的 `.tmp-video/`）。
> **引擎代码一行未改**，所以缓存仍是 `vcfm-v241`，不需要升。

**结论链（按这个顺序读 v241 那几节）**：
Opta 真实值 **1.70 次/队/场**（十二季一手数据，容许带 1.4~2.1）→ 路径分解发现判罚
**A 路 50.7% / B 路 49.3% 各占一半**，B 路是「越位球员根本不是传球目标却抢先拿到飞行球」
→ 堵 B 路 + 禁止传给越位队友实测 **2.04，首次进入真实带** → **但进球同时 2.92→3.67/场,
破 `match-realism-audit:362` 的 2.5~3.3 护栏** → **过量越位一直在给进攻当刹车**,
必须与「压低禁区循环」成对做，不能先上。

**⛔ 已证伪，换机后不要再花时间碰这三处**：
1. `_clampOffside`（暴露量只有 3.3% 的传球存在越位队友，自律是够的）；
2. `_isOffsidePosition` 的 `tol`（被吹者越线中位 1.64m，擦线只占 1.4%）；
3. `_bestCross` 的乘性越位抑制，含 `preferOnside`（作用面 0.3%，只降 8%）。

**🔜 待做（越位侧，但排在主线之后）**：
- 路径 A 要动就得**同时覆盖 `_passCandidates` / `_bestCross` / `_bestCutback` 三个出口**——
  只改 `_passCandidates`（`hardA` 档）仅降 14%，传中与回做全从旁边漏过去了。
- **三个降幅不可相加**（传中硬禁 −1.21、`peelB` −2.08、`hardA` −0.62 互相重叠）,
  要合并档实测。
- `peelB` 的 `stalls`（看门狗清球）与传球成功率**未验证**，采用前必须跑
  `node scripts/match-realism-audit.mjs 24`。

**⚠️ 这些东西留在家里那台机器上，不随 git 走**（但**都不再阻塞任何工作**）：
- **视频素材**：`C:\Users\WXX\Desktop\2D比赛画面对比\`（FM26 Mobile 96MB / VCFM 60MB）。
  **公司电脑没有，但已经不需要了**——唯一要用它的 `cmp8_ball.py` 今天跑完并**以负结果关闭**
  （追踪器锁死在静态亮点上，见 v241 负结果留档九）。要重跑得先重写消歧策略,
  那是「素材下次可达时」的事，不是主线。
- `.tmp-video/` 里的中间产物（`cmp_dots_fm.csv`、`cmp_dots_vc.csv`、`ball.csv`、
  抽帧 PNG、`cand.pkl`）**故意未入库**,`.py` 脚本本身已在 `scripts/_video-compare/`。
- **`C:\Users\WXX\.claude\projects\F--VCFM\memory\` 的记忆文件是本机的，不随仓库走。**
  今天新增的三条（小样本占比不可信、标定带互相补偿、argmax 乘零不是排除）的**实质内容
  都已同时写进本文件**，换机后靠 AGENTS.md 即可，不必担心丢。

### 换机后的起步动作

```
git pull                                         # 取本次交接 + d33c16f
node scripts/offside-event-integrity-audit.mjs 8  # 越位基线，应报 4.56 并告警
node scripts/match-realism-audit.mjs 24          # 标定基线（verify.mjs 默认不含它）
node scripts/box-possession-sampling-audit.mjs   # 禁区循环基线 = 主线的对象
```
⚠️ `node scripts/verify.mjs` 跑满 75 项要 **40 分钟以上**（`ecosystem-audit` 单项就十几分钟）,
**不要接 `| tail`**——`tail` 要等管道关闭才吐字，后台会看着像卡死。直接跑或重定向到文件。

### 换机后已完成（2026-09-02，公司电脑）

- 三条起步基线**全部逐位复现**（越位 4.56 / boxSeconds 1092.12 / 进球 2.88），
  仪器与家里那台机器一致，可以直接拿旧数字做对照。
- 主线推进一步：v240 交接要求的「定位 `_attackPlan` 的具体分支」**已完成**——结论是
  **跑位塌陷只发生在最后三区**（占无球决策 54.5%，目标距球员仅 2.08m，而组织阶段是 9~10m），
  且开工时押的「`_clampOffside` 把纵深削掉了」被**证伪为放大器**（组织阶段削掉量中位与
  p90 都是 0）。详见下方「🔑 主线诊断（2026-09-02）」。
  新增探针 `scripts/_offball-target-branch-probe.mjs`（`_` 前缀，不进 `verify.mjs`）。
- 主线再推进一步：按诊断点名的杠杆跑完了**标定曲线**（新探针
  `scripts/_final-third-movement-calibration-probe.mjs`，12 场，两道自检都过、与 v241
  的 control 行逐位对上账）。结果是**改目标点公式这条路封死了**（`wingRotate` 54090 次
  覆写只换 2pp 位移、进球顶到 5.67；`midLate` 全线反向；`releasePass` 无接缝），
  但 `release{R}` 证明了机制：**允许把防线身后当目标，`boxSeconds` −57%、禁区触球 −61%、
  均速 +35%，而进球 2.67 仍在护栏内**——这是四次失败之后第一次做到。
  唯一障碍是越位被打到 **24~37 倍**，所以不可采用。
  下一个杠杆因此转到**传球决策侧**（`okOffside` 落点上限 + 第 2 门 `advance`）。
- **另起一条线（用户指出的角球还原度）**：已完成第 1、2 步——
  联网取到有来源的角球真实参照值（官方 PL/Pulselive `corner_taken` 四季 + The Analyst 落点分区
  + premierleague.com 十八季进球占比），并新增 `scripts/corner-structure-audit.mjs`
  把「画面对不对」变成 4 条硬断言 + 12 项告警，已挂进 `verify.mjs`。
  结论：**角球数量只有真实 59%、每个角球威胁只有 52%，摆位整体比真实争点区（0~11m）
  远 5~10 米，且开球时无人跑动。** 第 3 步（改摆位 + 建跑动原语）未实施。
  详见「🅰️ 角球真实参照值总表」。
- 角球线第 3 步的**标定曲线也跑完了**（`scripts/_corner-rework-calibration-probe.mjs`，12 场，
  自检逐场同分）：**`slots + delivery` 是可采用的候选**——每角球射门 39.3% 对上真实 38.5%、
  角球进球占比 10.3% 落进十八季带 9.8~14.2%、进球 3.25 仍在护栏内（但只剩 0.05 余量，
  且射门 14.67/队场比现状更远离真实带）。**两半单独做都更差**：只改落点会把球吊进无人区
  （射门率 23.2%→7.7%、进球 2.17 破下限），只改摆位角球一个不进却在别处多漏 0.66 球
  （仍未归因，但「留守/留前场」已证伪——留档十）。
  `runs` 跑动原语机械上通了（出脚时距目标 1.16m、峰值 2.79 m/s），但威胁顶到真实 3.7 倍，需加阻尼。
  **引擎仍未改一行。**
- **引擎仍未改一行**，缓存仍是 vcfm-v241。

## 交接（2026-09-01 17:30，上一次换机，内容仍有效）

> **本次完成：确认「球员为什么慢」的根因 + 裁判表现层实现（v240）**
> 
> **核心结论已定（三重证据）：甲「目标点太保守」成立，乙「走不快」被排除。**  
> 杠杆在 `_attackPlan` 的目标点生成，不在移动物理层。球员不是走不快，而是没有被指派更远的跑位。
> 
> 详见本段下方「v240 诊断：近静止球员的目标点画像」。现在有定量依据支持留档七的方向：应先**建出缺失的快速进攻通路**（无球冲刺、纵深插入、拉开空间），而不是继续压制循环。
> 
> **表现层已完成**：主裁判 + 两名边裁视觉化（z-index 2 压在球员下方、主裁 0.22 插值跟球、边裁钉 x 跟越位线），已升缓存 v240、审计通过、浏览器验证通过。
>
> 视频对比素材在**原机器桌面** `C:\Users\WXX\Desktop\2D比赛画面对比\`（FM26 767 秒 / VCFM 快速高光 361 秒），公司电脑上没有。脚本已提交到 `scripts/_video-compare/`。

**⚠️ 用户已明确指出：下面这些运动学数字不是他说的「FM 更智能」。** 他指的是**球员跑位、传球、射门与对真实足球的还原度**——决策层。已量出的只是症状（站着不动的人多、队形散），**说不出病因**；同一现象在引擎里可对应十几种原因。**不要把这些数字当成改动依据。**

- **仪器可信（这条是后续一切视频量化的前提）**：VCFM 视频侧剔除慢镜后位移中位 **1.91 m/s**，与 AGENTS.md v238 记的引擎内实测 **2.05 m/s** 相差 7%。两个独立测法交叉验证通过，所以视频侧数字可与 FM 直接比。
  - **必须剔除慢镜段，否则结论是伪影**：VCFM 这段录屏 **27.3%** 的帧处于持续低速段（慢镜/死球摆位/进球 hold），不剔除时中位只有 1.10 m/s，会把差异夸大近一倍。FM 同口径剔除 13.9%。判定方式见 `cmp7_slowmo_check.py`（每帧中位速度 <0.6 m/s 且连续 ≥4 帧）。
- **标定方法（换素材后照做）**：不要用草皮包围盒换米——草皮画到边线之外，FM 的绿色区域宽高比 1.72 而真实 105:68=1.54，差 11%。用**白色标线**的行列投影定边线矩形，再用**真实禁区纵深 16.5m 交叉验证**：FM 禁区线距底线 121.5px / 772px×105m = 16.5m ✓；VCFM 禁区端线占全长 15.7% → 16.5m ✓。
- 运动学对比（中位数；FM 检出 14/22 人、VCFM 17/22，**比例与量级可信，绝对值不可当精确数**）：

  | 指标 | FM26 | VCFM |
  |---|---|---|
  | 位移速度（剔慢镜） | 2.75 m/s | 1.91 m/s |
  | 近静止占比 <1 m/s | 16.2% | **34.0%** |
  | 单队纵向铺开 p90−p10 | 27.2 m | 35.9 m |
  | 单队横向宽度 | 35.8 m | 53.0 m |
  | 单队覆盖面积 | 1821 m² | **3996 m²** |
  | 最近队友距离 | 7.26 m | 9.52 m |
  | 单队禁区内人数 | 1.0 | 1.0（持平） |

- **✅ 证伪了 v238 的一半结论，可据此砍掉待办**：v238 判断「引擎缺少 team block 随球整体平移 + 长度压缩」。实测**平移不缺**——球队重心沿长向的移动标准差 FM 15.5m / VCFM 15.3m 几乎相同，范围都是 18~88m，VCFM 的球队确实跟着球整体前后跑。**真正缺的只有压缩。** 那条待办的工作量因此小一半。
- **方法学教训（写脚本时会再踩）**：分队不能把色相翻倍后用 `(sin,cos,sat)` 做 2-means——VCFM 的橙红 H≈18 与粉红 H≈167 在圆环上 cos 都接近 +1，被判成同一队（人数比 1311:19111，等于没分开）。改用色相直方图双峰模态 + 圆环距离分配，修正到 0.78。**分队人数比会打印出来，低于约 0.6 就不要采信依赖分队的指标。**
- **~~未完成~~ 已跑，但结果无效（2026-09-02 关闭）**：`scripts/_video-compare/cmp8_ball.py` 已在家里那台机器上跑通（exit 0），但输出是伪影——追踪器锁死在静态亮点上（VCFM 球速中位 0.00 m/s、球在禁区 4562 秒 vs 引擎自测 1092 秒）。详见 v241「负结果留档九」。**所以「球在禁区内的时长」至今没有任何外部参照值**（Opta 不公布，视频这条路也失败），引擎侧 1092 秒的对账要走「禁区触球 26.1 次/队/场」那条替代量。备忘里「真实足球 60~90 秒」仍是**无来源的推测，不可引用**。

### 下一步建议（按优先级）

> **⚠️ 2026-09-02 追加的关键约束（会改变第 1 条的做法）：越位修法已标定完成，
> 但它把进球顶出护栏（2.92 → 3.67/场，护栏 2.5~3.3），说明过量越位判罚一直在给
> 进攻当刹车。这反过来给主线加了一条硬依据：进攻侧产量本来就过高，
> 必须先把禁区循环压下来，越位修法才能跟着落地。两件事要成对做，顺序是先主线。**
> 详见 v241 的「标定：两条路径的修法都有效，但不能单独采用」。

1. **主线仍是 v239 遗留 #1——但现在有了定量依据支持「建快速通路」方向。** v240 探针已证明：近静止球员距离目标点中位仅 0.31m（96.3% <1.5m），而跑动中球员目标距离中位 7.65m。**杠杆在 `_attackPlan` 的目标点生成**——缺无球冲刺、纵深插入、拉开空间的决策。（⚠️ 2026-09-02 已收窄到**最后三区的四处分支 + 分支内那一处 `_clampOffside`**，见「🔑 主线诊断（2026-09-02）」。）留档六提出的「成对改动：同步压低禁区停留 + 放开近门射门意愿」现在有了第一个具体着力点。
2. **决策层的差距需要用户带路，不要再从像素里挖。** 用户看过两段视频，让他指出具体场景（例如「FM 的球员接球前已跑向空当 / FM 会做纵向穿插拉开空间 / FM 射门时机更果断 / 防守时 FM 整条线一起压」），再去 `engine.js` 定位对应决策分支、量当前行为、然后改。v240 探针已给出方向，但用户的观察能直接定位到具体场景。
3. 若要把运动学差距变成可回归目标，可考虑把「近静止占比」「单队覆盖面积」「最近队友距离」做成审计断言——它们**有 FM 作为外部参照值**，比项目现有那些「实测基线 + 10% 余量」的防恶化线更有指向性（那些只能防变差，不能指出目标）。但**先做第 1 条**。

### 换机后的起步动作

```
git pull                                    # 取本交接
node scripts/match-realism-audit.mjs 24     # 确认标定基线（verify.mjs 默认不含它）
node scripts/box-possession-sampling-audit.mjs   # 禁区循环基线
```
注意 **`node scripts/verify.mjs` 默认不跑 24 场标定**（`match-realism-audit` 在 `verify.mjs:85-88` 的独立 `fullChecks` 列表里）。本次会话曾因此被退出码 0 误导，实际已破三条下限。

## 🔑 主线诊断（2026-09-02）：无球跑位的塌陷只发生在最后三区（`scripts/_offball-target-branch-probe.mjs`）

> 本段执行 v240 交接「下一步」——「定位 `_attackPlan` 中目标点生成的具体分支，量哪些
> 球员本该被指派纵深跑位、却被给了近距离目标点」。**引擎一行未改**，缓存仍是 vcfm-v241。
> 三条基线已在公司电脑上逐位复现（见本段末）。

6 场、种子 372000..372005、标准档、能力 15、0.1s 步长，口径与 `_player-stillness-probe` /
`_offside-line-occupancy-probe` 一致。包装 `_thinkAttackOffBall` / `_applyAttackTactics` /
`_commitOffBallTarget` / `_clampOffside` 四处，每处只调用原方法一次、不消费随机数；
分类只用确定性判据（`_isWinger` / `_isPrimaryMidRunner` / `_isFullback` / `_offsideLineY`
都是纯函数）。自检：插桩与不插桩逐场同分（3-3）。样本 **320274** 次无球进攻决策。

### 结论一：塌陷是分区现象，不是全场现象

| 球在哪一区 | 决策数 | 占比 | 原始目标领先球 | 最终目标领先球 | **目标距球员** |
|---|---|---|---|---|---|
| 本方三区 | 65293 | 20.4% | 10.45（11.0m） | 10.43 | **9.05m** |
| 中场 | 80485 | 25.1% | 10.34（10.9m） | 10.11 | **9.67m** |
| **最后三区** | **174496** | **54.5%** | **−15（−15.8m）** | **−15** | **2.08m** |

- **组织阶段的跑位是正常的**：目标点领先球约 11 米，球员被要求跑 9~10 米，76%（本方三区）/
  68%（中场）的决策里目标点领先球 4 单位以上，即直塞第 1 门的门槛。
  **「引擎从不指派远目标」这个说法在组织阶段是错的，不要再引用。** v240 只量了
  「近静止球员距目标 0.31m」，没有按球的位置分区，因此把一个分区现象读成了全局现象。
- **球一进最后三区就整体冻结**：目标点中位落到球后 15.8 米，距球员本人只剩 **2.08 米**。
- **而这一区占了全部无球决策的 54.5%**：一支球队控球时间里超过一半，球已经在对方三区，
  而那段时间没有人在跑。这条比例**不依赖外部参照值也能判断不真实**——
  控球时待在对方三区的时间不可能多于自己两个三区之和。

### 结论二：❌ 证伪「`_clampOffside` 把纵深削掉了」——它在组织阶段一点作用都没有

开工前的假设是：`_clampOffside:3469` 把目标 y 夹到 `min(offY, ball.y)`（主队），即**任何
无球目标点都不得越过球**，于是反越位插上被结构性禁止。**实测把这条降级为放大器**：

| 调用点 | 调用数 | 真的移动了目标 | 削掉纵深中位 | 绑定参照=球 | 绑定参照=越位线 |
|---|---|---|---|---|---|
| 分支内（`_thinkAttackOffBall` 各 return 前） | 281237 | 64459（**22.9%**） | 6.15 | 14.7% | **85.3%** |
| `_applyAttackTactics:1237` | 320274 | **17（0.0%）** | 3.09 | 16.5% | 83.5% |
| `_commitOffBallTarget:1335` | 320274 | **80（0.0%）** | 0.24 | 16.5% | 83.5% |

- 本方三区与中场：削掉纵深的**中位与 p90 都是 0**——夹取在组织阶段完全不生效。
- 绑定参照 **85.3% 是越位线、只有 14.7% 是球**，所以「不得越过球」那一项本身不是主项。
- **第二、三处调用几乎从不移动目标**（17 / 80 次，均 0.0%）。「一次跑位算两次 clamp，
  用 0.2 秒缓冲避免第二次纠正第一次」这个设计在实测里等于只有第一次有效——
  想动夹取只需要动分支内那一处。
- 夹取只在最后三区咬人（削掉 p90 9.17 单位），而那时球本来就已经很深。

### 结论三：塌陷的构成——三分之二的最后三区决策是「按设计站在原地」

最后三区各分支（分母 174496，前十行覆盖 99.99%）：

| 分支 | 决策数 | 占最后三区 | 最终目标领先球 | clamp 削掉 | **目标距离** |
|---|---|---|---|---|---|
| `cb-hold` 中卫留守（`engine.js:3232`） | 38808 | 22.2% | −55.52 | 0 | **0.25m** |
| `mid-2nd-layer` 非前插中场压在球后（`:3219`） | 35765 | 20.5% | −18.50 | 0 | **2.92m** |
| `wing-home` 边锋默认档（`:3309`） | 26117 | 15.0% | −3.61 | 0 | **1.30m** |
| `DEF-home` 后卫回位 | 15467 | 8.9% | −53.59 | 0 | **0.35m** |
| `ATT-home` 前锋「前插纵深」（`:3342`） | 1123 | 0.6% | **0.08** | **7.96** | **1.70m** |
| `DEF-support` 边后卫套边 | 20509 | 11.8% | −1.31 | **7.05** | 3.85m |
| `wing-support` 边锋内切 | 16335 | 9.4% | 1.55 | **3.64** | 3.33m |
| `core-support` 核心要球 | 12824 | 7.3% | 0.38 | **2.50** | 3.94m |
| `one-two` 撞墙 | 5580 | 3.2% | −0.20 | **2.31** | 3.33m |
| `MID-support` | 1951 | 1.1% | −1.06 | **5.80** | 5.16m |

- **前五行 = 67.2% 的最后三区决策，目标距离 0.25~2.92 米**：中卫、非前插中场、边锋默认档
  全部被指到自己已经站着的那个点上。这不是笔误，是三处**刻意**写下的分支
  （`:3219` 注释「避免六七个人同时被吸到点球点附近」、`:3232` 注释「中卫留作防反保护」）。
  **问题不在「该不该留守」，而在「留守」被实现成了「站在一个固定点上不动」。**
- **真正该创造机会的那五行（合计 32.8%）目标距离只有 3.3~5.2 米**，而且它们**全部**被
  `_clampOffside` 削掉 2.3~7.1 单位纵深——唯一还在动的人，也被压回球所在的深度。
- **`ATT-home|final` 是夹取删掉纵深最干净的一例**：前锋「前插纵深」分支的原始目标领先球
  8 单位，夹取削掉 **7.96**，最终 **0.08**（与球齐平），而他距那个点只有 1.70m。
  同一分支在中场档（`ATT-home|mid`）削掉 17.53 单位后**仍领先球 24.76**——
  说明夹取本身不是问题，问题是**球已经深到没有空间可插**。
- 对照组织阶段的同一段代码：`wing-support|mid` 目标距离 **9.16m**、98.2% 领先球 4 单位以上；
  同一分支在最后三区只剩 **3.33m**、38.8%。**代码没变，换个分区就失效了。**

### 这条把留档四~七的四次失败串成一句话

留档四（传球估值 `distPen`）、留档五（support 目标硬收回边缘）、留档六（禁区停留时限）、
留档七（进入率/带球越线）四次都栽在同一句「循环与产出是同一个现象」。现在能说得更准：

> **引擎的无球跑位模型只在球到达最后三区之前工作。球一进最后三区，67% 的人按设计钉在
> 原地、剩下 33% 被越位夹取压在球的深度上，全队没有任何位移可供利用。**

于是持球人唯一可选的动作就是短传给 2~4 米外的一个静态队友——这正是 58.3% 留区传球、
346 次禁区回合、1092 秒禁区时间、7.25 秒蹲守的直接来源。**压制循环之所以每次都削掉进球，
是因为被压掉的不是「多余的循环」，而是这个引擎在最后三区唯一剩下的进攻手段。**
同时也解释了 v241 那条「修好越位会把进球顶出护栏」：越位停顿是目前唯一还在打断这个
静态循环的东西。

### 🔜 下一步（当时的判断，**已按这条跑完标定曲线**；结论见下方「⚖️ 标定」与被重写的「🔜 下一步」）

杠杆已收窄到**最后三区的无球跑位**：四处分支 + 一处夹取，都在 `_thinkAttackOffBall` 内。

1. `mid-2nd-layer`（`:3219`）与 `cb-hold`（`:3232`）：把「固定点留守」改成**有位移的留守**
   （沿禁区弧顶横向轮转、延迟插上），**而不是取消留守**——取消已被 `crowdedPairs`
   （上限 14、干净基线 12、余量 2）与留档五的「强队被反超 29–33」两头堵死。
2. `wing-home`（`:3309`）：最后三区边锋默认档目标距离 1.30m，而这是真实足球里最活跃的
   位置（前后点交叉、拉边再回插）。它单独占 15.0%，是四处里最便宜的一处。
3. `_clampOffside` 对最后三区支援者的纵深压制（削掉 2.3~7.1 单位）：真实的反越位插上是
   「出脚瞬间不越位、球到时已越过」，而这里把**目标点**也夹住，等于禁掉时间差。
   **这一条必须与越位修法成对做**——放开纵深会推高越位判罚，而 `peelB`+`hardA` 恰好
   备着 2.5 次/队场的下调预算（4.54 → 2.04）。
4. 只需要动分支内那一处 clamp：另两处调用实测各只移动目标 17 / 80 次。

⚠️ **先做进程内包装的标定曲线探针，不要直接改引擎**（留档四~七的共同教训）。
参照 `_offside-path-fix-calibration-probe.mjs` 的做法一次测完多档。
⚠️ 采用前必须跑：`node scripts/match-realism-audit.mjs 24`（进球 2.5~3.3、射门 11.9~13.8
每队场）、`node scripts/box-possession-sampling-audit.mjs`、`node scripts/box-defending-audit.mjs`
（`crowdedPairs` ≤14）、`node scripts/offside-event-integrity-audit.mjs 8`。

### ⚖️ 标定：上面四条改法只有「放开越位夹取」真的起作用——而它把越位打成 24~35 倍（`scripts/_final-third-movement-calibration-probe.mjs`）

12 场、种子 372000..372011。**纯测量，不改仓库代码**：包装 `_thinkAttackOffBall`
（原方法先跑完、之后才覆写 `tx/ty`，覆写公式全是状态与 id 的确定性函数）与 `_clampOffside`
（先调原方法，再复用它自己缓存的 `offsideRunBuffer` 做单调放松）。**覆写后不自己调 clamp**
——引擎随后的 `_applyAttackTactics:1237` 与 `_commitOffBallTarget:1335` 会照常各夹一次，
所以**每 tick 的 `random()` 次数与原版逐位相同**，档位差异只来自行为改变。
分支归属用与 `_offball-target-branch-probe.mjs` 逐字相同的分类器。

自检两道都过：control 与未打包装引擎 **12 场逐场同分**；且 control 的六个指标与 v241
`_offside-path-fix-calibration-probe` 的 control 行**逐位相同**（越位 4.54 / 进球 2.92 /
射门 13.75 / 传球 1089.67 / 传中 4.21% / 禁区触球 276.29 / 直塞 0.75）——两个独立探针对上了账。

⚠️ **`boxSec` 列只在本表内可比**：control 1024.26 用的是 372000 种子组，
`box-possession-sampling-audit` 的 1092.12 用它自己的种子组，**不要跨表相减**。
⚠️ **`均速`/`近静止` 与视频侧不同口径**（本表是最后三区、非持球、按 tick 均值；
视频侧 FM26 2.75 m/s / 16.2% 是全场位移中位数），**只可比量级，不可当逐位对账**。

| 档位 | 越位 | 进球 | 射门 | 传球 | 传中% | 禁区触球 | boxSec | 直塞 | 近静止 | 均速 | 目标距离 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| control 现状 | 4.54 | 2.92 | 13.75 | 1089.7 | 4.21 | 276.3 | 1024.3 | 0.75 | 43.2% | 2.00 | 4.50m |
| `wingRotate` 边锋前后点 | 3.13 | **5.67 ✗** | 15.42 ✗ | 1101.4 | 4.09 | 271.6 | 1025.4 | 1.42 | 41.2% | 2.06 | 4.69m |
| `midLate` 中场锚在球门 | 3.54 | **2.17 ✗** | 14.33 ✗ | 1081.0 | 4.55 | 276.9 | 1054.2 | 0.50 | **48.6%** | 1.85 | 4.22m |
| `release4` 还回 4 单位纵深 | **40.67** | 3.08 | 13.00 | 1087.8 | 9.25 | 160.5 | 610.0 | 1.17 | 37.5% | 2.40 | 5.75m |
| `release8` 还回 8 单位纵深 | **59.83** | 2.67 | 12.71 | 1114.4 | 12.94 | 107.5 | 437.1 | 1.25 | 33.0% | 2.70 | 6.88m |
| `release8` + `wingRotate` | **63.17** | 2.67 | 13.04 | 1124.0 | 13.25 | **83.6** | **370.5** | 0.92 | **30.0%** | **2.84** | 7.33m |
| `releasePass` 只在传球飞行中 | 4.54 | 2.92 | 13.75 | 1089.7 | 4.21 | 276.3 | 1024.3 | 0.75 | 43.2% | 2.00 | 4.50m |
| 真实 / 护栏 | 1.70（1.4~2.1） | 2.5~3.3 | 11.9~13.8 | 800~1250 | 3~14% | 26.1 | — | 3.38 | — | — | — |

#### ❌ 证伪一：`wingRotate`（边锋前后点）——它造出的是产量，不是位移

进球 2.92 → **5.67**（护栏 2.5~3.3）、射门 13.75 → 15.42（真实带 11.9~13.8）。
而**位移几乎没动**：54090 次目标覆写只把近静止从 43.2% 压到 41.2%、均速 +3%、
目标距离 4.50 → 4.69m；`boxSeconds` 逐位没动（1024.3 → 1025.4）。
**所以它不是把「循环产量」换成「通路产量」，只是在后点多放了一个蹲守者**——
产量仍然来自禁区蹲守（留档六量过的 7.25 秒），只是蹲得更靠后点。
这与留档五是同一枚硬币的两面：留档五把人从禁区赶出去、进球掉到 2.29；
这里把人送到后点、进球涨到 5.67。**禁区里站着几个人，直接决定进球数。**

#### ❌ 证伪二：`midLate`（第二线锚在禁区弧顶）——全部指标反向

近静止 43.2% → **48.6%**、均速 2.00 → **1.85**、`boxSeconds` 1024 → 1054、
直塞 0.75 → **0.50**、进球 **2.17**（跌破 2.5 下限）。**没有一项朝对的方向走。**
机制：把第二线拉近球以后，他们要走的距离更短、到位更快，然后照样站住，
同时还多占了禁区。**「移动接应目标点」不产生位移，只是换个地方站着**——
这已经是这条链上第六次同样的失败（留档四~七 + 上面的 `wingRotate` + 本条）。

#### 🔑 `release{R}`：本项目第一个真的压下禁区循环、而进球没破线的杠杆

把 `_clampOffside` 在最后三区削掉的纵深**单调还回 R 单位**（只在夹取真的拉回目标时生效，
且不超过原始目标；边后卫刻意不放开）。R=4 与 R=8 的表现：

- **循环第一次真的塌下来**：`boxSeconds` 1024.3 → **610.0 / 437.1**（−40% / −57%）、
  禁区触球 276.3 → **160.5 / 107.5**（真实 26.1，从 10.6 倍降到 4.1 倍）。
- **位移第一次真的出现**：近静止 43.2% → 37.5% / **33.0%**，均速 2.00 → 2.40 / **2.70 m/s**，
  目标距离 4.50 → 5.75 / **6.88m**。加上 `wingRotate` 到 **30.0% / 2.84 m/s / 7.33m**。
  （量级参照：视频侧 FM26 2.75 m/s、VCFM 1.91 m/s，口径不同见上。）
- **而进球与射门都留在带内**：进球 3.08 / 2.67（护栏 2.5~3.3）、射门 13.00 / 12.71
  （真实带 11.9~13.8，control 的 13.75 本来卡在上沿）、传球 1087.8 / 1114.4（护栏 800~1250）。
  **这是留档四~七连续四次失败之后，第一次「禁区循环大幅下降而进球不破下限」。**
- **✅ 已排除「停顿假象」这个混淆**：每队每场 40~60 次越位意味着一场上百次判罚停顿，
  循环下降有可能只是球被判罚拿走。所以本探针另记了分母「最后三区控球秒数」：
  control 2998.66s → `release8` 2215.51s（−26%），而 **`boxSeconds` 占最后三区的比例
  34.2% → 19.7%（−42%）**。归一化之后降幅仍在，**不是停顿造成的假象。**
- **⛔ 但代价是越位彻底失控：4.54 → 40.67 / 59.83 / 63.17 次/队/场，是真实值 1.70 的 24~37 倍。**
  v241 标定好的 `peelB`+`hardA` 只备着 2.5 次/队场的下调预算，在这个量级面前毫无意义。
  **R=4 与 R=8 都不可采用。**
- 另一处要盯的：传中占比 4.21% → 9.25% / 12.94% / 13.25%，**逼近护栏上限 14%**。
  进攻从禁区倒脚转成了传中，这本身也不真实。

**这一档的价值不在于能用，而在于它证明了机制**：静态禁区拥堵是被**目标点上的越位夹取**
按住的。一旦允许球员把防线身后的空当当作目标，他们就跑起来、球也不在禁区里倒了，
而产出还在。问题纯粹在于他们**留在**越位位置——真实的反越位插上有时间差
（出脚瞬间不越位、球到时已越过），而「目标越过防线」在没有时间差的引擎里
退化成了「永久站在越位位置」。

#### ❌ 证伪三：`releasePass`（只在传球飞行中放开）是空档——**因为那段路根本没有夹取**

按 Law 11 想做时间差：只在 `ball.state === "pass"` 且他是 `receiverId` 时放开纵深。
越位快照在 `_pass:2925` 出脚那一刻就取好，出脚不越位、飞行途中越过防线不构成越位，
所以这一档预期能拿到位移而不推高判罚。**实测覆写次数 0，全部指标与 control 逐位相同**
（`releasePass + wingRotate` 也与单独的 `wingRotate` 逐位相同）。

根因在代码里，不在模型：**`engine.js:1721-1741` 有一条更早的分支**——球在飞行、
本方出脚、且自己就是预定接球人时，目标点被**直接设成球的落点**（`b.targetX/targetY`）、
`a.offBallTarget = null`，然后 **`return` 提前返回**，根本走不到 `_thinkAttackOffBall` /
`_applyAttackTactics` / `_commitOffBallTarget`，因此**一次 `_clampOffside` 都不会调**。

- **所以飞行段的反越位插上引擎本来就是自由的，没有东西可放开。**
- 真正卡住的是**出脚之前**：没人被摆到「能当身后球目标」的位置，
  于是 `_passCandidates` 压根看不到 `advance > 0.35` 的候选。
- 这条也顺带说明**「接球人 96.4% 处于 fsm receive」**（留档六记的那条）不是巧合，
  而是这条提前返回的直接后果。

#### ⛔ 探针自身的一处写错，不要照那版重跑

第一版 `depthRelease` 实现成「`legalY` 只取 `offY`、丢掉 `min(offY, ball.y)` 里的球项」。
**那是错的**——`min` 的单调性决定去掉球项只会让下界离球门更远或不变，
所以它只可能**更严**、绝不可能更松。12 场实测正好符合「变严」：越位 4.54 → 4.58、
直塞 0.75 → 0.75、进球 2.92 → 2.92 逐位不变，而禁区触球 276.3 → **286.6**、
`boxSeconds` 1024 → **1068**（支援者被压得更靠后 → 更多留区倒脚）。
**这一档不能读作「放开纵深无效」。** 实测 85.3% 的夹取绑定在**越位线**而不是球上，
要放松的是线那一项——`release{R}` 才是。

### 🔜 下一步（本轮标定之后重写）

**已封死的**：改最后三区的**目标点公式**造不出快速通路。`wingRotate` 54090 次覆写换来 2pp 位移、
`midLate` 全线反向、`releasePass` 无接缝。原因是 `_thinkAttackOffBall` 是一个**逐 tick 的位置
控制器**：目标每 0.42~0.82 秒按当前球位重算一次，球员永远只需走几米就到位，
所以任何公式改动都只是换个位置站住。**它没有「跑动」这个原语**
（有时长、有触发条件、有到达时刻）；现有最接近的是 `off-ball-movement.js` 的 1.5 秒租约。

**已证实的**：`release{R}` 说明**只要允许球员把防线身后当目标，循环就会塌、位移就会出现、
产出还在**（`boxSeconds` −57%、禁区触球 −61%、均速 +35%，进球 2.67 仍在 2.5~3.3）。
唯一的障碍是他们**留在**越位位置，越位被打到 24~37 倍。

**由此定位到的下一个杠杆，在传球决策侧而不是跑位侧**（`_passCandidates`，
`engine.js:2825-2884`）——因为飞行段已经自由，缺的是「把球打到身后」这个决定：

1. **`okOffside` 的落点上限（`:2880-2882`）**：直塞落点被限制在越位线之后 **2 单位**以内
   （`leadY >= offY - 2`）。真实的身后球落点在防线身后 5~15m，是接球人**跑过去**接的。
   这一项直接决定了「有没有身后球可打」。
2. **第 2 门 `advance > 0.35`（`:2871`，即接球人须比持球者近门 14 单位）**：
   `_through-pass-gate-probe` 实测它独立通过率只有 6.9%、候选 advance 中位 −0.03。
   在最后三区持球人本来就很深，这道门按构造过不了。
3. **第 1 门 `aheadOfBall > 4`（`:2856`）淘汰 68%**，但那是「比持球者靠前」，
   与球队整体结构有关，先不动。

⚠️ 做法仍是**先跑标定曲线再改引擎**，并且这一次要**同时**盯两件事：
直塞（真实 3.38、现状 0.75）与越位（真实 1.70、现状 4.54）。
若某档能把直塞抬起来而越位只小幅上升，它才可能与 `peelB`+`hardA` 那 2.5 次的预算配平。
⚠️ 任何提高禁区占用的档位都要另跑 `node scripts/box-defending-audit.mjs`
（`crowdedPairs` 上限 14、干净基线 12，余量只有 2），本表不测它。

### 公司电脑上的基线复现（交接要求的三条，全部逐位吻合）

| 审计 | 本机实测 | AGENTS.md 记录 |
|---|---|---|
| `offside-event-integrity-audit 8` | 73/73 一致、**4.56** 次/队场（按设计只告警） | 4.56 ✓ |
| `box-possession-sampling-audit` | boxSeconds **1092.12**、spells **346**、留区 **58.3%**、无人盯防 **51.83**、pressLag **1.94** | 逐项 ✓ |
| `match-realism-audit 24` | 进球 **2.88**、射门 31.13、传球 1085.46、直塞 **0.63**、转化 **9.2%**、传球成功率 81.8%、强队积分 **2.21**，exit 0 | 逐项 ✓ |

## 🅰️ 角球真实参照值总表（2026-09-02 联网取得，改角球前先读这张表）

> 来源与越位那 1.70 同一条路：官方 Premier League / Pulselive API（`corner_taken`，JSON 内
> `source:"OPTA"`），以及 Opta / The Analyst 与 premierleague.com 的公开文章。
> 归一化沿用越位那次**已被独立验证过**的口径：季度累计 ÷ **760 队场**（380 场 × 2 队）。

### 一、角球数量（Opta 一手，四季）

| 赛季 | compSeason | 全季角球 | 每队每场 |
|---|---|---|---|
| 2022/23 | 489 | 3830 | 5.04 |
| 2023/24 | 578 | 4111 | **5.41** |
| 2024/25 | 719 | 3890 | 5.12 |
| 2025/26 | 777 | 3788 | 4.98 |
| **四季均值** | | | **5.14（带 4.98~5.41）** |

**每场双方合计 ≈ 10.28 次（带 9.97~10.82）。**
⚠️ 2023/24 又一次是高位异常季（与越位 1.91、禁区触球 28.0、射门 13.80 同一季），
**不要单用它标定**，取四季均值。

### 二、转化率（The Analyst，2022/23 全季，Opta 数据）

| 口径 | 传中式角球 | 短角球 | 内旋 | 外旋 |
|---|---|---|---|---|
| 后续这段进攻里出现射门 | **38.5%** | 32.6% | 34.9% | 44.4% |
| 后续这段进攻里进球 | **4.1%** | 3.3% | 4.2% | 4.1% |
| 第一下触球直接射门 | 20.8% | 2.8% | 16.9% | 25.1% |
| 第一下触球直接进球 | **1.3%** | — | 1.32% | 1.25% |
| 直接射门的 xG/脚 | — | — | 0.14 | 0.08 |

- **内旋与外旋一样有效**（进球 4.2% vs 4.1%），差别在形态而非优劣：外旋更容易出射门
  （44.4% vs 34.9%）但每脚 xG 低一半（0.08 vs 0.14）。
- 短角球占比按队差别极大：曼联最高 **34.2%**，埃弗顿只有 4.6%（传中占 95.4%）。
  **所以短角球不是可以忽略的边角情况。**
- 另两个可交叉校验的点：2023/24 全季「开出后第一段进攻进球」峰值 **4.2%**；
  2024/25 前七轮 758 角球 21 球 = 2.8%（**部分赛季样本**，作者本人也说别急着下结论）。

### 三、角球进球占全部进球的比例（premierleague.com，Adrian Clarke，2022-06-04）

- **十八季区间 9.8%~14.2%**（数据自 2004 年起），2021/22 为 13.6%。
- 长期均值 **每季 127 个角球进球**，最近五季全部高于它：
  2016/17 104、2017/18 139、2018/19 146、2019/20 134、2020/21 132、2021/22 146。
- The Analyst 2022/23：角球贡献**全部进球的 13.9%、全部射门的 15.1%**。
- **可断言的是 9.8%~14.2% 这条十八季区间**；13.9%/15.1% 是单季值，
  按本文件的惯例不单独用来标定。

### 四、落点分区（The Analyst 2022/23，只统计吊进小禁区的球）

| 区域 | 第一下触球的射门数 | xG/脚 | 进球 | 该区角球的进球率 |
|---|---|---|---|---|
| 前点 | 68 | 0.12 | 8 | 1.6% |
| 中路 | 72 | **0.27** | 9 | 1.5% |
| 后点 | 42 | 0.17 | 5 | **3.1%** |

- **后点每个角球的进球率是前点的两倍**（3.1% vs 1.6%），中路每脚 xG 最高（0.27）。
- 西汉姆把 **49.5% 的角球吊进小禁区**（联赛第一）；利物浦专打「第二个六码区」
  （5.5~11m），**54.0% 的第一下触球发生在那里**。
- **这一节是本次角球工作最关键的参照**：真实角球的争点全部落在
  **0~5.5m（小禁区）与 5.5~11m（第二个六码区）**，而 VCFM 现状最靠前的进攻者在 **10.5m**、
  最靠前的防守者在 **12.6m**，两个真实争点区**一个人都没有**。

### 五、与引擎现状的差距（引擎侧取今天那份 24 场 `match-realism-audit` 基线）

| 指标 | 引擎 | 真实 | 比值 |
|---|---|---|---|
| 角球 每队每场 | **3.02**（6.04/场双方合计） | **5.14** | **59%** |
| 每个角球产生射门 | 24.8%（1.50 ÷ 6.04） | 38.5% | 64% |
| 每个角球产生进球 | **2.15%**（0.13 ÷ 6.04） | **4.1%** | **52%** |
| 角球进球占全部进球 | **4.5%**（0.13 ÷ 2.88） | 带 **9.8~14.2%** | **约 1/3** |
| 角球射门占全部射门 | 4.8%（1.50 ÷ 31.13） | 15.1%（单季） | 约 1/3 |

**差距可以干净地分解成两半**：数量只有真实的 59%，每个角球的威胁只有真实的 52%，
0.59 × 0.52 ≈ **0.31**——正好对上「角球进球占比 4.5% vs 真实 13.9%」的 32%。
**两半都要修，只修一半到不了。**

⚠️ **口径限制，写进结论时必须一起写**：
- 引擎的 `cornerShots` / `cornerGoals` 是「角球事件后 **18 秒**内」
  （`scripts/match-realism-audit.mjs:199,213`），而 The Analyst 的是**按控球段**
  （"ensuing passage of play"）。方向同类、**只可比量级，不可逐位对账**。
- Opta 的 `corner_taken` 数的是「开出的角球」，引擎的 `corner` 事件数的是「判给的角球」，
  两者应当接近但**未逐一核对过**。
- 第二、四节都是 2022/23 单季。按本文件惯例，可硬断言的只有第一节的四季均值
  与第三节的十八季区间。
- 现有审计护栏 `match-realism-audit.mjs:385-386` 是角球 **2.75~10 次/场**、
  角球射门 ≥0.5。**引擎的 6.04 在护栏内，但只有真实的 59%**——
  护栏当年只为「防恶化」而设，不代表 6.04 是对的。

### 六、引擎角球摆位与现实的十处差异（`_restart("corner","home",2,4)` 实测换算成米）

坐标换算：x 一格 0.68m、y 一格 1.05m，被攻球门在 y=0，门柱在 x=50±5.38 格（±3.66m）。

| | 进攻方（10 名外场） | 防守方（10 名外场） |
|---|---|---|
| 禁区内 | 点球点 (0, 10.5m)、(±10.2, 13.7m)；另两人 **(±4.8, 17.9m) 已在大禁区外** | (±2.7, 12.6m)、(±8.2, 16.8m)、(0, 19.9m) |
| 外围 | (±7.5, 28.4m)、(−15.6, 31.5m)、(0, 32.6m) | (±8.8, 32.6m)、(±17.7, 35.7m)、(0, 36.8m) |
| 门将 | 在自家球门 | **(1.1, 5.3m)** |

1. **小禁区（0~5.5m）与第二个六码区（5.5~11m）两边都是空的**，而第四节的真实数据说
   争点全在这两块。最近的进攻者 10.5m、最近的防守者 12.6m。
2. **两根门柱都没人。** 现实防守角球必设近柱人，站在球门线上。
3. **进攻「禁区五人」里有两人在禁区外**（17.9m > 16.5m），实际禁区里只有 3 人。
4. **防线比它该防的人更靠外**：防守五人距门线均值 15.7m，进攻五人 14.7m；
   点球点那个抢点手**比全部五名防守者都更靠球门**。盯人方向是反的。
5. **完全没有盯人**：防守坐标与进攻方位置无关，既不是盯人也不是真区域防守
   （真区域防守是沿小禁区线的紧凑一排，不是 12~20m 的五人弧）。也没按头球/力量/身高分配，
   而 AGENTS.md v233 段写明争顶已经读这三项。
6. **没人干扰主罚者**：最近防守者离角旗约 36m。Law 17 只要求 9.15m，
   现实里那条线上永远站着人。
7. **进攻方无人留守、防守方无人留前场**：除主罚者外 9 人全在对方球门 33m 内。
8. **门将离门线 5.3m 且钉在正中**（`engine.js:6240` `defGkY = 5`）。
   现实约离线 1~2m、偏向球门中后段。
9. **没有人跑动**：`engine.js:6294` 设 `cornerShapeUntil = t + 2.15`，
   `:1712-1717` 把除主罚者外所有人的目标点设成 `tx=x, ty=y` 冻住，
   球被吊进一张**静止画面**里，开出之后才恢复跑位。
   而真实角球的本体就是**有时机的抢点跑动**。
10. **开球没有形态选择**：强制 `_bestCross`，兜底吊到 x 38~62 / y 8±3。
    没有内旋/外旋（真实里由主罚脚与哪个角决定，第二节实测两者形态差异很大）、
    没有前点急球 / 后点高球、没有短角球（真实里按队占 4.6%~34.2%）。

**第 9 条与主线是同一个缺口**：引擎没有「跑动」原语（有时长、有触发、有到达时刻）。
角球反而是最适合先建这个原语的地方——触发时刻明确（出脚）、目标区域固定，
比运动战里的插上好定义得多。

### 七、新增 `scripts/corner-structure-audit.mjs`（把「画面对不对」变成可回归断言）

6 场、种子 372000..372005、标准档、能力 15（属性表含 `crossing`），口径与
`box-possession-sampling-audit` 一致。**只读引擎公开状态，不消费随机数。**
已挂进 `scripts/verify.mjs` 默认 `checks`（约 50s，6 场约 34 次角球，不存在零样本空转）。

断言分两级，沿用 `offside-event-integrity-audit` 立下的规矩：
- **硬失败**（现在就成立，防回归）：最小两人间距 ≥1.6m（**按米换算，不是坐标**）、
  主罚者距球 ≤3m、Law 17 的 9.15m、每次角球的落点都被记录到。
- **只告警**（12 项还原度缺口）：成因未修，硬失败会立刻挡住所有无关改动。

实测基线（34 次真实比赛里的角球，每次角球的平均人数）：

| 深度带（距被攻球门线） | 进攻方（不含主罚者） | 防守方 |
|---|---|---|
| 小禁区 0~5.5m | **0** | **0** |
| 第二个六码区 5.5~11m | 1 | **0** |
| 禁区其余 11~16.5m | 2 | 2 |
| 16.5~25m | 2 | 3 |
| 25~52.5m | 3.88 | 4.85 |
| 本方半场 | **0** | **0** |

- 角球 **5.67 次/场**（24 场 `match-realism-audit` 是 6.04，同量级，仪器可信）
- 禁区内均深：进攻 **12.6m** / 防守 **12.6m**——**完全没有深度分层**
- 门柱上：进攻 0 人 / 防守 **0 人**
- 比全部防守者都更靠球门的进攻者：**1 人**（应为 0，那是点球点那个抢点手）
- 最近防守者距球中位 **27.53m**（Law 17 只要求 9.15m，真实里那条线上站着人）
- 门将离门线 **5.25m**、横向偏移 0.57m
- **落点中位 15.1m**；吊进小禁区 **0%**、吊进第二个六码区 **0%**、吊到禁区外 **17.6%**
- 开球窗口 2.5s 内位移中位 **1.69m**、位移 ≥5m 的进攻者 **0 人**

**一句话概括这份基线**：进攻方最靠前的人在 10.5m、防守方最靠前的人在 12.6m、球吊到 15.1m
——**三者互相自洽，但整体比真实争点区（0~11m）远了 5~10 米，而且没有人跑动。**
所以第 3 步必须三样一起动：**只把球吊近会吊到无人区，只把人放前面会等不到球。**

#### ⚠ 一处检测陷阱（第一版踩了，写下来免得重踩）

**不要用 `cornerShapeUntil` 检测角球。** `engine.js:6577` 让人墙任意球复用同一个字段
（`this.t + 2.6`；角球在 `:6294` 是 `+2.15`），按它触发会把任意球当角球采进来。
第一版这么写，量出「站位重叠 1.02m」与「Law 17 只剩 1.57m」两个**假缺陷**——
与 `_restart` 逐次对账才发现触发点落在 `type: "freekick"`、球在 `23.2,22.4` 这种非角球位置。
改用 `ball.state` 切进 `"corner"` 检测后，最小间距 3.44m、最近防守者 27.53m，两条都正常。
（→ 又一次印证「两个探针报同一个量时必须对账，数字对不上先怀疑分类条件」。）

#### 顺手修掉一处混单位断言

`set-piece-presentation-audit.mjs` 的角球段一直在**直接对坐标取 `Math.hypot` 再和 3.3 比**
——正是该文件为点球段修掉的那类写法（同一个 3.3 可以是 2.24m 纯横向，也可以是 3.47m 纯纵向）。
已改为按米换算、阈值统一到 **1.6m**（与点球段、`support-target-crowding` 一致）。
实测该合成场景真实最小间距 3.44m，**所以这条修改不改变通过与否**，只是让它有物理含义。

### 八、第 3 步的设计（**已按这条跑完标定曲线，结论见第九节**）：改摆位 + 建「跑动」原语

三样必须一起动（理由见第七节那句概括）：

1. **摆位**（`engine.js:6300-6306` 那六个坐标数组）：把争点推进到真实位置——
   进攻方前点/中路/后点各一人落在 **0~5.5m**、第二个六码区留 1~2 人（真实第一下触球集中区）；
   防守方补**近柱人 + 小禁区两人**，并让盯人者站在被盯者的**球门侧**（现状 12.6m 对 12.6m，
   完全没有分层）。
2. **留守/留前场**：进攻方留 1~2 名中卫在本方半场防反、防守方留 1 名快马在前场做出球点。
   现状两边都是 0，全 20 名外场球员挤在同半场。
3. **跑动原语**：这是第 9 条差异的本体，也是主线上撞到的同一个缺口——
   `_thinkAttackOffBall` 是逐 tick 的位置控制器，**没有「有时长、有触发、有到达时刻」的跑动**。
   角球是最适合先建它的地方：触发时刻明确（主罚出脚）、目标区域固定（前点/中路/后点）、
   时长有界（飞行时间）。现状是 `cornerShapeUntil` 把所有人冻住 2.15s、球在冻结期内被吊出，
   开球窗口 2.5s 内位移中位只有 1.69m。

**同时要修数量那一半**（第五节）：角球 3.02 次/队场只有真实 5.14 的 59%。
两半相乘才是「角球进球占比 4.5% vs 真实 9.8~14.2%」的成因，**只修一半到不了**。
角球来源在 `engine.js:6178/6196`（门将托救托过底线 + 后卫封堵挡过底线），
提高数量要从那两条来源走，不要直接给角球加权。

⚠️ **护栏（改之前先记下现状值）**：
- `match-realism-audit.mjs:385-386`：角球 **2.75~10 次/场**、角球射门 ≥0.5；现状 6.04 / 1.50。
- 进球 **2.5~3.3**（现状 2.88）、射门 11.9~13.8 每队场（现状 15.57，本来就偏高）。
- `box-defending-audit` 的 `crowdedPairs` 上限 14、干净基线 12，**余量只有 2**。
  把人推进小禁区几乎一定顶到它，**必须单独跑**。
- `set-piece-presentation-audit` 的角球间距 ≥1.6m：往小禁区塞人会直接压这条。
- `corner-structure-audit` 的 12 项告警就是验收清单，改完应逐条清空。

⚠️ **顺序**：按本文件一贯的纪律，先做**进程内包装的标定曲线探针**
（照 `_final-third-movement-calibration-probe.mjs` 的接缝写法：原方法先跑完再覆写坐标，
覆写公式全用确定性函数，不额外消费随机数），一次测完「只改摆位 / 只加跑动 / 两者合并」
几档，再决定动不动引擎。

### 九、⚖️ 第 3 步标定曲线：`slots + delivery` 是可采用的候选，两半单独做都比不做更差（`scripts/_corner-rework-calibration-probe.mjs`）

> **⛔ 有效期声明（2026-09-04 复测）：下面这张表的数字是 `4ddac36` 时代引擎测的，已过期。**
> 探针逐字节未变、自检过，但引擎自标定后改了 7 次（含 `fdbb455` 门将出球、`a254017` 直塞 0.20），
> 在今天的 `d0eef32`/`ab8df5a` 上复现不出来。**下结论请以下面这份「今天的新基线」为准，旧表仅存档留证。**
>
> **📊 今天的新基线（`d0eef32` 引擎，2026-09-04 复测，同 12 场种子 372000..372011）：**
>
> | 档位 | 角球/场 | 每角球进球 | 每角球射门 | 角球进球占比 | 进球/场 | 射门/队场 | 传中占比 |
> |---|---|---|---|---|---|---|---|
> | control 现状 | 4.25 | 2% | 23.5% | 2.9% | 2.83 | 13.46 | 4.72% |
> | `slots` 只改摆位 | 5 | 0% | 36.7% | 0% | 3 | 14.38 | 5% |
> | `delivery` 只改落点 | 4.75 | 0% | 5.3% | 0% | 3.17 | 12.92 | 4.6% |
> | **`slots` + `delivery`** | 4.92 | 3.4% | **23.7%** | **5.4%** | 3.08 | 13.75 | 4.71% |
> | 真实 / 护栏 | 5.14 每队场 | 4.1% | **38.5%** | **9.8~14.2%** | 2.5~3.3 | 11.9~13.8 | 3~14% |
>
> **最强信号没了**：旧表 ★39.3% 对齐真实 38.5% 是本候选的立足点，今天只剩 **23.7%**（远未对上）；
> 进球占比从留档 10.3% 掉到 **5.4%**（跌出真实带 9.8~14.2%）；每角球进球从 7.1% 掉到 3.4%。
> **仍成立的结构性结论（与引擎版本无关）**：两半单独做都更差——`delivery` 单独把球吊进无人区
> （出脚时距目标 10.7m），`slots` 单独角球零进球却在别处漏球。`gkOnLine` 归因仍是机制性缺陷
> （`−门将上线` 把每角球进球拉到 10%，但门将离线 5.25m 违反真实 1.5m）。
> **纪律不变**：采用前仍须过 `match-realism-audit 24` / `box-defending-audit` / `corner-structure-audit`
> 三关；如今连射门率这条最强信号都没了，**在没有新对齐前不要动角球引擎**。
> 完整 12 场输出留在本机 `/tmp/corner-cal-12-rerun.txt`（不进库）。
>
> **🔬 复测同日追查（2026-09-04）：射门率塌陷的根因已定位，且「光调落点」被证伪为跷跷板。**
> 给探针加了两项诊断（`gk_claim`/`gk_block`/`gk_clear`/`save` 事件计数 + 落点深度扫描 `depthScale`），
> 结论：
> 1. **射门率从 39.3% 掉到 23.7% 的直接原因是「门将解围」翻 3 倍**（0.5→1.58 次/场）。
>    `delivery` 把球精确吊进小禁区 5m 深，而 `gkOnLine` 把门将放在离线 1.5m ≈ 同一深度，
>    **球被吊进门将出击圈正中央**，门将一步出来解围，射门根本发不出。
> 2. **落点深度是跷跷板，没有单调甜点**（落点+抢点点一起外推 5/7/9/11m）：
>    | 落点 | 门将解围 | 射门率 | 每角球进球 | 进球/场 |
>    |---|---|---|---|---|
>    | 5m（现状） | 1.58 | 23.7% | 3.4% | 3.08 ✅ |
>    | 7m | 0.33 | 19.6% | 0% | 2.58 |
>    | 9m | 0.50 | 31.4% | 0% | 2.67 |
>    | 11m | 0.33 | **42.9%** | **0%** | **2.0 ❌破下限** |
>    吊近＝能进球但被门将解围、射门率低；吊远＝门将够不着、射门率高，但落到小禁区外＝全是低质量
>    远射、**零进球、进球总数破下限**。
> 3. **硬结论：单靠落点/摆位调不出真实角球**（38.5% 射门 + 4.1% 进球）。真实角球的高射门+有效进球
>    靠的是**抢点者比门将先一步到达小禁区落点**＝「跑位时间差」（出脚不越位、球到已越过），
>    这正是**第 3 件事「最后三区跑位塌陷」的同一个解**（交接早已点明两条绑定）。现有 `+runs` 档
>    没做出真时间差（峰值速度 2.81 反而比不跑还慢），**下一步转去查引擎无球跑动机制**，
>    不再在落点/摆位上打转。诊断输出留在本机 `/tmp/corner-diag-12.txt`、`/tmp/corner-depth-12.txt`（不进库）。
>
> **🎯 引擎根因已挖到底（2026-09-04，下一个人从这里接手）：抢点者冲不起来 = `_integrate` 的到达减速。**
> `js/sim/engine.js:4713`：`const slowR = 5; desired = speed * Math.min(1, d/slowR) * …`。
> 球员离目标点 `slowR=5` 单位（纵向 ≈5.25m / 横向 ≈3.4m）以内时，期望速度按 `d/5` 线性衰减到 0，
> 好让球员平滑停靠目标点。**但角球抢点的目标点就是落点区**，抢点者一进落点 5 单位就被强制刹车，
> 所以峰值速度只有本可达到的一半（测试阵容 pace=0.75 本可 5.6 m/s，实测峰值 2.81）。
> 真实抢点是**全速冲进落点起跳/触球**，不是滑行停靠。这就是「跑位时间差」做不出来的机械原因。
> ⚠️ **不要直接改 `slowR` 常量**：`_integrate` 在 `:1634/:1638` 对全场所有 fsm 统一调用
> （receive/turn/support/cover/press/carry/home/recover/save/smother…，没有专门的「冲刺抢点」态），
> 改它会波及整场每一次到达行为，面极大。正确修法是**给「有明确冲刺意图的跑动」一个豁免标志**
> （新增字段或 fsm 态，只在那一小段窗口内绕过 `slowR`），这是一次真正的引擎改动，
> 必须过三关审计 + `verify --full`。**这是角球（第 4 件）与跑位塌陷（第 3 件）共同的下一步，别再在探针里打转。**

12 场、种子 372000..372011。**纯测量，不改仓库代码**，包装三处：`_restart`（原方法先跑完，
含门将那次 `random()`，之后才覆写坐标，再做一次确定性去重叠）、`_think`（原方法先跑完，
之后才覆写 `tx/ty`——`:1712` 的冻结分支就在里面）、`_bestCross`（先调原方法再替换返回值）。
覆写公式全是位置 / id / 角球序号的确定性函数，**一个随机数都不取**。
自检：control 与未打包装引擎 **12 场逐场同分**。

**开工前先量出的一条新事实，它决定了 `delivery` 必须单独成档**：
角球时 `_bestCross` **20 次调用全部返回 null（0%）**，所以每个角球都走 `_decideOnBall` 那条
兜底吊传（`tx = 50±6, ty = boxY±3`）。根因是 `_bestCross` 要求候选 `ahead >= 4`
（比出脚人更靠近球门），而主罚者站在角旗、几乎在球门线上，**没有任何队友能比他更靠前**。
→ **角球落点现在是一个常量，不是一次决策；主罚者的 `crossing` 完全不影响球吊到哪。**

| 档位 | 角球/场 | 每角球进球 | 每角球射门 | 角球进球占比 | 进球/场 | 射门/队场 | 传中占比 |
|---|---|---|---|---|---|---|---|
| control 现状 | 4.67 | 1.8% | 23.2% | 2.9% | 2.92 | 13.75 | 4.21% |
| `slots` 只改摆位 | 3.75 | **0%** | 35.6% | **0%** | **3.58 ✗** | 14.83 | 4.01% |
| `delivery` 只改落点 | 4.33 | **0%** | **7.7%** | **0%** | **2.17 ✗** | 13.79 | 4.05% |
| **`slots` + `delivery`** | 4.67 | 7.1% | **★39.3%（⛔ 已过期→今 23.7%）** | **★10.3%（⛔→今 5.4%）** | **3.25** | 14.67 ⚠ | 4.39% |
| 三项合并（+`runs`） | 4.92 | **15.3%** | 67.8% | **18%** | **4.17 ✗** | 15.25 ⚠ | 4.35% |
| **真实 / 护栏** | 5.14 每队场 | **4.1%** | **38.5%** | 带 **9.8~14.2%** | 2.5~3.3 | 11.9~13.8 | 3~14% |

#### ✅ `slots + delivery`：两项真实参照值几乎直接对上

- **每角球射门 39.3% vs 真实 38.5%**——这一项是本次最强的信号（样本 56 次角球、22 脚射门，
  比进球那一项可靠得多）。
- **角球进球占比 10.3%，落进十八季区间 9.8~14.2%**（现状 2.9%）。
- 进球 **3.25 仍在 2.5~3.3 内**，但**只剩 0.05 的余量，不是真的余量**。
- 每角球进球 7.1% 是真实 4.1% 的 1.7 倍。⚠️ 12 场只有约 56 次角球、4 个角球进球，
  **一个球就能挪动 1.8pp**，这一项**不足以下结论**，要看的是射门率那一项。

#### ❌ 两半单独做都比不做更差，而且是两种不同的坏

- **`delivery` 只改落点 = 把角球废掉**：射门率 23.2% → **7.7%**、进球跌到 **2.17（破 2.5 下限）**。
  原因表里就有：出脚那一刻抢点者距落点 **11.45m**——**球被准确地吊进了一片无人区。**
- **`slots` 只改摆位 = 角球一个球都不进，却在别处漏球**：每角球进球 0%、角球进球占比 0%，
  可进球总数反而升到 **3.58（破 3.3 上限）**。那 +0.66 球**不是角球进的**，
  ~~**成因未归因**——最可能是「进攻方留守 2 人 + 防守方留前场 1 人」改变了角球后的攻防转换。~~
  **成因仍未归因，但「留守/留前场」已被证伪——见「❌ 负结果留档十」。**
  采用前必须单独查清楚。
- 所以第七节那句「三样必须一起动」被定量证实了：**只把球吊近会吊到无人区，
  只把人放前面会等不到球。**

#### 🔑 `runs`：跑动原语机械上是通的，但威胁被顶到真实的 3.7 倍

- **原语是通的（有触发 / 有时长 / 有到达点）**：抢点者摆位时先退 3m，出脚那一刻
  距目标只剩 **1.16m**、峰值速度 **2.79 m/s**——**他确实赶到了落点。**
  这是本项目第一次做出「有时机的跑动」。
- **但威胁被顶过头**：每角球进球 **15.3%**（真实 4.1%、3.7 倍）、射门率 **67.8%**（真实 38.5%）、
  角球进球占比 **18%**（带 9.8~14.2%）、进球 **4.17（破顶）**。
  盯人者虽然跟着被盯者的目标点跑（球门侧 1.8m），**但显然没能形成有效争抢**。
- ⚠️ **`RUN_BACK = 3.0m` 不是随手取的**：主罚暂停只有 1.6s（`engine.js:6558`），
  而引擎无球球员实测只跑得动约 1.2~2.8 m/s，退 5m 时 2.5s 位移中位只有 3.06m、无人跑满 5m
  ——退太远就赶不到，量出来会是「跑了但没到」。**这条速度上限与主线那个「没有跑动原语」
  是同一个约束的两面。**
- ⚠️ **`2.5s 位移中位` 这个指标是废的，别再用**：它把「冲向落点」和「冻结解除后四散跑开」
  混在一起，各档都是 1.3~1.8m 分不开。**能分开的是「出脚时距目标」与「峰值速度」这两项。**

#### ❌ 负结果留档十：那 +0.66 不是「留守 / 留前场」造成的（`_corner-rework-calibration-probe.mjs` 归因三档）

**结论：交接里写的嫌疑证伪。+0.66 仍未归因，但可以从名单上划掉这两处。**

做法：给探针的 `V` 加 `restDef`（进攻方 2 名后卫拉回 60m）与 `outlet`（防守方 1 名前锋停在 55m）
两个开关，未显式指定时跟随 `slots`——所以**既有五档的数字一位没动**（12 场逐位复现留档：
control 2.92、`slots` 3.58、`slots + delivery` 的 39.3% / 10.3% / 3.25，全部对上）。
关掉的语义是「不给目标点」，`_restart` 里 `if (!spot) continue;` 会跳过，人留在引擎自己摆的位置。
**`backIds` 与 `rest` 的排除无论开关都成立**，所以四档的禁区结构逐位相同（小禁区攻 3、
争点区攻 5、小禁区守 5、最近守距球 29.35m、最小间距 2m），开关只动那两个安排。

12 场（种子 372000..372011）的非角球进球，control 基线 2.83：

| 档位 | 非角球进球 | Δ | 留守 | 留前场 |
|---|---|---|---|---|
| `slots` 只改摆位 | 3.58 | +0.75 | 2 | 1 |
| `slots −留守−留前场` | 3.67 | +0.84 | 0 | 0 |
| `slots −留守` | 3.58 | **+0.75** | 0 | 1 |
| `slots −留前场` | 3.75 | +0.92 | 2 | 0 |

**没有一档把 Δ 拉回 0 附近。** 摘掉留守，Δ 一位不变；两个都摘掉，Δ 反而略升。
方向与嫌疑相反：那两个安排本来就是 rest defence，在**压低**多漏的球，不是造成它。

⚠ **四档之间不要读排序**：0.75~0.92 的跨度是 0.17 进球/场，12 场合计约 2 个球，**是噪声**。
唯一稳的结论是「都不回 0」。

⚠ **本次引入的一处混淆项**：`_restart` 的包装只对被摆位的人做 `a.vx = a.vy = 0; a.tx = a.x; a.ty = a.y`，
所以关掉开关的那 3 人不只位置不同，速度与目标也没被清零。这是「没动他」的应有含义，
但对比因此不是纯位置的。不影响结论方向（摘掉后 Δ 变大而非消失）。

**🔜 新线索（数字自己指出来的）**：`slots` 还顺带改了三件比留守可疑得多的事——
门将离门线 **5.25 → 1.58m**、摆位最小间距 3.44 → 2m、小禁区塞进 3 名进攻者与 5 名防守者。
而且有一对指标配着动：**角球数 4.67 → 3.75（少近 1 个），射门 13.75 → 14.83（多 1.08）**，
而角球进球是 0%——原先出底线变角球的球现在留在场内变成了射门。
角球频率的来源是 `engine.js:6178/6196` 的托救与封堵，门将提到门线上、小禁区多 5 人，
封堵与扑救的结算必然变。这条通道同时解释「角球变少」与「非角球进球变多」。
**下一档只动 `gkSpot`（1.5m 改回引擎原值），其余全保持**——那是 `slots` 里唯一碰了守门员的部分。

#### ⚖️ 归因第二轮：门将占一半，但退回去连护栏也救不回来（`gkOnLine` 开关）

12 场，`V` 加 `gkOnLine`（关掉时 `gkSpot` 返回 null，`_restart` 不动门将）：

| 档位 | 非角球进球 | Δ | 门将离线 | 角球 | 最小间距 |
|---|---|---|---|---|---|
| `slots` | 3.58 | +0.75 | 1.58m | 3.75 | 2m |
| `slots −门将上线` | 3.25 | **+0.42** | 5.25m | **6.08** | **0.11m** ⚠ |

Δ 少了约一半，角球数从 3.75 摆到 6.08（control 4.67）——方向对上「门将位置改变封堵与扑救结算」。

**⛔ 但这一档不能直接采信，也不能采用**：
1. **被污染**：`最小间距 0.11m` 撞穿 ≥1.6m 护栏。成因是开关实现——`gkSpot` 为 null 时门将
   不被摆位，**也就不进 `relax(staged)`**，与站在 0.8/1.5m 深度的防守者重叠无人解。
   要干净必须先把门将加进 `relax()`。这是探针缺陷，不是引擎缺陷。
2. **退回门将 = 候选档整体崩掉**，而且这一条**不受污染影响**（量级太大不是重叠噪声）：

| | 每角球进球 | 角球进球占比 | 进球 |
|---|---|---|---|
| `slots + delivery`（候选） | 7.1% | **10.3%** ✅ 进真实带 | 3.25 |
| `slots + delivery −门将上线` | **1.8%** | **2.5%** ❌ 回到现状水平 | 3.33 ⚠ 仍破顶 |

**门将在门线上，是让角球变真实的必要条件，同时也是漏球的部分原因，两者不可分割；
而退回去连 3.3 的护栏都没救回来——付出了真实度，什么都没换到。**
以后谁再想动角球门将站位，先读这一条，不用重跑。

真正该查的不是「门将站哪」，而是**角球之后引擎怎么恢复门将站位**：量角球结束后数秒内
他离门线多远。若他赖在 1.5m 不回到开放比赛深度，那是引擎缺陷，修它才能既保住角球
真实度又不漏球。

#### 🔜 下一步（按这个顺序）

1. ~~**先查清 `slots` 那 +0.66 个非角球进球**（3.58 vs control 2.92，角球进球占比 0%）。
   嫌疑是「留守 2 / 留前场 1」改变了角球后的转换。可用同一探针加一档
   「只改禁区内摆位、不动留守/留前场」来隔离。~~
   **→ 已隔离完，嫌疑证伪（留档十）。改为：加一档只把 `gkSpot` 改回引擎原值，
   验「门将被提到门线上」是不是那 +0.66 的来源。**不查清不要采用。**
2. **给 `runs` 加阻尼并跑标定曲线**：现在是 15.3% / 67.8%，要压到 4.1% / 38.5% 附近。
   可调的量有三个——`RUN_BACK`（现 3.0m）、盯人者的提前量（现在跟的是被盯者的**目标点**，
   可以改成跟当前位置、或给一个反应延迟）、以及门将出击。
   ⚠️ 按留档二的教训，**不要一次动两个**，先各自跑出曲线再配对取值。
3. **`slots + delivery` 采用前必须过的三关**（本表都没测）：
   - `node scripts/match-realism-audit.mjs 24`：进球现在只剩 0.05 余量，
     且**射门 14.67/队场比 control 的 13.75 更远离真实带 11.9~13.8**，这是这档的真实代价。
   - `node scripts/box-defending-audit.mjs`：`crowdedPairs` 上限 14、干净基线 12，
     **余量只有 2，而这档往小禁区放了 5 名防守者**，几乎一定顶到。
   - `node scripts/corner-structure-audit.mjs`：12 项告警应逐条清空
     （本表已确认小禁区攻 3 / 守 5、留守 2、留前场 1、门将离线 1.58m、越过全部防守者 0）。
4. **数量那一半仍未动**：角球 3.02 次/队场 = 真实 5.14 的 59%。
   来源在 `engine.js:6178/6196`（门将托救托过底线 + 后卫封堵挡过底线），
   要提高得从那两条来源走，**不要直接给角球加权**。
   ⚠️ 注意本表 control 的角球是 4.67/场（12 场），`corner-structure-audit` 6 场是 5.67，
   24 场 `match-realism-audit` 是 6.04——**样本波动不小，判断数量是否改善必须用 24 场。**
5. **`_bestCross` 的 `ahead >= 4` 那道门**（`engine.js:2654` 附近）是角球落点变成常量的根因。
   真要落地 `delivery`，正解是让角球走一条**自己的落点选择**（前点/中路/后点按主罚脚与
   队内争顶能力选），而不是在 `_bestCross` 里给角球开特例——后者会把运动战传中一起带偏。

## v241 越位事件重复与判罚率（2026-09-01）

> 缓存 `vcfm-v241`，提交 `a43c189`（缓存升级在紧随其后的提交里）
>
> 本段是 v240 交接「下一步建议 #1」的直接产物：为定位「快速通路」而写的两支探针
> 意外撞出一个**用户可见的表现层 bug** 与一条**从未被审计覆盖的失真指标**。

### ✅ 已修：每次越位发出两条事件，第二条记在获得任意球的那一方（`engine.js:6578`）

- **现象**：同一时刻同一坐标出现两条 `type: "offside"`，第二条的 `team` / `agentId`
  是**对方**（获得任意球的防守方）的一名后卫。实测每场 26 条事件对应仅 13 次
  `_callOffside` 调用，逐场精确 2 倍（26/13、16/8、14/7）。
- **根因（一处命名冲突，不是判罚逻辑错）**：`_restart` 结尾用 `this._emit(type, taker, …)`
  发重启通知，而 `type` 就是重启原因。`_callOffside` 走的是 `_restart("offside", …)`，
  于是**重启通知本身被打上 `"offside"` 类型**，与它所重启的那次判罚无法区分；
  `taker` 是获得任意球一方的球员，所以第二条记在了错误的队。
  **越位是唯一「判罚原因」与「重启名称」同名的类型**——`corner`/`goalkick`/
  `freekick`/`throwin` 都只作重启名，所以只有越位会重复。
- **两条事件本可区分**：真实判罚带 `kickLineY`/`kickBallY`（`_callOffside` 写的），
  重启通知带 `setPiece: "offside"`。全仓库没有任何消费者读事件的 `setPiece`
  （`adapt.js:340` 与 `matchview.js:450` 读的是**帧**上的 `setPiece`，同名不同物），
  所以给重启通知改名是安全的。
- **用户可见后果**：`adapt.js:859` 的解说文案 `🚫 {short} 越位` 与 `main.js:9239`
  的直播 ticker 都按 `ev.type === "offside"` 触发，因此**每次越位念两遍，第二遍念的是
  对手越位**。之所以一直没被发现，是 `adapt.js:641` 的 `pickFlavorEvents` 给
  `offside` 设了 `cap: 3`，静默截断掉了大部分重复。
- **修法**：`_emit(type === "offside" ? "offside_restart" : type, …)`。不消费随机数，
  三个种子比分逐字未变（1-0 / 3-2 / 1-0），事件数与调用数修复后精确相等。

- **`match-realism-audit` 从不统计越位**（该脚本覆盖射门、进球、扑救、传球、抢断、
  角球、犯规、手球、VAR、伤病、停滞，见 `scripts/match-realism-audit.mjs:180-240`），
  所以这条失真可以长期漂移而不触发任何断言。**已补 `scripts/offside-event-integrity-audit.mjs`**，
  并挂进 `scripts/verify.mjs` 默认 `checks`（8 场约 60s；2 场即产生 21 次判罚，
  断言不会因零样本空转）。两条断言：
  - 「事件数 == `_callOffside` 调用数」**硬失败**，防上面那个重复 bug 复发；
  - 判罚率超出 1.4~2.1 **只告警不失败**——目标值已定（1.7），但成因未修，
    让它硬失败会立刻挡住所有无关改动。
- 已反向验证：把修复改回原样，审计以 exit 1 报出 13/26、8/16；恢复后 73/73 通过。
  **注意 `sameTickSamePlayerDuplicates` 在 bug 存在时仍是 0**——重复事件记在**另一名**
  球员上，所以真正抓住它的是「数量相等」那条，不是「同时刻同球员去重」。审计另加了
  「每条判罚必须带 `kickLineY`/`kickBallY`」，防止判罚将来被改名后两边一起归零、
  断言空转通过（[[vcfm-audit-dead-assertions]] 那类陷阱在事件层的变体）。

### ⚠️ 未修：越位判罚率约为真实的 2.7 倍（目标值已由 Opta 实测确定）

- 去掉重复后实测 **4.56 次/队/场**（8 场；2 场样本 5.25，同量级）。注意修复前从事件流
  读到的「约 13」是**被重复放大过的**数字，不要拿它当基线。
- **✅ 真实参照值已联网取得（2026-09-02）**，来源为官方 Premier League / Pulselive API
  （JSON 内 `"source":"OPTA"`，`total_offside` 季度累计 ÷ 760 队场）：

  | 口径 | 值 |
  |---|---|
  | 英超 VAR 时代（2019/20~2025/26）均值 | **1.70 次/队/场**（1.55~1.91） |
  | 英超 VAR 前（2014/15~2018/19）均值 | **1.99**（1.91~2.06） |
  | 五大联赛 2024/25~2025/26 区间 | 1.53~1.91（西甲最高、英超与意甲最低） |

  VAR 让越位旗减少约 15%；2025/26 的 1.55 是十二季最低（半自动越位 SAOT 首个完整赛季）。
- **⚠️ 我此前记的「英超约 1.4」是错的**，近十二季没有任何一季低到 1.4。审计已改为
  容许带 **1.4~2.1、目标 1.7**（1.4 是下沿不是均值）。**不要再引用 1.4 当真实值。**
- **归一化已被独立验证**，不是假设：同一 API 的 `total_pass` 按 ÷760 再乘 2，与 Opta
  自己公布的「双方每场传球数」逐位吻合（940.8 vs 941.0、893.4 vs 893.4、945.2 vs 945.2）。
  **凡是引擎数字与真实值差出「恰好 2 倍」时，先怀疑口径（单队 vs 双方合计）而不是模型。**
- 已排除的成因：**不是**「进攻球员不贴越位线」。见下方探针二——前锋在最后三区
  距越位线中位仅 −2.7m、80.3% 在 5 单位内，贴线行为是**正常**的。
- 归因数据（`.tmp` 探针，未入库）：判罚的触发路径为普通传球 69.8% / 传中 30.2% /
  直塞 0%。而按「出脚瞬间预定接球人已越位」的比例看，**传中 10.5%、普通传球仅 0.3%**
  ——即传中路径单次风险高 35 倍。`_bestCross`（`engine.js:2654-2693`）**完全没有越位项**，
  只要求 `ahead >= 4` 且离门 <32m，正好是越位区；而 `_passCandidates:2849` 对越位队友
  有 ×0.16~0.5 的抑制（且注释说明是刻意保留少量误传）。据此曾判断「传中缺这道抑制是
  最可疑的着力点」，并要求先跑标定曲线——**曲线已跑，那个判断被推翻，见下。**
- **➡️ 当前结论（2026-09-02，已标定）：修法找到了，但被进攻过量卡住，不能单独上。**
  路径分解显示判罚有 **49.3% 来自「越位球员不是传球目标、却抢先拿到了飞行中的球」**，
  单这一路就 2.25 次/队/场、已超真实总量。堵住它（`peelB`）+ 禁止传给越位队友（`hardA`）
  实测把 4.54 打到 **2.04，首次进入 1.4~2.1 带**——但**进球同时升到 3.67/场，
  超出 `match-realism-audit:362` 的 2.5~3.3 护栏**。
  说明**过量越位一直在充当进攻的刹车**，必须与「压低禁区循环」成对做。
  暴露量（3.3% 的传球有越位队友）与判定容差（越线中位 1.64m）**都已被证伪，
  不要去动 `_clampOffside` 或 `tol`**。详见下方两节。

### ❌ 负结果留档八：给 `_bestCross` 补越位抑制无效（`scripts/_cross-offside-calibration-probe.mjs`）

标定曲线（**12 场**，种子 372000..；进程内替换 `_bestCross`，不改仓库代码。自检：
系数 1.00 档与未打补丁的引擎逐场同分 `1-0 3-2 1-0 2-1 1-1 1-2 0-2 2-1 0-3 1-3 0-5 2-1`，
故复刻忠实）。**已按上一轮要求复跑到 12 场，括号内是先前的 4 场值**：

| 档位 | 越位/队场 | 传中占比 | 射门/队场 | 进球/场 | 禁区触球/队场 | 直塞/场 |
|---|---|---|---|---|---|---|
| 1.00 对照 | **4.54**（4.63） | **4.21%**（3.51%） | 13.75（14.25） | 2.92（2.5） | 276.3（288.1） | 0.75（0.25） |
| 0.60 / 0.35 / 同式 / 0.12 | **逐字与对照相同**（4 场已证，12 场未复跑） | | | | | |
| 0.00 乘零（非硬禁） | **3.79**（3.38） | 4.52% | 14.0 | 3.0 | 275.3 | 0.75 |
| preferOnside 保留绝境误传 | **4.17**（新测） | 4.46% | 14.08 | 2.92 | 269.5 | 0.75 |
| skip 真硬禁 | **3.33**（2.88） | **4.23%**（3.31%） | 13.83（13.0） | 2.67 | 276.5（256.4） | 1.17（1.5） |
| 真实目标 | **1.70** | 3~14% 护栏 | 11.9~13.8 | — | 26.1 | 3.38 |

- **曲线从 1.00 到 0.12 完全是平的**——不是「强度不够」，是乘性抑制在这里**结构性失效**。
  诊断（12 场共 **14635** 次 `_bestCross` 调用；4 场值 4732 次，两次口径一致）：

  | 情形 | 次数 | 占调用 |
  |---|---|---|
  | 有候选 | 1888 | 12.9% |
  | 含越位候选 | 131 | 0.9% |
  | **越位与非越位并存（乘性抑制唯一的作用面）** | **38** | **0.3%** |
  | 仅有越位候选（乘任何正系数都翻不动） | 93 | 0.6% |
  | 最终选中越位者 | 104 | 0.7% |

  并存那 38 次里，「越位最优/非越位最优」价值比**中位 0.74**（p90 2.05）——多数情况下
  越位候选**本来就输**，根本不需要抑制。**作用面 0.3% 且多半已自行落选 = 这条杠杆是死的。**
- **✅ `preferOnside` 已测，预测被证实**：4.54 → **4.17**（只降 8%）。它按设计只改
  「并存那 0.3%」里的选择，作用面与乘性抑制相同，所以降幅同量级。
  **这条记下来是因为它验证了「先量作用面、再决定要不要改」这个顺序**——
  诊断表在跑之前就把结论说对了。
- **⚠️ 方法学陷阱（会再踩）：`value *= 0` 不等于硬禁。** `_bestCross` 的收口是
  `if (!best || value > best.value)`——值全为 0 时 `value > best.value` 恒假，于是
  `!best` 分支照收第一个候选，**仍然传给越位球员**，只是从「取最高分」变成「取遍历顺序第一个」。
  上表「0.00 乘零」与「skip 真硬禁」差出 0.46 次/队场，就是这一条造成的。
  **凡是 argmax 循环带 `!best ||` 兜底的地方，乘零都不是排除。**
  探针的 skip 档刻意把两次 `random()` 照常消费**再** `continue`，与原版随机数流逐位一致，
  所以量到的差异只来自候选剔除，不掺 RNG 错位的蝴蝶效应。
- **`skip` 是这条路径的绝对上限，只关掉到 1.70 的 43% 差距（4.54 → 3.33）。**
  ⚠️ 4 场时读到的是「60%（4.63 → 2.88）」，**被小样本高估了**。
- **❌ 撤回一条 4 场得出的反对意见：「传中占比正在逼近 3% 下限」不成立。**
  12 场实测对照 4.21% / skip 4.23%——**硬禁几乎不动传中占比**，离
  `match-realism-audit:366` 的 3% 下限还有余量。4 场时读到的 3.51%→3.31% 是采样噪声，
  当时据此判断「不能直接采用」是错的。**占比类指标在 4 场样本上有约 ±17% 的偏差，
  不足以对着护栏下限下结论**（→ [[vcfm-small-sample-share-metrics]]）。
- **真正的结论不是「传中估值写错了」，而是「禁区里没有非越位的接应点可选」。**
  含越位候选的 131 次里有 **93 次（71%）压根没有非越位候选**——那正是
  `_passCandidates:2847` 注释所说的「绝境」。所以传中路径的越位**不是选错人造成的**，
  是禁区结构造成的，直接接回主线（禁区触球 10.7 倍、缺快速通路；12 场对照 276.3 复现了
  `_box-touch-count-probe` 的 279.5，两次独立测法一致）。
- **剩下的 3.33 不在传中路径上。** 硬禁传中之后仍是真实值的 2.0 倍，
  **不要再回来拧 `_bestCross`**。下一个杠杆见下方路径分解——已经定位到了。


### 🔑 诊断：越位判罚的路径分解——下一个杠杆已定位（`scripts/_offside-trigger-decomposition-probe.mjs`）

8 场，种子 372000..。**纯测量**：只包装 `_pass` / `_think` / `_resolvePossession` /
`_callOffside`，不消费随机数、不改状态。自检「打桩与不打桩逐场同分且越位数相同」通过
（`1-0 3-2 1-0 2-1 1-1 1-2 0-2 2-1`，73 = 73）。

先读代码得到的结构，**这是理解下面全部数字的前提**：`_pass:2925-2937` 的越位快照收的是
**出脚瞬间所有处于越位位置的队友**，不只是预定接球人。此后有两条互不相干的判罚路径：

| 路径 | 位置 | 触发条件 | 占判罚 | 次/队/场 |
|---|---|---|---|---|
| A | `_think:1698` | **预定接球人**向球做动作即吹，不等真正触球 | 50.7%（37/73） | 2.31 |
| B | `_resolvePossession:5306` | **谁先拿到球**就吹，可以是完全没被瞄准的第三人 | 49.3%（36/73） | 2.25 |

- **❌ 证伪「站在越位位置的人太多」——不要去动 `_clampOffside`。** 暴露量其实**很低**：
  8837 次传球里只有 **294 次（3.3%）**有任何队友处于越位位置，平均 **0.045 人/次传球**
  （分布：0 人 96.7%、1 人 2.6%、2 人 0.4%、≥3 人 0.3%）。
  越位自律（`_clampOffside:3469` 的缓冲与 4~12% 启动失误率）是**够用的**。
- **❌ 也证伪「判定容差太严」——不要去调 tol。** 被吹者在出脚瞬间已越线
  **中位 1.64m**（p90 2.95m），不足 0.5m 的擦线只占 **1.4%**（1/73）。
  这些人是**实打实越位**，不是被 `_isOffsidePosition:3447` 的 `tol=0.45` 单位（≈0.47m）扫进来的。
- **✅ 超标出在转化率，不在暴露量。** 按传球种类（口径同 `match-realism-audit`）：

  | 种类 | 传球数 | 含越位队友 | 被吹 | 转化率 | 占全部判罚 |
  |---|---|---|---|---|---|
  | 普通传球 | 8449 | 251（3.0%） | 53 | **21.1%** | 72.6% |
  | 传中 | 383 | 43（11.2%） | 20 | **46.5%** | 27.4% |
  | 直塞 | 5 | 0 | 0 | — | 0% |

  即**只要有队友站在越位位置，这球就有 1/5（传中近 1/2）的概率被吹掉**。
- **🔑 路径 B 是结构性不真实的那一半，也是下一个杠杆。** 它 **2.25 次/队/场，单这一项
  就超过真实总量 1.70**。语义是：一名越位球员**根本不是传球目标**，却在球飞行途中
  抢先拿到了球。真实足球里处于越位位置的球员**知道自己越位**，多半主动让开或举手示意；
  而 `_resolvePossession:5306` 完全没有这个意识，照常让他参与抢球竞争。
  **着力点：让处于 `b.offsideIds` 且不是 `b.receiverId` 的球员，在 `b.state === "pass"`
  期间不去争这颗球。** 这是照 Law 11 补一条真实行为，不是为压数字加的特例
  （符合本文件顶部的最高设计原则）。
- 路径 A 那一半是「传球者仍然瞄准了已越位的队友」。`_passCandidates:2849` **已有**
  ×(0.16+(1−vision)·0.34) 抑制（能力 15 → 约 ×0.245），而负结果留档八已证明
  在传中侧再加同一个乘法**没有作用面**。**路径 A 要动就得改「越位队友能否成为接球目标」
  的硬性资格，而不是继续拧系数**——先做 B，再看 A 是否仍需动。
- 判罚延迟中位 **0.1 秒**（正好一个 tick）：路径 A 在出脚后立刻吹，球还没飞出去。
  这与 Law 11「触球或干扰才算参与」不严格一致，但**不建议**改成等真正触球——
  那只推迟判罚、不减少数量，还会让球飞一段再回吹，表现层更差。
- ⚠️ **预算**：A 2.31 + B 2.25 = 4.56。**只消除 B 也只到约 2.31，仍是 1.70 的 1.36 倍、
  仍在 1.4~2.1 带外。** 两条都要动才进带内；但 B 是「行为缺失」、A 是「估值口径」，
  先修 B 更站得住。

### ⚖️ 标定：两条路径的修法都有效，但**不能单独采用**（`scripts/_offside-path-fix-calibration-probe.mjs`）

12 场，种子 372000..。**纯测量，不改仓库代码**，只包装两处（接缝选择与逐处核对见脚本文件头）：

- `peelB`：`_resolvePossession` 执行期间把 `b.offsideIds` 里的球员临时置 `sentOff`，
  `finally` 恢复——只影响 `engine.js:5252` 那一处控球候选筛选，语义是「越位球员知道
  自己越位、不去争这颗飞行球」。**反证通过**：标记期间 `_restart` 调用 **0 次**，
  没有把越位球员误排除在重启开球人之外。
- `hardA`：包装 `_passCandidates`，把已越位候选从**返回值**里剔掉。所有 `random()`
  都在原方法内消费完，该次调用的随机数流与原版逐位一致。

自检：control 与未打包装的引擎逐场同分（12 场全同）。

| 档位 | 越位/队场 | 传球/场 | 传中占比 | 射门/队场 | 进球/场 | 禁区触球/队场 | 直塞/场 |
|---|---|---|---|---|---|---|---|
| control 现状 | 4.54 | 1089.7 | 4.21% | 13.75 | 2.92 | 276.3 | 0.75 |
| `peelB` | **2.46** | 1052.6 | 4.20% | 14.04 ⚠ | 3.33 ⚠ | 276.1 | 0.67 |
| `peelAll`（含预定接球人） | 2.46 | 1052.6 | 4.20% | 14.04 | 3.33 | 276.1 | 0.67 |
| `hardA` | 3.92 | 1091.1 | 4.21% | 14.29 ⚠ | 3.50 ⚠ | 272.6 | 0.67 |
| **`peelB` + `hardA`** | **★2.04** | 1093.5 | 4.46% | 15.13 ⚠ | **3.67 ⚠** | 281.5 | 1.00 |
| 真实/护栏 | 1.70（1.4~2.1） | 800~1250 | 3~14% | 11.9~13.8 | **2.5~3.3** | 26.1 | 3.38 |

- **✅ 路径分解被定量证实。** `peelB` 单独就把 4.54 打到 **2.46**（−46%），与分解算出的
  「路径 B = 2.25 次/队场」吻合。**合并两条修法落进 1.4~2.1 带内（2.04）**——
  这是本项目第一次让越位率进入真实带。
- **✅ `peelAll` 与 `peelB` 逐位相同（每个指标、每场比分都一样）**，证明**预定接球人
  从不经由控球归属被吹**——他总是在 `_think:1698` 提前一个 tick 就被吹了。
  路径 A / B 是真正互斥的两条，不重叠。
- **⚠️ `hardA` 只降 14%（4.54 → 3.92），远低于路径 A 的 2.31。** 原因：
  **`_passCandidates` 不是唯一的传球目标来源**——`_bestCross`（2110/2148/2362/…共 9 处调用）
  与 `_bestCutback` 都不走它，所以瞄准越位队友的传中/回做完全没被 `hardA` 拦住。
  **要动路径 A 就得同时覆盖这三个出口，只改 `_passCandidates` 是漏的。**
- **❌ 决定性障碍：修好越位会把进球顶出护栏。** 每个档位的进球都往上走，
  合并档 **3.67 次/场，超出 `match-realism-audit:362` 的 2.5~3.3**；射门 15.13/队场
  也离真实带 11.9~13.8 更远（control 的 13.75 本来是**带内**的）。
  折算下来**每减少一次越位停顿约多 0.15 个进球**（少 2.5 次/队场 → 多 0.75 球/场）。
- **🔑 这条才是真正的结论：过量的越位判罚一直在给进攻**当刹车**。**
  引擎的射门与进球之所以看着落在标定带里，部分是靠每队每场多吹 2.8 次越位把进攻掐掉。
  一旦按真实规则松开，**底下的进攻过量就露出来了**——这正好接回主线那个
  「禁区触球 10.6 倍」的老诊断：进攻侧产量本来就过高。
  **所以越位修法不能先做，必须与「压低禁区循环 / 建真实快速通路」成对做**，
  否则等于把一处失真换成另一处。
- ⚠️ **未验证项**：本探针没测 `match-realism-audit` 的 `stalls`（看门狗清球）与传球成功率。
  `peelB` 让球可能无人接管地滚过去，理论上有僵持风险；传球量 1052~1093 没有异常塌陷，
  但**真要采用必须先跑 `node scripts/match-realism-audit.mjs 24`**，本表只是提前预警。
- ⚠️ 本表**不含传中路径**（`_bestCross` 没有同样干净的接缝）。负结果留档八单独测得
  传中硬禁 = 3.33 次/队场。**三个降幅不可相加**——它们互相重叠，需要合并档实测。

### ✅ 真实参照值总表（2026-09-02 联网取得，改标定前先读这张表）

来源均为官方 PL/Pulselive API（`source:OPTA`）或 Opta/The Analyst 公开文章。
**全部是「每队每场」，除注明外。**⚠️ 2023/24 是多项同时创高的异常季（越位 1.91、
禁区触球 28.0、直塞 1.75、射门 13.80），**只用它单季标定会把引擎整体拉高**，取多季均值。

| 指标 | 真实值 | 可断言强度 |
|---|---|---|
| 越位 | 1.70（带 1.4~2.1） | **可断言**，十二季一手数据 |
| 直塞 尝试 | 1.69（带 1.2~2.2） | 仅量级，见下 |
| 直塞 成功 | 0.74（带 0.5~0.9） | 仅量级 |
| 直塞 成功率 | 43.7%（带 35~50%） | 仅量级 |
| 对方禁区内触球 | 26.1（带 20~30） | **可断言为带** |
| 射门 | 11.9~13.8 | **可断言 11~15** |
| 禁区外射门占比 | 33.1%(23/24) → 43.0%(14/15) 十年递减 | **可断言**（非点球口径） |
| 进球位置 | 禁区内 85.7%（转化 15%）/ 外 14.3%（转化 3.7%） | **可断言**，长期基线 |
| 球在禁区内的时长 | **NOT FOUND** | **不可断言任何值** |
| 单次控球段时长 | 定义间差 6.4~17.6 秒，不可引用 | 不可断言 |

- **`_capAttackersInBox` 那条「真实 60~90 秒」终于有了替代对账量。** Opta 不公布禁区内
  时长（已确认 NOT FOUND），但公布禁区触球数。`scripts/_box-touch-count-probe.mjs` 实测
  引擎 **279.5 次/队/场 vs Opta 26.1，10.7 倍**——而且该探针**只数离散触球
  （出脚/接球/射门），不含带球中的连续触球，是下界**。所以「禁区循环过量」这个从 v239
  背了三个版本的诊断，**第一次有了真实外部参照值支撑，且倍数比原先猜的更严重。**
  构成：出脚 155.1、接球 113.0、射门 11.4（每队每场）。
- **直塞那 0.63 次/场只是 2.3~5.4 倍低，不是 20 倍低，甚至可能是对的。**
  关键在口径：`match-realism-audit` 的 `perMatch` 是 `总数 / 场数`，**合并双方**，
  且数的是**尝试**（`event.type === "pass" && through && !cross`）。故应对照
  「双方合计尝试 3.38」→ **引擎 0.63 是 5.4 倍低**。
- Opta 直塞定义（引用原文）：*"A pass splitting the defence for a team mate to run on to."*
  它是 pass 的**限定词**，而 Opta 的 pass 本身**不含传中与界外球**。所以引擎里
  `through && cross` 那 39~47 次/场**不该计入直塞对账**，现有审计已正确排除。
- **⚠️ 有两个同源字段绝不能拿来对账**：`put_through`（16.0 次/队/场、成功率 46%，
  **无任何公开定义**）与 `pen_area_entries`（27.4~28.9，同样**无公开定义**）。
  按名字直觉去对账会追一个完全不同的量。
- 直塞的两处不稳定，决定了它只能当量级参考：2019/20 与 2020/21 比相邻赛季低 30~65%
  （同一 feed 的传球数与 Opta 公布值逐位吻合，所以是**标注口径变了**，不是数据坏）；
  且跨供应商差 20%（Opta 完成数 0.74 vs Data Sports Group 约 0.6）。
- 一条可用的交叉校验：Analytics FC 统计十一季约 13000 次直塞，**64% 落在对方禁区内**，
  约每 4 次产生一脚射门、每 15 次产生一球。引擎侧可用同口径复算对账。

### ❌ 负结果留档九：`cmp8_ball.py` 跑通了但结果无效——球在禁区内的时长仍拿不到

2026-09-02 趁素材还在家里那台机器上把 `scripts/_video-compare/cmp8_ball.py` 跑完了
（exit 0，`cv2 5.0.0`；此前 AGENTS.md 记的「一次都没跑成」这条待办**至此关闭，但是以负结果关闭**）。
输出**不可用**，四条互相独立的证据：

| 项 | FM26 | VCFM | 为什么是伪影 |
|---|---|---|---|
| 检出率 | 3198/3200 (100%) | 1501/1501 (100%) | 看着完美 |
| **多候选帧** | **3194（99.9%）** | **1501（100%）** | 几乎每帧都有多个符合"球"的连通域 |
| 球速中位 | 1.86 m/s | **0.00 m/s** | 球有 72.7% 的帧位移 <0.5m/s，不可能 |
| 球在禁区帧占比 | 27.5%（≈1483s/90min） | 84.5%（**≈4562s**/90min） | 引擎自测 1092s，**差 4.2 倍** |
| 三区分布 | 后 56/中 35/前 9% | 后 **94**/中 6/**前 0%** | 高光集里前场 0% 不可能（那段有进球） |

- **根因是消歧策略，不是阈值。** `find_ball` 按「小面积 + 高亮 + 低饱和 + 近圆」取候选，
  多候选时 `track()` 选**离上一帧位置最近**的那个。当候选歧义率接近 100%，
  这条规则**必然锁死在静态亮点上**——静态点与 `last` 的距离恒为 ~0，而真球在移动，
  于是每一帧都把静态点选回来，自我强化。VCFM 那个 0.00 m/s 中位就是锁死的指纹。
- **脚本的自检形同虚设**：只有 `len(rows) < 50` 一道门槛（样本量），检出率 100% 反而
  让它看起来健康；`多候选` 数字**打印了但从未用于把关**。
  这是 [[vcfm-audit-dead-assertions]] 那类陷阱在测量脚本上的变体。
- **真正抓住它的是与引擎侧对账**（4562s vs 1092s）。这正是 2026-09-01 交接立下的
  「仪器可信是一切视频量化的前提」那条纪律——当时圆点追踪器是靠 1.91 vs 2.05 m/s
  交叉验证通过才被采信的。**这次没通过，所以一个数字都不能引用。**
- 因此**总表里「球在禁区内的时长 = NOT FOUND / 不可断言任何值」保持不变**，
  现在有两条理由：Opta 不公布，视频这条路也失败了。
  引擎侧的 1092 秒**仍然没有任何外部参照值**，`_capAttackersInBox` 的对账要继续走
  「禁区触球数 26.1」那条替代量。
- **要修的话**（下次素材可达时，不是现在的优先级）：消歧必须按**运动学**而不是按邻近——
  对所有候选建轨迹、挑动力学像球的那条，或从一帧球明确可辨处播种；
  自检必须对 `多候选/检出` 比与「速度中位为 0」硬失败。



留档七提出「缺一条快速进攻通路」，直塞是其典型形态。逐门淘汰（8 门与
`engine.js:2869-2882` 逐字一致，含决定性自检、插桩不改比分）：

| 门 | 首个失败占比 | 独立通过率 |
|---|---|---|
| 1 `aheadOfBall(>4)` | **68.0%** | 32.0% |
| 2 `advance(>0.35)` | **25.2%** | 6.9% |
| 3 `lineGap(<11)` | 5.5% | 29.5% |
| 5 `safety(>0.28)` | 1.0% | 40.2% |
| 7 队冷却 / 8 越位 | 0.1% | 99.7% / 82.0% |

- **卡点在前两门，不在意愿或冷却**：一次决策中「存在几何可行的直塞接球人」只占 **1.5%**，
  而队冷却只挡掉 0.4%、第 6 门意愿期望通过率 51%。**即甲「无人可传」成立，乙「不愿意传」被排除。**
- 第 2 门 `advance > 0.35` 要求接球人比持球者近门 **14 单位（≈15m）**，而全部候选的
  advance 中位仅 **−0.03**、p90 才 0.31——**这道门几乎没人能过**（独立通过率 6.9%）。
- 卡在第 3 门的 3275 对候选中 **100% 是「太靠后」、0% 是「已越位」**，lineGap 中位 39.34 单位。
- 对账：本探针实测直塞 0.67 次/场，与 24 场标定的 0.63 同量级，量的是同一件事。
- 口径限制：分母是「`_passCandidates` 调用 × 队友」，同 tick 可能被调多次，**可比的是
  门与门之间的相对淘汰率，不是绝对次数**。

### 诊断：进攻球员离越位线多远（`scripts/_offside-line-occupancy-probe.mjs`）

上面第 3 门的「100% 太靠后」看起来支持「没人插上」，但那个分母被前两门污染了。
本探针**无条件按 tick 采样每名进攻方外场球员**（仅取本队控球的 tick），结论**反向**：

| 角色 | 带符号 lineGap 中位（正=已越位侧） |
|---|---|
| ATT | **−5.58（−5.9m）**，p90 −0.66 |
| MID | −22.91（−24.1m） |
| DEF | −49.84（−52.3m） |

- **球已进最后三区时的前锋：距线中位 −2.6 单位（−2.7m）、p90 +0.05、80.3% 在 5 单位内。**
  **前锋确实在贴越位线，「没人插上」这个假设被证伪。**
- 全体进攻方外场采样中 2.5% 处于越位侧、19.9% 在距线 5 单位内。
- **所以直塞稀少的原因不是「锋线不贴线」，而是第 2 门 `advance` 的门槛与
  「持球人自己也已经很靠前」共同造成的**：持球人推进到前场后，`myProg - mProg`
  自然趋近 0，advance 永远过不了 0.35。这与留档七「产出全部来自慢速禁区纠缠」
  是同一现象的两面——持球人本来就在禁区里，前方没有 15m 纵深可供推进。

## v240 比赛官员表现层 + 诊断：近静止球员的目标点画像（2026-09-01）

> 缓存 `vcfm-v240`，提交 `cdec34a`

### 表现层：主裁判 + 两名边裁视觉化

- **需求来源**：v239 后画面只有 22 名球员与球，缺主裁判与边裁。真实足球画面始终能看到官员移动，这是比赛完整性的一部分。
- **实现**（`index.html` 3838~3865、`js/main.js` 2835~2879）：主裁判（灰色圆点 18px）+ 两名边裁（灰色圆点 16px），z-index=2 压在球员（z-index=3）与球（z-index=6）之下，`pointer-events:none` 不偷点击。
- **移动逻辑**：主裁 0.22 插值跟球、不进禁区（离门 >16.5m）、横向限 x∈[12,88]；左边裁钉 x=3、y 跟最后第二名防守者（越位线）；右边裁钉 x=97、y 同样跟越位线。所有移动平滑且不越界。
- **验证**（`scripts/_officials-visual-verify.mjs`）：Playwright 跑 6 场、每 0.1s 采样 5400 帧，断言三名官员全可见、尺寸正确、z-index=2、全在场内、中心点命中测试通过（点到的是容器而非官员，说明官员没挡住交互区）。主裁中位距球 12.6%、范围 [6.1, 14.6]（跟球但不贴球）；两名边裁钉在 x=3 和 x=97，y 各移动 12.7 和 21.1 单位（在跟越位线）。

### 诊断：近静止球员的目标点画像（`scripts/_player-stillness-probe.mjs`）

**背景**：视频侧量出 VCFM 全场位移中位 1.91 m/s、近静止占比 **34.0%**，而 FM26 是 2.75 m/s、16.2%。整个引擎在慢动作运行。留档四~七连续四条杠杆失败，共同原因是「循环与产出是同一个现象」。留档七结论：应先**建出缺失的快速进攻通路**而不是继续压制循环。但「建通路」有两种完全不同的含义，工作量差一个量级：

- **甲「目标点太保守」**：球员已站在自己的目标点上，所以不动。缺的是无球冲刺/插入的**决策**，杠杆在 `_attackPlan` 目标点生成。
- **乙「走不快」**：目标点在远处，但加速度/最大速度/体力把他压住了。杠杆在移动物理层，与决策无关。

**测量方式**：6 场种子 372000..372005、能力 15、标准档、每 0.1s 采样，口径与 `_box-entry-rate-probe` / `_box-carry-entry-probe` 一致。速度阈值 1.0 m/s 与视频侧「近静止 <1 m/s」同口径。排除持球人（他的慢是带球控球）与被罚下者，门将单独统计。

**结论（三重独立证据）：甲「目标点太保守」成立，乙「走不快」被排除。**

1. **近静止球员距离自己的目标点中位仅 0.31 米**（p25=0.12m、p75=0.59m、p90=0.79m）——已经站在目标点上。
2. **96.3% 的近静止球员距离目标点 <1.5m**（"已到达"），只有 **1.8%** 距离 >4m 却不动（「远目标却不动」）。
3. **跑动中(>2m/s)球员的目标距离中位 7.65m、p75 15.45m**——远目标的人会跑，近目标的人不动。差别不在移动能力，在目标距离。

**这与 v238 留档的结论一致**（当时"目标 >5m 却不动"仅 0.1%）。

**所以杠杆在 `_attackPlan` 的目标点生成，不在移动物理层。** 球员不是走不快，而是没有被指派更远的跑位。缺的是无球冲刺、纵深插入、拉开空间的决策。（⚠️ 2026-09-02 收窄：只在**最后三区**如此，组织阶段目标距离 9~10m 正常。）

**附加发现**：
- 近静止球员的 fsm 分布：cover 50.6%、home 33%、support 12%、press 1.5%。
- 体力属性中位 0.76（p25=0.73、p10=0.7），排除了体力属性过低这个解释。
- 非持球外场球员速度中位 2.24 m/s、近静止占比 30.2%，与视频侧（1.91 m/s / 34.0%）在同一量级，确认这把尺子量的是同一件事。

**~~下一步~~ 已完成（2026-09-02）**：分支已定位，见本文件「🔑 主线诊断（2026-09-02）」。
**并附一条对本段的口径更正**：本段「近静止球员距目标 0.31m」是**全场合并**的数字，
按球所在分区分解后，组织阶段的目标距离其实是 **9~10m**（正常），**2.08m 只出现在球已进最后三区时**（占无球决策 54.5%）。
所以「缺无球冲刺、纵深插入」应改读作「**最后三区**缺无球冲刺与纵深插入」——组织阶段那条通路是通的。

## v239 禁区盯人、门将封角与比赛表现修复（2026-08-31）

> 本段登记 `1b26120`（表现层）与 `4c4cf87`（禁区盯人/门将/射门冷却）两个提交。二者在 v238 发布后落地却没升版本号与缓存，老客户端拉不到新代码，本次补齐。

- **画面上「空门不射」的根因是防守压制用错了层。** 插桩跑 6 场真实比赛、每 0.1s 采样禁区持球：「离门 10m 内且 3m 内无人防守」每场 **20.5 次**，其中射门只占 9.8%、传球 58.5%；门将离门线中位 5.87m，进攻者杀到 8m 内时仍只有 5.82m、横向偏移仅 1.60m。射门线路距离中位 3.05m < 可达范围 5.66m，引擎判定「够得着」于是既不出击也不封角，但球门两侧其实全是空的。机会本身过量（每场约 336 次禁区持球回合，真实约 40~60），引擎只能靠 `_shoot` 里 420~680 秒的全队射门冷却把射门数压回 24/场，代价是近距离好机会一并被压掉——这才是「站在空门前不射」的来源。**三处必须一起改，单独改任何一处都会破坏标定。**
- 禁区盯人（`_defPlan` / `mark`）：球进到自家球门 11m 内派 **2** 人（原来全程 1 人，而此时禁区里通常已插入两名以上进攻者，一个盯人必然漏一个）；危险区（离门 12m 内）`markWeight` 上限 0.66 → **0.86**，最小离球距离按防守意识分级（顶级约 1.1m、平庸约 2.1m）。原上限 0.66 意味着即使在禁区也有三分之一权重把人拉回区域位置，实测最近防守者中位 2.03m 是「贴人」与「守区域」的混合值，不是真的贴身。
- **分级不能一刀切**：先做成固定 1.15m / 0.86 时强队场均积分从 1.5+ 掉到 1.38，跌破 `match-realism-audit` 下限。盯得住人本来就是能力差异最直接的体现，必须跟属性走（与同函数内既有 `markDistance` 同一原则）。
- 门将（`_thinkGK`）：新增沿「射门点 → 球门中心」连线的站位（`laneShare` 0.55~0.95，越近越贴线），旧实现横向只走 `advance*0.45`、等于几乎不离开中路；单刀（持球者 3.2m 内无己方防守者）且球进到 13m 内时出击上限 7 → **11**，有人协防则保持克制值 7；出击距离仍受 `dGoal-1.1` 约束，**绝不越过球**——越过之后才是真空门。常规站位横向跟随收窄到 0.72（门将横移幅度本就小于球，站到与球同 x 会把近角完全让开）。
- 全队射门冷却（`_shoot`）：420 + rand*260（7~11 分钟）→ **300 + rand*180**（5~8 分钟）。机会质量现在由防守压制，不再需要这么长的人为冷却。先放到 180 + rand*120 时进球升到 3.88、破 3.3 上限，据此回收。
- 24 场标定（全部在 `match-realism-audit` 区间内）：进球/场 2.63 → **2.88**（区间 2.5~3.3）、射门/场 23.5 → 31.1、转化率 11.2% → **9.2%**（区间 9~15%）、禁区外射门占比 45.7% → 37.8%（区间 25~50%）、强队积分/场 2.21（下限 1.5）。探针复测：「无人盯防近距离」机会 20.5 → **11.3** 次/场，其中射门 9.8% → 20.0%、传球 58.5% → 40.0%。
- 表现层修复（`1b26120`，均不改判定结果与球的物理）：`onEvent` 的 `simDrive` 分支 `case "save"` 从未调 `triggerDirectorMoment("save")`，整场无镜头推进与门将倒地；门将兜底 `find(p => p.pos === "GK")` 有一半概率取到对方门将、把动作画在错误一端，改按 `ev.teamId` 选边。入网闪光挂在 `.mp-field.mp-goal-flash::after`，而 `mp-replay-slow` 的 `::after`（"慢镜"角标）写在样式表后面、特异度相同，把闪光整条规则盖掉——**闪光恰好在最需要它的时刻不显示**，改成独立元素。扑救失败 18% 概率的指尖折射不发信号，画面上球自己拐弯，新增只活一帧的 `_deflectPulse`。
- 点球摆位改为沿合法边界布点 + 按 id 派生的确定性抖动 + 6 轮互推去重叠（旧实现是 5×2 等距网格，画面上是两条标尺横排，且站到离球 13~18m 而规则只要求 9.15m）。**抖动刻意不用 `this.random()`**：它是有序流，在 `pScore` 抽样前插入调用会改掉本场之后所有随机数、破坏 seed 决定性。
- `set-piece` 审计原来断言 `player.y >= 24`（旧网格的行坐标），并用 `Math.hypot(x-50, y-12) >= 9.15` 比距离——**坐标不是米**（x 一格 0.68m、y 一格 1.05m），混合单位比 9.15 没有物理含义。改成按真实 Law 14 断言。
- 中场时钟停在 46' 的真因不是取值优先级：`nextDisplayedMinute` 用 `Math.max` 强制单调（有审计钉着），而半场末快照已滚到 46、`ht` 事件写的是 45，`Math.max(46, 45)` 恒为 46。单调保护本身是对的，改成只在 `ht`/`ft` 这两个真实时钟边界显式 reset。
- 已验证：`npm test` 全量通过、24 场标定、探针复测。**浏览器实际观感未验证。**
- 缓存 `vcfm-v239`

### 遗留问题（下一步主线）

1. **禁区持球回合过量是真正的根因，尚未解决。** 每场约 299.8 次禁区持球回合、球在禁区内约 713 秒（真实足球 **60~90 秒**），77.3% 的禁区持球以传球结束。机会过量的原因是球在禁区里循环太久，不是防守松。`press` 上抢者目标点离持球人中位 2.36m（不是 `standoff` 公式看起来的 0.8m——0.8 只在球贴门线时出现），且他**落后自己目标点 2.09m：在追一个随球连续移动的点，永远追不上**（`engine.js` press 分支的 `a.tx/a.ty` 取 `b.x/b.y` 的当前值）。动这条会牵动传球总量（区间 800~1250）和几乎所有标定指标。

   **负结果留档（2026-08-31，已回滚，不要重试）：给 press 目标点加提前量不是这条的解。** 按「读球路提前封堵」的思路，让目标点取 `b.x + b.vx * leadSeconds`（`leadSeconds` 按赶到所需时间估、上限 0.75 秒、按防守意识缩放、提前量本身限 3.2m），实测 6 场（`box-possession-sampling-audit` 同种子）：上抢者**落后自己目标点**由 1.94m 降到 1.75m（如预期改善），但他**本人离持球人**由 2.52m 涨到 **2.65m**，最近防守者 2.34m → 2.39m，禁区秒数 1092 → 1102、无人盯防机会 51.8 → 53.0 次/场，全部微幅恶化。原因是提前量把目标点推离了人本身：上抢者更好地跟住了一个**站在人前面**的点，却没有更靠近人。**「追不上移动点」是对现象的正确描述，但不是可用的着力点。**

   **数据指向的真正着力点是禁区内的传球循环，不是上抢追击。** 本审计基线显示禁区持球回合 **74.4% 以传球结束、仅 3.6% 以射门结束**，回合中位仅 2.9 秒而每场有 346 次——即球在禁区里被短传不断倒来倒去，每次传球又开启新回合，球始终不离开禁区。

   **已量出循环的具体形态（`boxPassDestinationPct` / `medianAttackersInBoxAtSpellStart`）**：以传球结束的禁区回合中，**58.3% 的传球目标点仍在同一禁区内**（41.7% 出禁区），而回合开始时禁区内平均只有 **3 名**进攻方球员（含持球人）。

   **已量出 value 构成，杠杆是 `distPen` 而不是 `advance`（`scripts/_box-pass-value-probe.mjs`）。** 6 场只读采样，统计真实发生过的禁区内传球（因子按 `_passCandidates` 普通传球同式复算，不含直塞加成）：

   | | value | 第一因子 (advance) | safety | distPen |
   |---|---|---|---|---|
   | 传球留在禁区内（855 次） | **0.051** | 0.306 (−0.044) | 0.219 | **0.759** |
   | 传球出禁区（648 次） | 0.038 | 0.235 (−0.115) | **0.338** | 0.581 |
   | 选了留在禁区时，最佳出禁区备选（854 次） | 0.018 | 0.259 (−0.091) | 0.191 | 0.475 |

   - **出禁区选项 99.9% 的时刻都存在**（854/855），不是没有出球点
   - 留在禁区的传球 value 约为最佳出禁区备选的 **2.8 倍**，价值函数确实强烈偏向留下
   - **但主导项是 `distPen`（1.60 倍优势），不是 `advance`（1.18 倍）**。原假设押在「把球传出禁区被记为负推进」上，实测它只是次要项；真正的杠杆是 `distPen = clamp(1 - d/55, 0.2, 1)` **奖励短传**——而禁区内的选项全都是短传（中位 distPen 0.759 对应约 13 个场地单位，出禁区 0.475 对应约 29 个）
   - 最尖锐的一条：**出禁区其实更安全（safety 0.338 vs 0.219），却仍然评分更低**，因为距离惩罚吃掉了安全收益。把球从拥堵的禁区里倒出来在真实足球里是正确动作，这里却因为「传得远」被扣分
   - 注意 advance 中位数即使对留在禁区的传球也是负的（−0.044），说明禁区内传球本身多是横向/略回；两者差距远小于原假设

   （口径提醒：上表是各自分布的中位数，因子相乘只能近似还原 value 中位数，可靠的是排序而非乘积精确值。）

   **由此得出的改动方向（未实施）**：`distPen` 目前只看传球距离，与**所在区域是否拥堵**无关，所以「从拥堵禁区里把球倒出来」这个真实足球里正确的动作，被单纯当成一次更长的传球来扣分——即使实测它更安全。合理的修法是让距离惩罚对上下文敏感（拥堵度高时放宽长传扣分，或让 safety 的权重随拥堵度上升），而**不是**全局调小 `distPen`：那会同时让中后场的长传变便宜，直接冲击传球总量（区间 800~1250）与传球成功率（72~88%）两个标定项。
   动手前务必先读本文件 #1 开头那两条负结果——今天两次失败都是「故事说得通就直接调参数」，而这次的数据恰好又推翻了我自己押在 `advance` 上的假设。**先量，再改。**

   **负结果留档四（2026-08-31）：上面那条「让 `distPen` 对拥堵度敏感」的改动方向已在改引擎之前被实测否决，不要实施。** 探针 `scripts/_congestion-distpen-probe.mjs`（同种子 372000..372005、与 `_box-pass-value-probe.mjs` 同口径），候选式 `distPen = clamp(1 - d/(55*(1+k*c)), 0.2, 1)`，c=0 时逐位等于现状：

   | 变体 | 禁区翻转率 | 非禁区被波及 | 留区优势中位（改前→改后） |
   |---|---|---|---|
   | k0.6 / 断点1-4 | 3.0% | **75.5%** | 0.027 → 0.027 |
   | k1.0 / 断点1-4 | 4.6% | **75.5%** | 0.027 → 0.026 |
   | k1.0 / 断点2-5 | 3.2% | **49.8%** | 0.027 → 0.027 |
   | k1.6 / 断点2-5 | 4.1% | **49.8%** | 0.027 → 0.026 |

   - **根因：拥堵度根本不区分禁区内外，它不是那个判别变量。** 出脚人 8m 内对方非门将球员数的中位数——禁区内留区传球 **3**、禁区内出区传球 **3**、非禁区传球 **2**（且非禁区有 41.4% 的样本恰好也是 3 人）。禁区并不比中场更挤，所以任何以拥堵度为闸门的改动都会同时打开中后场。
   - 代价与收益差一个量级：想要的翻转只有 3~4.6%，而波及了 **49.8%~75.5%** 的非禁区传球、被波及者 `distPen` 中位涨 8~11%。这正是 AGENTS.md 原本警告的「让中后场长传一起变便宜」，只是换了个入口进来。
   - 留区优势中位数 0.027 几乎不动（最多到 0.026），说明**留在禁区的优势很稳，只有尾部会翻**。把 k 继续加大只会先撑爆中后场，翻不动主体。
   - **同时量出一条更该追的异常**：6 场里非禁区传球 4059 次、**禁区内出脚 1503 次（855 留区 + 648 出区）= 每场 250 次，占全部传球的 27%**。真实足球中在对方禁区内出脚的传球只占极小比例。**所以问题不在这 250 次传球被如何估值，而在于每场竟有 250 次是从对方禁区里传出去的**——这与 #1 的「禁区 1092 秒」和上文 crowdedPairs 的「12/12 持球人是 ATT、全在对方禁区纵深」是同一件事的三个观测面。
   - **由此建议的下一步是往上游走，不要再在 value 函数上找闸门**：58.3% 的禁区传球能留在禁区，前提是**禁区里站着可传的接球人**（实测非持球进攻者中位仅 2 名，本就在死代码 `_capAttackersInBox` 的上限之内，所以不是人数堆积）。该验的是这 2 名接球人是**静态占位**还是**跑动插入**——若是无球 support 目标把他们钉在禁区里，杠杆就在 support 目标生成，而不是传球估值。这也能解释 crowdedPairs 为何与之耦合。

   **✅ 已验证（2026-08-31）：是「静态占位」，杠杆在 support 目标生成。这是今天唯一通过实测的诊断。** 探针 `scripts/_box-receiver-occupancy-probe.mjs`（同种子 372000..372005、与上面两份探针同口径），855 次留区传球的接球人画像，三条独立证据同向：

   | 证据 | 实测 | 指向 |
   |---|---|---|
   | 接球人自己的 `offBallTarget` | **97.5% 也在禁区内**（禁区外仅 0.2%） | 是被指过去的，不是路过 |
   | 在对方禁区内连续停留 | 中位 **6.7s**、p75 14.3s、p90 24.1s、最长 **82.4s**；≥4s 占 66.7% | 真实足球不会在对方禁区里站这么久 |
   | 出脚瞬间接球人速度 | 中位 **0.82 m/s**，85.5% 低于 2 m/s | 几乎站着不动，不是插入跑动 |

   - 出脚瞬间禁区内非持球进攻者**人数中位 2，其中目标点也在禁区内的中位同样是 2**——即两个可传对象**全部**是被 support 目标钉在禁区里的静态占位者。
   - 全场层面：support 球员**有 8.9% 的时长把目标点放在对方禁区内**（16006s / 178979s，6 场）。
   - 口径提醒：出脚那一帧接球人 `fsm` 已切到 `receive`（留区/出区两组都是 100% `receive`），所以读到的 `offBallTarget` 是他还在 `support` 时留下的那个、`kind` 多为空。这不影响结论——正是那个目标把他放到了禁区里；而停留时长与速度两条证据独立于此。
   - **机制已定位到代码形态（不是某一行的笔误，是整类分支的共同写法）**：`_attackPlan` 的各 support 分支都用「相对持球人/球再往前推一段」生成目标点，例如 core 分支 `engine.js:3246-3247`
     ```js
     a.tx = clamp(owner.x + side * (6 + this.random() * 5), 8, 92);
     a.ty = clamp(owner.y + dir * (4 + this.random() * 6), 5, 95);
     ```
     以及 3390-3391、3413-3414 等同类写法。`dir` 是进攻方向，所以目标点**恒在持球人前方**，而 `clamp` 只约束到场地边界（y 5~95，几乎就是球门线），**全程没有任何「对方禁区」的概念**。于是持球人一旦进入禁区，接应目标点按构造必然生成在更深的禁区内——这就是 6.7 秒静态占位的来源。
   - **这条因果链把今天三次失败串起来了，它们都在下游**：support 目标把 2 人钉在禁区 → 持球人永远有 2 个静态短传选项 → `distPen` 奖励短传使 58.3% 的禁区传球留在禁区 → 每次留区传球又开启新回合（346 次/场、1092 秒/场）→ 机会过量 → 引擎只能靠 5~8 分钟人为射门冷却压住进球 → 画面上「站在空门前不射」。press 提前量（留档一）、射门冷却阻尼（留档二）、`distPen` 拥堵闸门（留档四）三者全部作用在链条下游，所以都失败。crowdedPairs 的「12/12 持球人是 ATT、全在对方禁区纵深、目标点被拉开」也是同一批 support 目标的另一个观测面。
   - 真实足球的对照行为是：接应者在禁区**边缘**保持接应（等倒三角/二次球），进入禁区应是**有时限的插入跑动**，而不是长时间静态占位。

   **负结果留档五（2026-08-31，已回滚，含完整标定曲线）：直接把 A 类目标点硬性收回禁区边缘不可行——它连真实的禁区抢点一起铲掉了。** 实现方式是在 `_commitOffBallTarget` 收口处加 `_holdSupportAtBoxEdge`（只在「持球人也在同一禁区内」时把目标 y 收到边缘线 84/16），两处调用：进 `resolveOffBallTarget` 前修正候选点、赋值后再收一次（租约命中会原样透传旧目标）。改动前先用 `scripts/_box-support-target-relocation-probe.mjs` 预测过波及面：A 类仅占 support 总时长 **4.7%**、禁区内进攻方存在时长只有 **29.1%** 来自 A 类（B 类抢点 16.6%、其他 54.3%），据此判断「抢点能力不会受伤」。**这个判断错了。**

   | 指标 | 区间 | 改前 | 改后 |
   |---|---|---|---|
   | 进球/场 | 2.5~3.3 | 2.88 | **2.29 ✗** |
   | 转化率 | 9~15% | 9.2% | **7.9% ✗** |
   | 强队积分/场 | ≥1.5 | 2.21 | **1.17 ✗** |
   | 强队比分 | — | 62–33 | **29–33（被反超）** |
   | 射门/场 | — | 31.1 | 28.96 |
   | 禁区外射门占比 | 25~50% | 37.8% | 45.2% ✓ |
   | 传球/场 | 800~1250 | — | 1061.5 ✓ |
   | 传球成功率 | 72~88% | — | 81.7% ✓ |

   - **禁区循环指标确实按预期改善了，方向没错**（box-possession-sampling-audit 同种子对比它自己的基线）：boxSeconds/场 1092 → **980.6**（−10.2%）、boxSpells/场 346 → **319.7**、留区传球占比 58.3% → **51.4%**、无人盯防近距离机会/场 51.8 → **21.2**。press 各项未动（落后目标点 1.94 → 1.94），确认没串到防守路径。
   - **但代价是进攻产出**：传球总量与成功率毫发无损，说明作用面确实窄；破线的是进球、转化率与强队积分。强队被反超（29–33）说明砍掉的不是「人为的循环」，而是强队真正依赖的禁区抢点——这不是能靠缩短射门冷却补回来的标定偏移，**所以没有再去动冷却**（也避免了一次改两处、无法归因）。
   - 预测与实测的差距值得记下：探针预测能消掉 56% 的留区传球（479/855 接球人属 A 类），实际留区占比只降了 6.9pp。原因应是那 43.3% 未归类的接球人在规则生效后仍能提供留区选项。**「按类别统计占比」不能直接当成「改动后的消除率」，中间隔着替代选项的再分配。**
   - 一条被证伪的推测：改前我预期「把接应点推出禁区会让 `crowdedPairs` 下降」，实测**仍是 12**（阈值 14、干净基线 12），既没恶化也没改善。重叠只是换到禁区边缘发生。
   - **流程教训（重要，与本次改动无关但会误导下一个人）**：`node scripts/verify.mjs` **默认不跑 24 场标定**——`match-realism-audit` 在 `verify.mjs:85-88` 的独立 `fullChecks` 列表里，需要显式跑。本次 verify 退出码 0 却已破三条下限，正是因为它没被覆盖。**任何动比赛引擎的改动，verify 通过不等于标定通过，必须另跑 `node scripts/match-realism-audit.mjs 24`（含 background 档）。**
   - ~~**下一步该走的设计（未实施）**：诊断本身仍然成立（静态占位是禁区循环的上游来源，已由三条独立证据确认），错的是「永久排除」这个手段。按真实足球，禁区占用应当是**有时限的插入**：允许接应者进禁区抢点，但给该状态一个存续时限/触发条件（例如仅在传中或倒三角窗口内成立），到点回撤到边缘，而不是无条件禁止进入。这样既掐掉 6.7 秒静态占位，又保住强队的抢点产出。~~
     **↑ 这条建议已于 2026-09-01 被实测否决，见下方留档六。不要按它实施。**

   **负结果留档六（2026-09-01，未改引擎，纯测量否决）：留档五提出的「有时限的禁区插入」不可行——产出与循环来自同一批长停留，任何时限都按同比例砍掉进球。** 探针 `scripts/_box-dwell-outcome-probe.mjs`（同种子起点 372000、能力 15、标准档、dwell 与 `_box-receiver-occupancy-probe.mjs` 逐字同口径）。24 场样本：

   | 分布（秒） | n | 中位 | p25 | p75 | p90 |
   |---|---|---|---|---|---|
   | 禁区内**射门**者出脚时已停留 | 530 | 4.5 | 2.3 | 9.6 | 18.3 |
   | 其中**进球**者（真正产出） | 70 | **7.25** | 3.7 | 14.7 | 21.7 |
   | 留区传球接球人（循环） | 3585 | **7.0** | 3.1 | 14.3 | 23.1 |

   **进球与循环的 dwell 分布几乎逐位重合（中位 7.25 vs 7.0）。** 时限 T 的权衡曲线（T 秒内发生的占比，= 会被保留的部分）：

   | T(秒) | 2 | 3 | 4 | 5 | 6 | 8 | 12 |
   |---|---|---|---|---|---|---|---|
   | 进球保留% | 8.6 | 15.7 | 28.6 | 37.1 | 45.7 | 54.3 | 65.7 |
   | 留区传球保留% | 17.7 | 24.4 | 31.5 | 37.9 | 44.2 | 54.6 | 68.5 |
   | 产出−循环差(pp) | −9.1 | −8.7 | −2.9 | −0.8 | **+1.5** | −0.3 | −2.8 |

   最好的一档（T=6）差值只有 +1.5pp，在 n=70 下是噪声；短时限（T=2~3）反而**优先砍掉进球**（−9pp）。留档五「时限能掐掉静态占位又保住抢点」的前提不成立。

   - **结构性结论（比这条否决更重要）**：**本引擎的进球产出是「靠禁区蹲守」实现的**——得分者在出脚前已在对方禁区连续站了中位 7.25 秒。蹲守不是循环的副产品，它**就是**进球机制。这解释了为什么每一次减少禁区时间的尝试都破进球下限（留档五硬 clamp：2.88→2.29），也解释了强队为何被反超：强队的优势恰恰来自更有效的蹲守。**任何「只减少禁区停留」的改动都必然按比例削掉进球，与手段是硬排除还是软时限无关。**
   - **一处认知修正**：留档五说「静态占位者是禁区里的接球人」是对的，但**射门者不是同一批人**。禁区内射门者 96.4% 处于 `fsm: "home"`、98.4% 无有效 `offBallTarget`——因为射门发生在持球人的决策分支（`canShoot` 在 `_think` 的持球路径里），持球人不经过 `_commitOffBallTarget`。**support 目标机制根本不管射门者**，它只管接球人。留档五改的是接球人侧，却由此削掉了进球，说明两者是同一条链的上下游：先当接球人蹲住，接到球后成为持球人再射。
   - **由此推出的下一步（未实施，需成对改动）**：单独动任一侧都已被证伪——射门意愿放开会破进球上限（留档二：阻尼 0.55→3.33、1.0→3.88），禁区循环收紧会破进球下限（留档五 + 本条）。二者必须**同时**移动：一边压低禁区停留，一边同步放开近门射门意愿，让进球总量留在 2.5~3.3 而禁区秒数下降。留档五刻意避免「一次改两处、无法归因」在方法上是对的，但数据现在表明这是个耦合系统，**分开试必然两头都破线**。归因问题应靠「先各自单独跑出标定曲线、再按曲线配对取值」解决，而不是继续单改。

   **✅ 上游测量（2026-09-01，`scripts/_box-entry-rate-probe.mjs`）：1092 秒的主要贡献者是「进入次数」而不是「每次停留时长」，且最大的新进攻进入机制是「带球越线」。** 留档六封死了压缩停留这条路，于是改问「球凭什么一场进禁区那么多次」。同种子 372000..372005、标准档、纯几何判定（球跨 `_inOwnFoulBox` 边界，短于宽限期的出线合并为同一次进入）。

   - **先纠正一个易错量级**：`boxSpells` 346/场是**禁区内持球回合数**，不是进入次数——禁区内传给队友会结束一个回合、开启下一个（留区传球占 58.3%），346 里含大量同一次进入内的连续倒脚。两者不可混用。
   - **仪器交叉验证**：本探针独立累计的「进攻方持球在对方禁区内」秒数 **1103.95/场**，与 `box-possession-sampling-audit` 的 1092.12 只差 **0.9%**，两个独立实现同向，尺子可信。
   - **进入次数对判定宽限期不敏感**（这条决定 200+ 这个数是不是禁区线抖动的假象）：宽限期 0.4s → 1.0s → 2.0s（放大 5 倍），进入次数 223.2 → 214.0 → **202.3**，只降 9%。**每场两百多次进入是真的。**
   - **时长分解**（取最保守的 2.0s 宽限期）：进入 **202.3 次/场** × 每次中位 **4.5 秒**（p25 1.6、p75 8.9、p90 14.8、最长 39.7）≈ 909 秒，与实测 1104 秒同量级（均值高于中位，尾部拉长）。**次数是主要项。**
   - **进入机制归因**（占比 / 每场次数）：

     | 机制 | 占比 | 次/场 |
     |---|---|---|
     | 出区后又回流（**非新进攻**，起脚点在同一禁区内） | 38.7% | 78.3 |
     | **带球越线** | 20.8% | **42.0** |
     | 传中 | 13.2% | 26.7 |
     | 普通传球（自禁区外） | 11.3% | 22.8 |
     | 二次球/散球滚入 | 5.1% | 10.3 |
     | 防守方解围/回传滚入 | 4.6% | 9.3 |
     | 射门/折射滚入 | 4.0% | 8.0 |

   - **最大的「新进攻进入」机制是带球越线，42 次/场，比传中（26.7）还多 1.6 倍。** 这个比例本身就不真实，**且不依赖任何外部参照值即可判断**：真实足球里进禁区绝大多数靠传球与传中，带球直接杀进禁区是少数事件，不可能超过传中。**即进攻方能过于轻易地带球走进禁区，没有人在禁区边缘把他拦下来。**
   - **这是一条与留档一~六都不同的着力点**：前六条动的是 support 目标、press 提前量、传球估值、射门冷却，**没有一条动过「持球人能不能走进禁区」**。它属于防守侧（禁区边缘的拦阻），而留档六封死的是进攻侧的停留时长，两者不冲突。
   - 进入的结束方式：被防守方夺回/清空 48.3%、传/带出禁区 43.8%、**射门合计仅 7.2%**（射门后出区 5.8% + 射门后死球 1.4%）。同一波进攻内球反复进出禁区中位 1 次、p90 2 次、最多 6 次。
   - **⚠️ 尚未验证的部分（不要当依据）**：「真实足球一场约 30~50 次禁区进入」是我的估计，**与备忘里「禁区 60~90 秒」一样没有实测来源**。所以「202 次是几倍于真实」这个倍数不可引用；可引用的是上面那条**免参照的内部比例**（带球越线 > 传中）。真实参照值仍应由 `scripts/_video-compare/cmp8_ball.py` 从 FM 录屏取得（素材在原机器桌面）。

   **负结果留档七（2026-09-01，未改引擎，纯测量否决）：上一条推测的「禁区边缘无人拦阻」是错的，且带球越线不可压制——它是产出效率最高的进入方式。** 探针 `scripts/_box-carry-entry-probe.mjs`（同种子、同宽限期 2.0s，分类与 entry 探针逐字一致、两者都报 42 次/场可对账）。

   上一条据「`_inOwnFoulBox` 在 `_defPlan` 3663~4206 区间一次都没出现」推断防守侧缺少禁区边缘拦阻。**代码观察是对的，推断出的现象是错的**：

   | 越线瞬间 | 实测 |
   |---|---|
   | 最近防守者距离（米） | 中位 **2.64**（p25 2.24、p75 3.07、p90 3.62） |
   | 最近者 ≤3m 占比 | **72.2%** |
   | 最近者 >5m 占比 | **2.0%** |
   | 最近者在身前（goal-side） | 62.7% |
   | 3m 内防守者为 0 人 | 27.8% |
   | 最近者 fsm | **cover 67.1%** / press 29.0% / mark 4.0% |
   | 越线者速度（m/s） | **中位 0.72**（p75 1.47） |

   - **不是「没人拦」（甲），也不纯是「拦不住」（乙），而是第三种我没预设的情况：人就在旁边，但他的职责不是拦阻。** 72.2% 的情况下 3m 内有防守者、且六成在身前，但其中 **67.1% 处于 `cover`（区域盯防）**，不是 `press` 也不是 `mark`——最近的人在守区域，没有转为上抢。
   - **越线速度中位仅 0.72 m/s（走路速度）。** 所以这 42 次/场根本不是「盘带突入」，而是**球以走路速度被推过禁区线，旁边 2.64m 站着一个不上抢的防守者**。
   - **这与 #1 开头早已记录的 press 保持距离是同一个现象，不是新杠杆**：本审计基线 `nearestDefenderToCarrierMedian` 2.34m、press 目标点离持球人 2.36m，与这里的 2.64m 同一量级。留档一（给 press 目标点加提前量）已经在这条路上失败过。
   - **🚨 安全闸门已触发，不要压制带球越线**：产出对照显示它的效率是传球进入的两倍——

     | 进入方式 | 次/场 | 产生射门% | 产生进球% |
     |---|---|---|---|
     | 带球越线 | 42.0 | **14.3** | **2.8** |
     | 传球进入 | 121.7 | 6.8 | 1.2 |

     压制产出效率更高的那一类，必然重演留档五（进球 2.88→2.29、强队 62–33 被反超）。**所以「进入率」这条路同样封死。**
   - **四条杠杆连续因同一原因失败，这本身是结论**：留档五（support 目标）、留档六（停留时长）、留档四（传球估值）、本条（进入率）——每次都是「循环与产出是同一个现象」。本引擎的进攻产出全部来自慢速、拥堵、近距离的禁区纠缠（0.72 m/s 越线、7.25 秒蹲守、2.6m 不上抢），**没有一条独立的「快速进攻」通路可以在压制循环时接住产出**。
   - **由此得出的方向（未实施，工作量远大于前几轮）**：不要再找「压制循环」的手段，任何一条都会削产出。应先**建出缺失的快速进攻通路**（真实足球的进球来自快速配合、跑位插入与一脚出球），等产出有了替代来源，循环才可能被压下去而不破下限。这与交接「下一步建议」第 2 条一致：用户抱怨的本来就是**跑位、传球、射门的还原度（决策层）**，而这四轮测量恰好从反面确认了「决策层缺一条快速通路」。
   - 一处流程留档：本探针第一版把「带球越线」判定为 `b.lastKicker === owner.id`，**检出 0 次**——带球时 `lastKicker` 往往是早先传球给他的人，不是他自己。是与 entry 探针互相对账才发现的。**两个探针报同一个量时必须对账，数字对不上先怀疑分类条件。**

   **注意这排除了「禁区里人太多」这一解释，也排除了复活死代码这条路。** `js/matchview.js:4409` 的 `_capAttackersInBox` 确实实现了「有球方非持球进大禁区 ≤3」的软顶，且按 v238 的结论整个路径是死代码（`matchview.js:5524` 早退短路）。但该上限**不含持球人**，而上面实测的 3 人**含持球人**，即非持球者中位只有 2 名——**本就在死代码上限之内，复活它在中位数上根本不生效。** 所以循环不是人数堆积造成的：禁区里只有两三个人，传球却有近六成仍然选择留在禁区内的那一个。**杠杆在传球价值评估（`_bestPass` 的 value 函数）为何持续把禁区内的短传选项排在最前，而不是在人数或跑位分布。** value 构成已量出（见上表），主导项是 `distPen` 的短传奖励。

   **已定位到具体代码：`rangeBonus` 对近距离是反向激励。** 射门决策（`engine.js` `_think` 内 `shootDecisionP`）里：

   ```js
   const rangeBonus = dGoal >= 9.5 && dGoal <= 22 ? 0.32 : dGoal < 9.5 ? 0.1 : 0;
   ```

   **离门 9.5m 内只给 0.1，而 15m 处给 0.32——越靠近球门越不愿射。** 原注释的动机是「避免强队总是一路带到六码区才射」，但代价正是画面上的「站在空门前不射」。再叠上冷却期的 `cdBlocked ? 0.3`，一个无人盯防、离门 8m 的球员每次决策的射门概率只剩约 0.05~0.07。这与本审计实测「无人盯防近距离机会里只有 3.5% 以射门结束、67.5% 以传球结束」完全对得上。另注意 2330 行的门槛把 `shootQuality` 与 `passQuality` 直接对比，禁区内短传质量天然高，进一步压低射门。

   **负结果留档二（2026-08-31，已回滚，含完整标定曲线）：放宽近门区的冷却重压不可行，原因是它在治根因的症状。** `canShoot` 已把 `dGoal < 9.5` 豁免出全队射门冷却（注释写明为避免门前无动作可选→底线僵持），但 `shootDecisionP` 随后仍对同一脚球乘 `cdBlocked ? 0.3`。**这不是笔误**——原注释明确写「穿透全队冷却的门前射门是『保活性』的例外通道，不是常规机会：概率重压（×0.3）」，是有意设计。可议的是它的后果：冷却长 5~8 分钟意味着 `cdBlocked` 大部分时间为真，于是「例外通道」实为常态通道。按 3 个阻尼值跑满 24 场标定：

   | 阻尼 | 进球（≤3.3） | 转化率（9~15%） | 禁区外占比（25~50%） | 无人盯防机会的射门占比 |
   |---|---|---|---|---|
   | **0.3（现状）** | **2.88 ✓** | 9.2%（贴下限） | 37.8% | **3.5% ✗ 画面问题** |
   | 0.55 | 3.33 ✗ 破顶 | 10.1% | 35.1% | — |
   | 1.0（完全豁免） | 3.88 ✗ 破顶 | 10.9% | 30.5% | **9.8% ✓** |

   斜率约 1.43 球/单位阻尼；要把进球压回 3.15 需阻尼 ≈0.48。但**另有次生回归**：`box-defending-audit` 的 `crowdedPairs` 在 0.55 与 1.0 两处都是 **17**，超过阈值 14（干净引擎实测 **12**——注意该审计注释写的基线是 9，实际已漂到 12，余量只有 2）。两个阻尼值给出相同的 17，说明它在这一区间不敏感，≈0.48 大概也过不了。

   **结论：不能先修「门前不射」，必须先修「门前机会过量」。** 全队射门冷却存在的唯一理由就是压制过量机会（每场禁区内 1092 秒 vs 真实 60~90 秒）。在机会供给仍高一个量级时放开任何一处射门意愿，进球必然破顶——冷却是当前唯一压住进球的东西，而它本身是项目自己承认的「人为冷却」。**修复顺序应当是：先降低禁区持球循环（把 boxSeconds/boxSpells 拉向真实量级），冷却随之可以缩短或取消，届时门前射门意愿自然回归，无需与标定上限搏斗。** 若将来仍要单独动这一处，注意 `rangeBonus` 对近门区同样是反向激励（<9.5m 给 0.1，10~22m 给 0.32），且真正的空门走另一条通道（`clearOpenGoal` → `openGoalDecisionP` 0.72~0.98）不受该乘数影响。
2. **转化率 9.2% 贴着下限 9%，余量只剩 0.2pp**，后续任何提高机会质量的改动都要重新标定。
3. ~~**审计有系统性盲区**~~ **（已补）**：`goalkeeper-open-goal-audit` 与 `box-defending-audit` 都是人为摆位的合成场景，只验证「摆成空门时会射」，不验证「空门在真实比赛里出现得多频繁」——所以审计通过而画面明显不对。已新增 `scripts/box-possession-sampling-audit.mjs`：跑固定种子完整比赛、每 0.1 秒采样禁区局面，把禁区持球时长、回合结束方式、无人盯防机会频率、上抢追击与门将封角距离变成可回归断言。**全程只读引擎公开状态，不消费随机数，同种子下开关它不改变比分。** 已进 `verify.mjs` 默认列表（6 场约 50 秒）。
   - 该审计与那次一次性插桩**口径不同，两组数字不可直接相减**（它合并两队进攻、只统计 `held`/`control` 的真实持球、机会用 0.5 秒滞回折叠连续帧）。同口径的三项交叉验证了仪器：回合以传球结束 74.4% vs 插桩 77.3%、press 目标点离持球人 2.27m vs 2.36m、落后自己目标点 1.94m vs 2.09m。**要比较就用该审计自己的基线比。**
   - 上界一律是「实测基线 + 约 10% 余量」，作用是**防止进一步恶化**，不代表这些值已经真实——禁区内 1092 秒/场对比真实的 60~90 秒仍差一个量级。主线修好后应同步收紧。
4. 一次误诊留档：持球人身边的防守者 **79% 的时间执行 `press` 而非 `mark`**，所以第一版的盯人参数对「贴身逼抢持球人」完全无效（最近防守者中位数改动前后都是 2.03m）。本次「机会减半」主要来自门将不再让开角度。**改防守行为前先确认该球员实际走哪条职责分支。**

5. **`box-defending-audit` 的 `crowdedPairs` 余量已被历史漂移吃掉，且它与 #1 是同一个根因。** 该断言上限 14，注释写的基线是「4 场共 9 段」，但干净引擎实测已是 **12**——余量只剩 2，不是注释暗示的 5。这不是某一次改动引入的，是历次累积。新增诊断脚本 `scripts/_crowded-pairs-probe.mjs`（`_` 前缀，不进 `verify.mjs`）复用审计同种子逐条打出这 12 段，结果高度集中：
   - **12/12 全部发生在对方禁区纵深**，且 **12/12 的持球人都是 ATT**；角色对以 ATT+MID 为主（7/12）
   - 重叠持续时间中位 **3.5 秒**、最长 5.1 秒，而检测门槛只要 0.6 秒——是两名队友在对方禁区里长时间站在彼此的目标点上，不是一闪而过的抖动
   - 4 场中 3 场各 4 段，`165003` 一段都没有

   **所以 `crowdedPairs` 不是独立缺陷，而是 #1 那个禁区循环的另一个观测面**：接应球员挤进禁区、目标点互相重叠，正是「58.3% 的禁区传球仍落在禁区内」得以成立的条件。这也解释了为什么放宽近门区射门时它在阻尼 0.55 与 1.0 两处都跳到 17——两者都提高了禁区内的进攻活动，重叠随之增加。**修好禁区循环应当同时把 `crowdedPairs` 带回低位；反过来，任何提高禁区占用的改动都会立刻顶到 14。**

   未解开的一处观察：12 段里有 **5 段的持续时间恰好都是 5.1 秒**，完全相同的数值通常意味着背后有个定时器。已排除无球路线租约（`off-ball-movement.js` 的 `leaseSeconds` 是 1.5 秒）；也注意 `engine.js` 里防守 support 距离常量恰好是 5.1 但那是**米**、与秒无关（勿混单位）。找到这个定时器可能就是收口的着力点。

   **负结果留档三（2026-08-31）：5.1 秒不是相位翻转定时器，该假设已实测排除。** 假设是 `teamShapePhase` 的 ATTACKING_TRANSITION → IN_POSSESSION 翻转（阈值 `attackSeconds = clamp(4.7 + (tempo-3)*0.55 + styleAdj, 2.8, 8.8)`，探针球队 tempo 3/balanced 恰好 4.7，量级对得上），机制上说得通：off-ball 租约的 `sameContext` 要求 `previous.phase === phase`，翻转会让所有 support 目标在同一帧失效。实测 `scripts/_crowded-pairs-end-reason-probe.mjs`（与 `_crowded-pairs-probe.mjs` 同种子同口径，只加归因字段）：**符合「结束时刻落在翻转阈值上」的段数 0/12**，且 12 段全部自始至终处于 `in-possession`、段结束时 `attackAge` 在 24~74 秒之间，早已远离 4.7 秒边界。**又一次「机制说得通但数据不支持」，仍然是先量才发现。**
   - 同时量出：**12 段的结束原因全部是「目标点被拉开」**（不是持球人易主、也不是离开 support 职责），**12/12 的持球人都是 ATT**。所以终止者在无球跑位的去重叠逻辑里，不在球权或职责切换上。
   - `off-ball-movement.js` 的去重叠（`activeReservations` + `supportOptions`）目标间距 `supportSpacingMetres` 是 **1.8m**，而审计的重叠判定门槛是 **1.6m**——即该逻辑一旦运行，结果必然已越过审计门槛。**能活下来的重叠是那些去重叠根本没运行的**（`activeReservations` 为空，例如同帧内两名球员的预约登记有先后顺序），而不是运行了却没解开。下一个该验的是预约登记顺序，不是间距常量。
   - 5.1 秒本身仍未解释。但它属于跑位层，与 #1 的传球价值函数（`distPen`）无关，**不构成先修 `distPen` 的前置条件**。
   - 附带发现，与 #1 同向且更刺眼：段结束时 `attackAge` 常态在 **41.9 / 54 / 55.8 / 69 / 73 秒**——即**一方连续控球超过一分钟不易主**。这是「球留在禁区 1092 秒」的另一个观测面，可作为主线修复的第二个回归指标（真实足球单次控球段远短于此）。

## v238 球场标线与引擎判定对齐（2026-08-29）

- **画出来的禁区和引擎判罚的禁区从来不是同一个。** `mp-lines` 的 SVG 用 `viewBox="0 0 100 150"`，而球员用 `style.left/top = x%/y%` 定位、坐标是引擎的 0-100，所以 **SVG_y = engine_y × 1.5**。按这个换算，画出的大禁区 `y 117-147` 等于引擎 `y 78-98`，而 `_inOwnFoulBox` 从 `y >= 84` 才判罚——**浅了 6 个单位、约 6.3 米**。站在 `y=80` 的球员看着在禁区里、引擎算他在禁区外，玩家看到的就是「禁区内犯规不判点球」。边线同理画在 `x 3-97 / y 2-98`，而球员可以走到 0 和 100，会跑到线外。
- 根因是认知而非笔误：`docs/matchview-coords-migration-phase3.md` 当年写着「SVG 坐标与逻辑坐标 1:1 对应」（y 方向其实 1:1.5）+「这些值从不改变」，于是没人再对过账。已在原文追加更正，不抹历史。**「从不改变」不等于「是对的」，静态常量同样要和真相来源对一次账。**
- 标线按 `引擎坐标 × [1, 1.5]` 重画，并对齐引擎常量：禁区 → `_inOwnFoulBox`（x 22-78、y>=84），球门口 → `SIM.GOAL_X0/X1`（44/56）。中圈改 `<ellipse>`：x/y 缩放比不同（68 m 对 105 m），画 `<circle>` 在屏幕上是椭圆。小禁区/点球点/罚球弧/角球弧改用真实尺寸（18.32×5.5 m、11 m、1 m）。
- **`matchview.js` 的「导演 AI」是死代码，不是坐标覆盖源。** 排查时确认：玩家场恒定 `simDrive = true`（`js/sim/adapt.js` 的 `shouldUseSim` 只要有 `userSide` 就 true），`matchview.js:5524` 的早退把其后约 3400 行位置逻辑整体短路——那些「每帧强制无球方不得蹲禁区」「禁区人数软顶 ≤3」的硬约束**一条都不生效**。唯一活着的 fallback 是进球录帧 <4 帧时走 `playGoalHighlight`。**改比赛观感要动 `js/sim/engine.js`，不要去 matchview 找覆盖逻辑。**
- 代码里有多套互不相同的「禁区」定义，`_inOwnFoulBox` 才是判罚真相；`js/matchview-coords.js` 的 `AREA.BOX_LARGE`（宽 44%）差得最远，但只被死路径用，本次未动。
- **肉眼读静态帧会系统性高估「球员静止」。** 看 6 帧连续快照曾判断「球员像雕像不动、目标离得老远却不走」，被实测全面推翻：位移中位数 2.05 m/s、几乎静止占比 13.0%、目标 >5m 却不动仅 **0.1%**。2.5 秒内 2 m/s 只移动 5 米，缩略图上看不出来。**画面问题先量化再动手。**
- 量化后确认的真问题是队形纵向被拉长，且**只发生在进攻三区**：本方三区 39.5 m（正常）、中场 41.9 m、进攻三区 **58.9 m**（真实 30-40）。分区分解显示不是「防线不跟进」——后卫线已压到距己方球门 51.4 m，但中线是 52.5 m，**后卫线始终没过中线**，而前锋已站到 90.2 m（对方禁区内）。
- **根因：静态阵型模板本身就有 59.85 m 长，而引擎没有「球队整体随球平移并压缩」的机制。** 4-3-3 的 slots（`js/data.js:775`）后卫最靠后 `y=75`（距己方球门 26.25 m）、前锋最靠前 `y=18`（86.1 m），模板长度 **59.85 m**。`home` 状态只做 `baseY + (ballY - baseY) × 0.2` 的朝球浮动（`engine.js:4152-4154`），而 `_applyExplicitShapeAnchor` **仅在玩家显式配置了阶段阵型方案时才生效**（`engine.js:846-847` 无方案直接 return false）。于是进攻三区时球员基本钉在静态模板上，实测 58.9 m ≈ 模板的 59.85 m；本方三区因为朝球浮动把前锋拉回来才显得正常（39.5 m）。`_defLineY` 有威胁度回收逻辑，但只服务防守任务，不管进攻时的整体前压。
- 修这个要加的是 team block 的纵向平移 + 长度压缩（真实球队是一个 30-40 m 的块整体跟球移动），**会显著改变比赛轨迹**（进球、射门、控球），必须跨 n=24/n=48 复核并重跑全套比赛审计。留给后续。
- 新增 `scripts/_pitch-snapshot.mjs`（按场景抓连续帧渲染 PNG）、`_shape-metrics.mjs`（队形指标 vs 真实基准）、`_shape-zones.mjs`（按区域分解）、`_pitch-lines-check.mjs`（新旧标线叠加引擎边界对比）。均为 `_` 前缀、不进 `verify.mjs`。注意其中「真实基准」是推测量级、非公开追踪数据实测，偏离大的结论稳健，边缘偏离不足以单独支撑改动。
- 已验证：`node --check`、`npm test`、标线对齐对比图（白线与引擎边界重合）
- 缓存 `vcfm-v238`

## v237 禁区贴身防守与接应目标分离（2026-08-27）

- **对方在自家禁区持球时，防守方实际站着看。** 实测最近防守者离球 2.42 米、中卫 3.95 米，`mark` 只占防守任务时长的 **0.2%**。三条规则叠出来的：① `_refreshDefPlan` 的 `markingLimit` 在阵地战下要求 `pressing >= 5`，而多数俱乐部是 3，于是禁区里一个盯人都不派，四名后卫全落在 `shape`；② `_separateAgents` 在禁区内把最小间距**放大**到 3.35 场地单位（沿球门方向合 3.52 米），比禁区外的 2.85 还大——动机是让 2D 画面里小禁区的圆点不糊成一团，代价是战术目标要求的 3.05 米被这条几何下限直接顶开，**贴身防守物理上不可能**；③ 盯人者被支援安全圈推到离球 2.8 米，被盯者一靠近球就自动松开。
- 修复：球进入自家禁区 16.5 米（真实禁区纵深）内时给 1 个盯人名额；身体半径统一用同一个下限，不因球进禁区而变大；盯人者禁区内安全圈收到 1.9 米。实测最近防守者 2.42 → **2.05 米**，2 米内防守人数 0.46 → **0.66**，`mark` 任务时长 **44 倍**。
- **贴身程度此前与属性完全无关**（`markDistance` 只读球到球门的距离），弱队后卫因此拿到与顶级中卫等同的贴身收益——既压缩实力差，也不真实。改为读防守意识（marking/positioning/decisions），1.5–3.4 米。首轮只做前三条时强队分/场由 2.08 掉到 1.71、净胜球比值 1.81→1.63；属性化之后净胜球比值回到 **1.80**。
- **抢断频次不是问题所在，反而不能动。** 实测成功抢断 44.5 次/场（双方合计）已在现实区间 30–40 的上沿，而审计上限是 55。禁区内上抢者**在射程内的时间占 74.4%，但冷却开着的只有 0.9%**——全队抢断冷却 44–54 秒（`this.t` 是秒）使其长期饱和。冷却量级本身可疑（相邻的丢球方只有 7/8 秒），但它掩盖的是引擎缺少「贴身封堵但不出脚」状态：真实后卫在射程内是贴住、封角、等失误，而引擎只有"在范围内 → 掷骰"，只能用超长冷却把频次压回现实值。**这条留给后续，改数字会直接冲破上限断言。**
- **接应目标拥挤（`support-target-crowding`）从 8 降到 0。** 根因不是分层失败——实测全部事件 `sources` 都是 `["updated","updated"]`，去拥挤压根没运行。真因是 `_commitOffBallTarget` 先分层再 `_clampOffside`：越位夹取把纵深投影到越位线，把刚分开的接应点重新压回一起，最终间距只剩两人 role buffer 之差（2.55 与 1.15 单位相差 1.47 米，报告 gap 1.44 米），而检测阈值是 1.6 米。**这两个口径从来没对齐过。**
- 新增 `_separateSupportTargets()`：在本 tick 全部球员提交完目标后做一次队级横向松弛。逐球员分层读的是队友**上一 tick** 的预留表，同一 tick 内排在前面的球员看到的全是过期数据；队级后处理同时看到全部最终目标，不受顺序与过期影响。只动 x、纵深锁在越位线上，有界 Gauss-Seidel 最多 4 轮，配对与分侧由稳定 id 决定，不消费随机数。分组按 `team + ownerId`——**刻意不按阶段**，`offBallTarget.phase` 记的是提交那一刻的阶段，按它分组会把本该分开的两点判成互不相干（这是修了三次没修好的原因）。
- 死球摆位期间不参与判定：定位球要求球员站到法定位置，此时目标重叠是规则结果；检测器本身也在 `ball.restartType` 存在时重置计时。
- **点球重标定。** 禁区贴身让点球升到 0.63（门槛 0.5）。来源分解（后台档 12 场）：抢断犯规 0.167→0.25、门将出击 0.25 不变、**手球 0→0.25**。手球总数没变（0.29→0.27），是发生地点从禁区外挪进了禁区——因果正确，但总量超标。`_commitFoul` 的 `inBox` 乘数 0.04 → **0.014**、`handballContactDecision` 的 `inPenaltyArea` 乘数 0.42 → **0.14**，系数 0.334 由实测推导。后台档 0.63 → **0.40**、标准档 0.54 → **0.33**。
- 两个"失败"经样本翻倍证伪为混沌噪声：标准档点球 0.54→0.42、后台档进球 3.38→3.19。**改变轨迹的改动必须跨样本量复核，不能只看 n=24。**
- `phase-shape-evidence-audit` 的 `extraStepSharePct` 预算 32 → 34：禁区贴身让 `close-contest` 细步窗口更常触发，这是预期后果（细步本就是为了让接触判定更准）。实测代价 8 场后台档 16.6 → 17.4 秒（+4.8%），占比 31.9% → 32.3%；基线原本只剩 0.1pp 余量。
- 新增 `scripts/box-defending-audit.mjs`，已登记进 `verify.mjs`。
- 已验证：`npm test`、标准档 n=24 与 n=48、后台档 n=48、`match-motion-integrity-audit`（标准档 0 warning，基线 6）、`off-ball-movement-audit`、`edge-rules-audit`
- 缓存 `vcfm-v237`

## v236 国家队征召代价与赛前准备对称（2026-08-26）

- **国家队征召此前对俱乐部零后果。** `js/intl.js` 只在 `nationalCallupScore` 里**读** fitness 做征召评分，`runInternationalBreak` 跑完比赛只把 `callups` 交给 `appendInternationalNews` 发新闻，全文件没有一处**写**体能或伤病——球员打完国家队比赛满体能回来、零伤病风险。新增 `applyCallupCost`：整场出战按与 `match.js` 的 `drainFitness` 同量级（4–9）扣体能，另加长途奔波固定项 2，下限同样取 35；每人承担一次伤病判定，沿用 `injuries.js` 既有的诊断与复发风险，不新增伤病类型。
- 单场伤病基准 `CALLUP_INJURY_BASE = 0.02`，与俱乐部比赛同量级（`tryInjury` 每分钟 0.005、每次随机选一队，90 分钟约合每队每场 0.22 次，摊到 11 人即约 0.02/人）。**征召更让俱乐部头疼不是因为基准更高，而是国家队没有俱乐部队医与康复设施的减免**（`doctorInjuryMod` / `trainingInjuryMod`），再加上奔波消耗。
- 伤情与比赛结果存在同一条记录：`match.callupInjuries` 由 `runInternationalBreak` 汇总返回，推进事件 `international_break` 携带被征召人数与自家伤员，工作台推进摘要按中英双语展示。受伤球员由既有的 `nationalAvailable` 过滤，不会在下一个比赛日被再次征召。
- **赛前准备模式此前是玩家独占机制。** `training-boost.js` 的攻防/体能/士气加成只有玩家 UI 与 `delegation.js` 会设置，AI 走 `autoPickTraining` 只设 focus/intensity，永远停在 `balanced`——而 `match.js:523` 对双方都调 `applyMatchPrepBonus`，等于给玩家一份单向的 ±5% 攻防加成，正是最高设计原则禁止的隐藏加权。
- 新增 `pickPrepMode` 作为准备模式的唯一决策口径，玩家助教（`assistantTrainingPlan`）与 AI（`autoPickTraining`）共用同一实现、同一份输入口径（平均体能均只统计可出场球员），并同样经过 `setTrainingMode` 的 3 天冷却。决策全程不消费随机数，不改变 `autoPickTraining` 原有的训练抽取。
- `nextMatchDaysByClub` 改为 `nextMatchByClub`，同一次赛程扫描顺带带出对手 id；`processTrainingDay` 建一次俱乐部索引，避免为 270 家俱乐部逐个 `find` 对手。
- 删除死代码：`js/matchview-canvas.js`（315 行，全仓库零 import，只在 `docs/matchview-optimization*.md` 里提到，是那次优化建的 4 个模块中唯一没接进去的）；`js/matchday-income.js` 的 `calculateMatchdayBonus()` 及其私有辅助（224 → 57 行）——它与 `facilities.js` 的 `matchdayIncome()` 重复计算德比、杯赛阶段与争冠/保级加成，靠一句文档注释"禁止叠加"防守，是用了就出 bug 的定时炸弹。
- 新增 `scripts/international-callup-audit.mjs`、`scripts/training-prep-symmetry-audit.mjs`，均已登记进 `verify.mjs`。
- **生态样本的对比方法**：`scripts/ecosystem-audit.mjs` 新增 `VCFM_ECO_SEED`（默认种子不变，样本仍逐字段可复现）。本次改动给每名出场球员增加了 2 次随机抽取，固定种子下这足以让整份五赛季样本走进另一个随机世界——**单点对比会得出完全错误的结论**：首轮实测债务余额 2902 万 vs 基线 1880 万（+54%），看起来像严重的经济退化，但把两边各跑几个种子后，基线自身波动 1880–2568 万、改动后 2083–2902 万，两个区间完全重叠。**任何改变随机抽取次数的改动，都必须跨种子比区间，不能比单点。**
- 唯一跨种子稳定的差异是 `indebtedClubs`：基线 18–21，改动后 26–27（270 家里 6.7% → 9.6%）。这是预期方向——征召伤病造成临时人手缺口，部分俱乐部小额举债周转，故举债家数上升而债务总额不变。`negativeClubs` 实测 2 家，远低于 15% 红线。
- 已验证：`npm test`（全量语法检查 + 75 项检查：72 项审计与 3 组单元测试）、`node scripts/cache-audit.mjs`、两个新审计，以及基线 2 种子 / 改动后 3 种子的五赛季生态样本对比
- 缓存 `vcfm-v236`

## v235 同队姓名多样性与旧档修复（2026-08-26）

- 主要联赛姓名池扩充到足以覆盖一线队与青训，减少同队重复使用少数英文名/姓；姓名组件只影响展示，不参与能力、转会或比赛结算。
- 新建球员、青训补充和赛季末阵容补位共享同队已用姓名集合，优先避开完整姓名、名和姓的可避免碰撞，不额外消费比赛随机流。
- 旧档加载时按稳定球员 id 修复同队重复姓名；已存在的虚构姓名替换可反查原始姓名池，正常球员不被无故改名。
- 新增 `scripts/player-names-audit.mjs`，覆盖 270 家俱乐部、青训姓名多样性与旧档重复修复。
- 缓存 `vcfm-v235`

## v234 AI 阵容出口与管理因果提示（2026-08-26）

- AI 转会窗口吞吐量按夏/冬窗提高，继续服从位置需求、预算、财政禁令与最低阵容骨架；转会、租借和解约统一产生结构化 `ai_squad_moves` 推进事件。
- 真实冗余在没有合适买家、且俱乐部能够承担剩余合同补偿时主动解约进入自由市场；不删除球员，不修改能力、成功率或赛果。
- 经理工作台读取多年阵容规划的同一份位置过剩、轮换顺位和处理理由，为玩家提供可见的出售/解约建议，但不替玩家执行。
- 生态审计逐季记录 AI 转会、租借和解约数量；固定种子五季样本验证人数、经常性赤字、债务与最低位置骨架保持可解释。
- 缓存 `vcfm-v234`

## v233 持久化球衣号码偏好与赛季登记（2026-08-23）

- 球员新增稳定、持久化的钟情号码列表与偏好强度；偏好按球员身份和详细位置一次性生成，转会后继续保留，不影响能力、成功率或赛果。
- 新增统一的重要号码候选评价：号码传统适配读取详细位置，7/8/9/10/11 同时读取公开的首发、核心主力与绝对核心事实；同分按稳定球员 id 处理，不消费随机数。
- 俱乐部按赛季持久化号码登记。上季仍在队的合法持有人优先保留；号码空缺时，强烈钟情该号码的球员或绝对核心可在新赛季换号，归队、提拔和新补充球员随后竞争剩余号码。
- 转会、自由签约和租借继续在新球队清空旧号后登记；青训统一使用 40–99，手动及赛季末自动提拔均清空学院号后进入一线队登记；旧档加载会补齐偏好并修复重复号。
- 球员资料公开钟情号码；新增手动换号 API、专项 `scripts/squad-numbers-audit.mjs` 与存档结构校验。
- 缓存 `vcfm-v233`

## v232 长期经济与比赛画面修复（2026-08-21）

- 赞助市场按签约赛季重新定价（`js/sponsorships.js`，基准 2026 年、每季 3%，对齐实测球员工资通胀）。只作用于新签合同，已生效合同保留签约周基数，老档在换约时跟上；`clubDebtCapacity` 同步跟随收入。
- **收入端此前完全没有通胀**：赞助、比赛日、赛事奖金三处都是名义恒定，而工资与身价随球员能力逐年走高，导致经常性收支持续恶化。修复后 5 赛季样本中位经常性净额由 -22 万转为 +32 万，亏损俱乐部 146→116 家，债务余额 2653 万→1790 万。
- **`negativeClubs` 不能反映生态健康**：现金转负会触发老板注资（`js/club-finance.js` 的 owner loan，3.5%/年计息），持续把该指标重置回低位，5 赛季实测它与真实亏损俱乐部数相差 29 倍。生态审计已补 `recurringDeficitClubs`、`medianRecurringNet`、`indebtedClubs`、`debtOutstanding`、`annualDebtInterest`，均为账本纯读，不触发结算副作用。
- **仍未解决——阵容只进不出**：5 赛季 5719→6606（每队 21.2→24.5 人）。增速在快速衰减（+464/+291/+99/+33），说明它收敛到约 24.6 人/队的均衡而非无限膨胀，`players <= 270 * 25` 红线大概率**不会**如先前预计那样在第 6–7 季触发。真正的问题是均衡值偏大：同期单人工资中位仅 +7.5%，阵容却 +15.5%，工资总额 +24.2%，压过赞助通胀带来的 +12.6% 收入增幅——**这是经常性赤字的主因，而非「另一半」**。根因是出口缺失：全仓库没有任何一处因超编放人，`squad-planning.js:279` 算出的 `surplus` 只用于降低购买欲望（`:289`），唯一的软刹车是 `:726` 的魔数（拐点 23、斜率 4），它把均衡顶在 24–25 人。
- **生态审计此前不可复现**：职员 id 掺了 `Date.now()`（`js/staff.js` 的 `uid`），而教练流派（`js/manager-ecosystem.js` 的 `ensureCoachIdentity`，seed 取 `coach.id`）与职员国籍都拿它当稳定种子 hash，于是同一份代码换个时刻跑就得到不同的联赛——实测两次运行债务余额相差 46%（2365 万 / 1616 万）。按仓库既有惯例（`phase-shape-evidence-audit`、`background-spatial-worker-audit` 都这么做）在 `scripts/ecosystem-audit.mjs` 钉死 `Date.now`，五赛季样本现可逐字段复现。**生产代码未动**：时间戳只影响实体首次生成，而首次生成本就该随机（`ensureStaffProfile` 会把结果连同 `profileVersion` 落档，`ensureCoachIdentity` 保留 `existing.archetype`），这是审计基础设施问题，不是游戏 bug。
- 比赛底栏溢出球场区域会盖住解说折叠按钮（底栏 `position:relative`，解说栏 static，故画在其上）。`.fm-pitch-col` 的高度上限改为同时受视口和父容器约束；替补席空的一侧保留网格列，避免五列结构错位。
- `finishMatchUI` 不再在解锁「继续」按钮之前渲染工作台：该处抛错会同时跳过 UI 解锁与调用方的 `saveGame`，导致卡死且赛果丢失。
- 首周引导版本迁移保留玩家已跳过的选择与已完成步骤；页签步骤由 `MANAGER_ONBOARDING_TAB_STEPS` 从步骤表派生，不再手工抄一份。
- 信箱实体解析使用单次渲染共享索引；国家查询改读 `NATIONALITIES` 静态数据，此前每取一个国家名都会重建全部国家队名单（分组全部球员并逐国选首发）。
- 缓存 `vcfm-v232`
- 已验证：`npm test`（71 项审计全通过）、`npm run test:browser`、财务专项 7 项、5 赛季生态样本

## v230 2D 无球跑位连续性（2026-08-19）

- 无球目标新增约 1.5 秒路线租约：同一持球人和球队阶段内，球员尚未完成原路线时不会因相邻决策反复立即反向；传球换人、阶段切换和明确首次二过一仍可立即建立新路线。
- 接应目标继续以既有战术几何为基础；只有与同队、同阶段、同持球人的已预留目标相距不足 1.8 米时才确定性横向分层，不修改能力、传射成功率、xG 或赛果。
- 空间快照和运动片段公开每名球员的目标坐标、来源、路线类型、租约、持球人和阶段；诊断回放双轨球场绘制实际跑位目标线。
- 运动完整性诊断新增“跑位目标反复”和“接应目标拥挤”，只在同一阶段持续达到几何阈值时报警，并区分传球/阶段边界；新增 `js/off-ball-movement.js` 与 `scripts/off-ball-movement-audit.mjs`。
- 已验证：标准与后台档各 24 场真实性审计；标准档 3.17 球/场、24.21 次射门、81.7% 传球成功率，后台档 2.71 球/场、26.50 次射门、81.0% 传球成功率，两档均零停滞；强队分别 2.08 与 1.92 分/场。6 场后台与 2 场标准运动审计零严重异常，后台目标反复 3 次、接应拥挤 9 次，标准分别 1 次与 6 次，均在发布门槛内。
- 缓存 `vcfm-v230`

## v229 2D 比赛运动完整性（2026-08-19）

- 新增 `js/match-motion-integrity.js`，直播期间检查非法坐标、球员/球瞬移、异常加速度与反向、持续重叠、持球脱节和引擎/渲染坐标偏差；只读观察不消费随机数，以有界内存保留最近 12 秒双轨运动帧
- 比赛底栏新增保存运动片段入口；诊断覆盖层并排回放引擎坐标与实际画面坐标，支持播放、逐帧、跳转自动标记及导出含比赛种子、阈值和镜头上下文的 JSON
- 门将接高速来球改为连续控制与减速收球，画面在第一脚控制完成前不再提前显示持球或吸球；扑救结算不再额外平移门将坐标，抱稳时球绑定门将实际位置
- 身体非穿透投影按时间步限制每名球员的累计纠正距离，深度重叠改为连续数帧解开；空间快照公开真实碰撞接触，诊断只豁免有接触证据的急减速和反向
- 新增 `scripts/match-motion-integrity-audit.mjs`，固定种子完整比赛覆盖 2 场标准档与 6 场后台档；标准档零严重异常、零警告，后台档零严重异常、4 次粗步长快速反向警告，同种子观察开关不改变比分、射门、传球或事件
- 缓存 `vcfm-v229`
- 已验证：`node scripts/verify.mjs`、`node scripts/match-motion-integrity-audit.mjs`、`node js/sim/_p5_integration.mjs`、标准与后台档各 24 场真实性审计、`npm run test:browser`、`npm run test:save-browser`、Edge 6 核 134 场浏览器基准及 `git diff --check`；标准档 3.21 球/场、24.83 次射门、81.1% 传球成功率，后台档 2.92 球/场、24.63 次射门、81.0% 传球成功率，两档均零停滞；浏览器基准 69.1 秒并完整上报 134/134

## v228 阶段阵型证据链（2026-08-18）

- 空间引擎以 1 秒分辨率累计双方持球、无球、进攻转换和防守转换的真实用时、实际阵型及球员平均站位；不调用额外随机数，不修改能力、成功率或赛果
- 战报持久化赛前、中场及 60/75 分钟阶段阵型方案、来源、比分与公开原因；同步、拆分 Worker 和历史战报读取同一事实，后台档省略球员位置明细
- 赛后战术分析新增“阵型”标签，比较双方阶段占比、真实平均站位与调整时间线；桌面和移动端使用同一报告数据
- 新增 `scripts/phase-shape-evidence-audit.mjs`，扫描完整赛季 4,590 场方案并运行同种子配对；高应变方案平均移动 9.6、最大 13.8，配对样本每场进球相同、射门差 2、传球差 2.5
- 审计世界同时固定随机流、模型 ID 计数器和职员时间戳；缓存 `vcfm-v228`
- 已验证：`node scripts/verify.mjs`、`node js/sim/_p5_integration.mjs`、标准与后台档各 8 场真实性审计、`node scripts/background-spatial-worker-audit.mjs`、`npm run test:browser`、`npm run test:save-browser` 与 `git diff --check`；标准档 2.63 球/场、81.8% 传球成功率，后台档 2.75 球/场、82.8% 传球成功率，两档均零停滞

## v227 AI 持球/无球阶段阵型（2026-08-18）

- 主教练理念新增稳定、持久化的持球与无球阵型偏好；职员资料公开展示偏好，旧档按既有理念稳定补齐
- AI 赛前计划读取基础阵型、当前首发位置适配、阵型移动距离、对手锋线人数、宽度与实力差；相同公开事实确定性地产生相同阶段方案。只有应变 5/5 的主教练会自动采用分离结构，上半场持球结构默认跟随基础阵型，只允许接近基础阵型的无球块，避免整队瞬移
- 中场和 60/75 分钟复核按真实比分与比赛阶段启用阶段阵型；AI、完全委托与俱乐部经营模式共用同一逻辑
- 阶段阵型只写入 `tactics.possessionFormation` / `tactics.outOfPossessionFormation` 并改变空间站位，不修改基础阵型、首发、能力、动作成功率或赛果
- 俱乐部资料展示实际基础、持球与无球阵型；新增 `scripts/manager-phase-shapes-audit.mjs`，缓存 `vcfm-v227`
- 已验证：`node scripts/verify.mjs`、`node js/sim/_p5_integration.mjs`、标准与后台档各 8 场真实性审计、`npm run test:browser`、`npm run test:save-browser` 与 `git diff --check`；标准档 2.63 球/场、81.8% 传球成功率，后台档 2.75 球/场、82.8% 传球成功率，两档均零停滞

## v226 可编辑持球/无球双阵型（2026-08-18）

- 战术板新增持球阵型与无球阵型选择器，支持 8 种既有阵型；留空表示跟随基础阵型，旧存档自动迁移且 AI 默认保持原有链接行为
- 选择结果持久化到 `tactics.possessionFormation` / `tactics.outOfPossessionFormation`，战术摘要显示当前两阶段阵型；完整职责委托时控件只读，不绕过主教练权限
- 空间引擎按稳定距离与位置职责把基础首发槽映射到阶段目标槽位，进攻、转换、防守和松散球阶段逐步靠拢对应几何；只改变可解释站位，不写入隐藏能力、胜率或赛果修正
- 新增 `scripts/team-shapes-audit.mjs` 与存档、浏览器断言，覆盖旧档回退、阵型映射、阶段站位、持久化及移动端布局
- 已验证：`node scripts/verify.mjs`、`node js/sim/_p5_integration.mjs`、`npm run test:browser`、标准与后台档各 8 场真实性审计及 `git diff --check`；标准档 2.63 球/场、81.8% 传球成功率，后台档 2.75 球/场、82.8% 传球成功率，两档均零停滞
- 缓存 `vcfm-v226`

## v225 比赛引擎热路径几何分配优化（2026-08-18）

- `pitchDistanceMetres` 与 `pitchOffsetToward` 直接在标尺坐标上完成米制换算和归一化，移除高频临时向量对象分配，不改变空间规则、随机调用顺序或事件数据
- 曾验证的 8 米空间候选网格方案因 Edge 端到端基准退化至 84.8 秒而撤回；v225 仅保留无状态算术优化，避免用性能倒退换取额外索引维护
- 固定种子标准档 8 场与后台档 8 场统计保持 v224 对照，均零停滞；Edge 6 核 134 场浏览器基准 56.4 秒（v224 为 75.4 秒），进度事件完整到 134/134
- 缓存 `vcfm-v225`
- 已验证：`node scripts/match-realism-audit.mjs 8 standard equal-only`、`node scripts/match-realism-audit.mjs 8 background`、`node scripts/background-spatial-worker-audit.mjs`、`node js/sim/_p5_integration.mjs`、`npm run test:browser`、Edge 6 核 134 场浏览器基准及 `git diff --check`

## v224 后台关键事件自适应积分与档位一致性（2026-08-18）

- 后台空间档继续以 `0.3s` 推进普通全队决策和跑位；射门飞行、可能接触球员或边界的传球/自由球改用 `0.1s` 局部球物理与接管步长，不重算全队决策，也不改写赛果
- 受真实持球威胁的门将以 `0.1s` 积分移动，门将出击接触半径按后台采样宽度校正；抢断和门将出击的犯规概率读取实际接触步长，减少粗步长造成的空门、出击和犯规偏差
- 比赛战报持久化自适应积分统计，公开粗步、细步、额外执行步数、关键窗口占比及触发成因；同步、拆分 Worker 和历史战报继续读取同一空间事件与种子
- 24 场后台真实性审计新增固定种子标准档对照，约束进球、射门、传球、犯规、空门、门将事件与强弱区分；关键球路窗口占 15.5%，额外接管步数占 31.0%，两档均零停滞
- 缓存 `vcfm-v224`
- 已验证：`node scripts/verify.mjs`、标准与后台档各 24 场 `match-realism-audit`、`node js/sim/_p5_integration.mjs`、`node scripts/background-spatial-worker-audit.mjs`、`npm run test:browser`、Edge 6 核 134 场浏览器基准 75.4 秒及 `git diff --check`；标准档 2.63 球/场、81.3% 传球成功率、26.5 次犯规，后台档 3.04 球/场、81.5% 传球成功率、23.29 次犯规，两档差值分别为 +0.41 球、+0.2 个百分点和 -3.21 次犯规

## v223 耐久存档、可见推进与长期生态修复（2026-08-17）

- 浏览器三槽存档迁移到 IndexedDB，所有并发读取共享同一个初始化任务；旧压缩 localStorage 槽在耐久写入确认后清理，连续自动保存按槽合并并保证读取最新快照，页面退出不再同步压缩数十 MB 世界
- 保存序列化省略可重建的阵容计划，把出场历史压缩为可迁移元组；AI 只保留真实出场分钟，玩家队继续保留承诺复核所需的可用场次，导出/导入与旧档保持兼容
- 比赛 Worker 池逐场上报进度，日历推进显示已完成/总场次；防守计划按原随机顺序首次计算后在同一步复用，越位线与战术读取减少重复分配，并新增 Edge 浏览器性能基准
- 后台空间档加入时间步长离散补偿，标准档按真实射门量缩短全队射门复核间隔并统一校准门将基础覆盖；24 场发布门禁同时覆盖标准与后台档，直塞由视野、决策、压力、角色和习惯共同决定，VAR 初判允许禁区边界/手球视野误差，最终结论仍只读取精确空间证据
- AI 董事会把长期盈余投入真实球场、训练或青训项目，每赛季最多一次并服从负债、财政限制和现金储备；资金、工期、维护与设施效果继续走统一总账和既有设施系统
- 新增 `js/save-serialization.js`、`scripts/browser-save-audit.mjs`、`scripts/browser-performance-audit.mjs` 与 `scripts/ai-facility-investment-audit.mjs`；缓存 `vcfm-v223`
- 已验证：`node scripts/verify.mjs --full`、`node js/sim/_p5_integration.mjs`、`node scripts/match-seed-audit.mjs`、`npm run test:browser`、`npm run test:save-browser`、Edge 6 核 134 场浏览器基准 59.6 秒及 `git diff --check`；标准档 2.63 球/场、10.9% 转化、24.17 次射门、81.3% 传球成功率，后台档 3.08 球/场、13.2% 转化、23.29 次射门、84.3% 传球成功率，两档均零停滞

## v222 克制信息层、稳定镜头与赛事氛围（2026-08-17）

- 比赛底栏新增全场、电视与战术三种持久化镜头：全场和战术镜头始终固定完整球场，电视镜头按真实球位软跟并只在禁区关键戏有限推近
- 真空间画面移除静态阵型椭圆、控球半场染色与进攻方向大箭头；战术镜头改为逐帧连接双方真实后卫线、中场线和锋线，不把预设阵型冒充当前站位
- 普通跑动不再绘制三层残影，电视镜头只为关键冲刺保留一条短运动线；持球双层光环收敛为单环，方向箭头仅用于持球、焦点和关键压迫
- 新增连续生成式观众底噪，强度读取上座比例、赛事重要性、德比/淘汰赛、真实球位威胁、比分和比赛时间；进球、扑救、判罚和触球只触发有衰减的现场反应，不写回比赛结果
- 新增 `js/match-broadcast.js` 与 `scripts/match-broadcast-audit.mjs`；缓存 `vcfm-v222`
- 已验证：`node scripts/verify.mjs`、`node js/sim/_p5_integration.mjs`、`npm run test:browser`、`node scripts/match-seed-audit.mjs` 与 `git diff --check`

## v221 空间手球与证据驱动 VAR（2026-08-17）

- 手球只在球真实进入外场球员控制半径且处于 0.85–2.20 米上半身高度时评估；风险读取来球速度、身体朝向暴露、传中/射门、是否预期接球、球员判断和禁区收手事实，不按比分或球队身份加权
- 地面球、超过可接触高度的高球和门将合法用手不会触发手球；成立时同时记录 `handball` 成因与统一 `foul` 纪律事实，禁区外判直接任意球，防守方禁区内进入点球流程
- 进球和点球统一生成 `var_review` 与 `var_decision`，复核门线穿越、门框范围、横梁高度、越位快照、禁区坐标和犯规类型；证据冲突才推翻原判，正常精确空间判罚不会随机制造改判
- VAR 结果会真实改变比分或重启类型；直播风味事件显示手球成因、点球手球和 VAR 确认/推翻，空间快照保留最近一次复核编号、事件、结论与原因
- `scripts/edge-rules-audit.mjs` 扩展覆盖地面球排除、上半身手球、禁区点球、门线确认、越位推翻和实际重启；真实性审计新增手球、VAR 复核与随机推翻发布门槛
- 缓存 `vcfm-v221`
- 已验证：24 场标准档 3.00 球/场、13.7% 射门转化、21.88 次射门、1075.3 次传球、81.3% 成功率、0.33 点球/场、0.46 次手球/场、3.33 次 VAR/场、零随机推翻与零停滞；强队 2.04 分/场且总比分 42–22

## v220 有利原则、回传违例与间接任意球（2026-08-17）

- 禁区外犯规会按持球方真实前进目标、距球门距离、对手压力和边线空间判断有利；裁判给出 2.2 秒观察窗，形成推进、传球或射门则继续，立即丢失利益则回到原犯规点重罚
- 有利判罚、继续进攻和最终犯规共用同一事件流；犯规、牌面和纪律事实不因裁判延迟鸣哨而丢失，空间快照公开当前有利状态
- 受压后卫在本方组织区可以真实回做门将；有意脚下回传会随该脚传球记录候选事实，门将通常用脚控制，只有在本方禁区误用手接球时才构成违例；传中、直塞、头球与对手最后触球不被误判
- 回传违例判给对手间接任意球；间接任意球接入既有死球站位、保护窗和越位规则，首脚强制传给队友，不能直接射门
- 新增 `js/edge-rules.js` 与 `scripts/edge-rules-audit.mjs`，覆盖有利成立/召回、回传识别、门将接球、间接重启、首脚限制、事件与快照事实
- 缓存 `vcfm-v220`
- 已验证：24 场标准档 2.67 球/场、12.2% 射门转化、21.92 次射门、1083.0 次传球、81.5% 成功率、0.42 点球/场、零停滞；强弱对照强队 1.67 分/场且总比分 45–34

## v219 集体防守交接、压迫触发与分层回收（2026-08-17）

- 防守计划新增公开、可审计的压迫触发：丢球反抢、回收、第一脚失误、边线陷阱、背身接球、深位威胁和中位逼近；触发只改变上抢角度、紧迫度与任务，不改单次抢断或拦截成功率
- 既有唯一上抢者保留保护阴影；下一名防守者屏蔽最危险接球点，稳定 screen 任务按判断属性和空间差异迟滞交接，明显优势出现时才换人，避免全队同时扑向持球者
- 增加 mark、wide-cover、recover 任务：边路真正进入边线区域才触发肋部保护，弱侧只做小幅收缩；高压或丢球转换期才额外盯危险跑者，普通落位继续使用原防守块，避免重复封堵射门线
- 反抢小组由压迫、屏蔽和通道拦截组成，其余球员按回收目标落位；空间快照记录双方触发类型、球侧、任务计数、交接次数和每名球员当前防守任务，直播、后台、发展队共用同一事实
- 防守者现有 marking、positioning、decisions 只影响贴靠距离和区域权重；门将基础扑救常数统一校准为 0.29，仍由轨迹、反应、距离、覆盖、速度和空门事实结算
- 新增 `js/collective-defense.js` 与 `scripts/collective-defense-audit.mjs`，覆盖触发、保护阴影、盯人交接、弱侧收缩、边中协防和分层回收
- 缓存 `vcfm-v219`
- 已验证：24 场标准档 2.50 球/场、10.6% 射门转化、23.58 次射门、1080.9 次传球、81.6% 成功率、0.33 点球/场、零停滞；强队 2.29 分/场且总比分 48–28

## v218 连续触球、身体朝向与护球动量（2026-08-17）

- 空间引擎新增连续的第一脚控制阶段：接球先按来球速度、压力、球员现有第一脚/灵活/平衡属性和惯用脚决定停球方向、控制脚、停球时长与可行动时刻；不再在同一模拟步把飞行球瞬间变成完全静止的持球
- 持球者保留真实身体朝向和控制脚，转身按灵活与平衡产生时间成本；加速读取现有速度、灵活和平衡属性，护球时按防守者相对动量与身体遮挡调整抢断抗性，不提高传球、射门或抢断成功率
- 普通接球、抢断、拦截、换人和门将出击都经过同一控制状态；准备传球/射门会等待控制阶段结束并记录动作延迟，失误释放为带有控制方向和速度的真实二点球
- 直播、后台、发展队和赛后快照共用同一套控制脚、身体目标朝向、控制阶段与动作准备字段；新增 `js/player-control.js` 与 `scripts/player-control-audit.mjs`
- 收紧禁区接触犯规与门将出击犯规的公开物理判定，避免连续触球增加重复接触后点球泛滥；没有向球队或界面写入隐藏胜率修正
- 缓存 `vcfm-v218`
- 已验证：24 场标准档 2.50 球/场、11.2% 射门转化、1064.3 次传球、79.9% 成功率、0.33 点球/场、零停滞；强队 1.71 分/场且总比分 40–26

## v217 持球、无球与攻防转换球队形态（2026-08-17）

- 空间引擎新增可审计的球队阶段状态：得球后的进攻转换、稳定持球、丢球后的防守转换与稳定无球防守；直播、后台与发展队继续运行同一逻辑
- 三类形态只读取现有公开战术事实：阵型、风格、压迫、节奏、宽度、防线、角色与职责；只改变站位目标、支援距离、反抢窗口和防守块收缩，不增加能力、成功率、xG 或比分权重
- 反击风格在得球转换期更快攻击纵深，控球风格在稳定持球后缩短接应距离；高压球队丢球后维持一名真实上抢者并增加传球线路拦截，随后回落为稳定防守块
- 防守转换继续使用空间引擎的连续加速度移动，并按公开压迫与风格决定反抢时间窗；稳定无球阶段按现有宽度、压迫、防线和球位形成横向结构，中性设置保持既有球侧收缩基线
- 空间快照记录双方真实 `teamPhases`，战术页展示由同一战术事实派生的持球、无球与转换摘要；新增 `js/team-shapes.js` 与 `scripts/team-shapes-audit.mjs`
- 缓存 `vcfm-v217`
- 已验证：24 场标准档 2.79 球/场、11.4% 转化、78.1% 传球成功率、0.46 点球/场、零停滞；强队 2.04 分/场且总比分 62–33

## 当前节点（2026-08-17）

- `master` 位于 `eee742f`（`fix: improve goal replay and spatial defending`），已推送并与 `origin/master` 一致；记录前工作区干净
- v216 已完成真实空间帧进球回放、回放统计隔离、米制传球/防守/门将接触距离、分层协防与会话内比赛 Worker 复用；当前缓存仍为 `vcfm-v216`
- 发布验证已通过 `node scripts/verify.mjs`、`npm run test:browser`、`node js/sim/_p5_integration.mjs`、相关专项审计与 `git diff --check`
- 最终 24 场标准档为 2.79 球/场、11.4% 转化、78.1% 传球成功率、0.46 点球/场、零停滞；24 场后台档为 3.13 球/场、14.2% 转化、82.1% 传球成功率、0.46 点球/场、零停滞
- 额外强弱对照中强队 1.75 分/场且总比分 41–26；8 场等强小样本曾因随机零点球触发审计下限，不据此反向调参，发布口径以已通过的 24 场样本为准
- 桌面已保存 `C:\Users\WXX\Desktop\VCFM与FM26的2D比赛画面差距报告.txt`
- 下一阶段建议但尚未实施：持球/无球/转换三套球队形态、身体朝向与惯用脚驱动的连续触球、集体压迫和盯人交接、有利原则/回传/间接任意球等规则，最后再精简 2D 残影、光环和阵型色块

## v216 真实进球回放、米制防守与会话 Worker（2026-08-15）

- 进球回放优先读取空间引擎已记录的真实帧，保留实际传球、跑位、射门与入网轨迹；无帧旧战报只使用稳定回退，且没有 `assistId` 时不再推断或演示虚构助攻
- 回放以只读表现事务运行，结束后恢复原比赛状态、球权与画面；回放中的传球和触球不再写入传球网络或热区统计
- 控球、抢断、传球拦截、门将出击、上抢排序、贴身距离和协防安全圈统一按 68×105 米场地换算，横向、纵向、斜向与边线附近使用相同实际距离
- 连续二点球只有真正近门机会可绕过球队射门节奏；门将出击失败后的犯规风险继续由处理、站位和进攻者控球共同结算，不按球队或比分加权
- 比赛 Worker 在同一日历会话内复用，失败任务仍回退到同一空间引擎并按原赛程提交；性能基准分别报告日历推进、JSON 序列化、压缩和总墙钟耗时
- 扩展 `scripts/replay-ui-audit.mjs`、`scripts/defensive-spacing-audit.mjs`、`scripts/background-spatial-worker-audit.mjs` 与浏览器 E2E；缓存 `vcfm-v216`
- 已验证：24 场标准档 2.79 球/场、11.4% 转化、78.1% 传球成功率、0.46 点球/场、零停滞；24 场后台档 3.13 球/场、14.2% 转化、82.1% 传球成功率、0.46 点球/场、零停滞

## v215 进球自动回放与分层防守（2026-08-14）

- 普通空间比赛事件路径在真实入网画面后自动进入带明确标识的进球回放，不再只有连续高光时间轴能够触发自动回放
- 空间回放期间临时释放 `SIM_DRIVEN`，结束后恢复原空间驱动状态、球权与界面锁；赛后和暂停回放继续恢复各自原状态
- 防守计划保持只有一名球员贴身上抢；次近者盯危险接球点，标准压迫只派一名通道拦截者，极高压迫的两名拦截者分居持球者前方两侧
- `screen`、`intercept` 与阵型补位目标保持支援距离，不再与上抢者同时扑向持球人同一点；禁区内仍允许更紧凑的真实协防
- 新增 `scripts/defensive-spacing-audit.mjs` 并扩展回放与浏览器 E2E，覆盖拥挤复现、拦截通道、回放可见 Canvas 及 `SIM_DRIVEN` 恢复；缓存 `vcfm-v215`
- 已验证：24 场标准档 2.79 球/场、10.7% 转化、77.0% 传球成功率、零停滞；24 场后台档 3.08 球/场、11.6% 转化、82.0% 传球成功率、零停滞

## v214 传球速度与球物理一致性（2026-08-14）

- 传球距离与初速统一按 68×105 米场地换算，相同实际距离的横传、纵传和斜传不再因 0..100 坐标比例产生方向性速度差
- 5–40 米普通传球按距离使用约 12.2–25.2m/s 初速，到达时间由同一套摩擦、飞行与落地积分计算；门将大脚复用相同米制速度与接应时间
- 地滚球不再每个模拟步重复触发落地减速；空中球按精确触地时刻耗散反弹，避免低高度固定循环和永久处于传球状态
- 后台 0.3s 档内部以 0.1s 子步推进传球物理，使直播与无画面比赛的同一脚球保持相同距离、速度和高度
- 新增 `scripts/pass-speed-audit.mjs` 并接入统一验证，覆盖方向一致性、初速/到脚速度、预计到达、门将出球、地滚球与弹跳收敛；缓存 `vcfm-v214`
- 已验证：24 场标准档 2.88 球/场、12.0% 转化、76.0% 传球成功率、零停滞；24 场后台档 3.29 球/场、12.7% 转化、83.4% 传球成功率、零停滞

## v213 传球轨迹连续性（2026-08-14）

- 直播空间帧在出脚与接球过渡期间保持球为飞行态，直到真实接球帧才绑定持球人，避免画面提前把球吸回球员脚下
- 持球、传球和射门阶段切换时重置轨迹尾迹，不再把前一段带球、当前传球和接球后的动作连成多段假折线
- 真实折射、拦截、停球失误与比赛结果保持不变；新增无遮挡传球共线性和出脚/接球插值审计
- 浏览器 E2E 使用真实 `MatchView` 与 Canvas 播放确定性直传，覆盖共线采样、球权边界、尾迹清理和非空像素
- 缓存 `vcfm-v213`

## v212 位置角色属性原型（2026-08-13）

- 球员不再逐项独立随机属性；按门将、中卫、边后卫、防守中场、组织核心、全能中场、边路中场、边锋、内锋、突前、支点、压迫前锋和伪九号等公开原型分配 1–20 能力
- 总体能力与既有世界稀缺标尺保持一致，原型只重分配关键、辅助和弱项属性，不向比赛写入隐藏胜率或角色加成
- 旧档按球员既有细分位置与属性专长选择最近原型，一次性温和重塑并保留 OVR、潜力、年龄、历史和训练形成的个体差异
- 青训、赛季成长和一线队训练按原型关键属性加权但仍可改善弱项；个人习惯优先匹配属性原型，继续只改变动作选择而不提高成功率
- 新增 `js/player-attributes.js` 与 `scripts/player-attributes-audit.mjs`；缓存 `vcfm-v212`
- 已验证：`node scripts/verify.mjs`、8 场标准档真实性样本、`node js/sim/_p5_integration.mjs`、`node scripts/background-spatial-worker-audit.mjs`、`npm run test:browser` 与 `git diff --check`；标准档 2.13 球/场、8.5% 转化、75.3% 传球成功率、零停滞，强队 1.69 分/场且总比分 33–21

## v211 比赛日并行空间模拟（2026-08-13）

- 日历 Worker 将同一天互不冲突的 AI 比赛拆成确定性任务，由比赛 Worker 池并行运行空间比赛时钟，再按原赛程顺序提交积分、球员、财政与赛事结算
- 无画面空间档使用 `0.3s` 步长与 4 轮非穿透投影，仍逐步运行同一球员决策、球物理和裁判规则；不改用概率赛果
- 准备、模拟与提交共享同一比赛种子和空间引擎；同步路径与拆分路径的赛果、球员状态、积分榜及财政义务保持严格一致
- 后台 AI 历史战报保留比分、关键事件、射门/xG、推进、压迫与阵型事实，省略用户界面不会读取的完整热区、传球网络和全员评分，减少存档克隆与回传体积
- 日历 Worker 改为会话内复用；新增比赛 Worker 与 Worker 池离线缓存登记，失败时仍回退到相同空间因果的本地执行
- 缓存 `vcfm-v211`

## v210 经理工作台与日期推进反馈（2026-08-12）

- 概览页新增经理工作台，把紧急信箱、董事会压力、现金续航、比赛可用性、赛事报名、短约与更衣室风险统一按紧急程度排序，不额外持久化重复待办状态
- 工作台提供上下文快捷入口，统一复用既有页签导航；下一场比赛继续读取联赛、杯赛与洲际赛事的共同选择器
- 日期推进前后比较球员伤病、停赛、平均体能、平均士气、现金与信箱待办，并结合真实推进事件生成最近变化摘要，明确展示变化及影响
- 新增 `js/ui/manager-workbench.js`；浏览器 E2E 覆盖工作台内容、快捷入口、推进摘要与手机端横向溢出
- 缓存 `vcfm-v210`

## v209 角色、职责与防守侧空间行为(2026-08-12)

- 战术板新增角色职责面板：球员脚下显示"角色·职责"标签，点击打开右侧角色面板，查看适配度、描述、可切换职责和习惯契合/冲突提示
- 新增 `js/player-roles.js`：声明 24 种角色、3 种职责（defend/support/attack）及角色适用细分位置、属性侧重、习惯契合/冲突、行为倾向和教练标签；导出 `roleBehavior`、`roleSuitability`、`roleFitsPosition`、`rolesForDetailedPosition` 等工具
- 空间引擎防守侧接入角色行为：抢球中场/压迫型前锋的高压迫倾向影响上抢者指派排序、拦截距离和站位贴进度；下脚倾向影响抢断范围和冷却；角色职责深度影响防线回收位置，量级克制，默认角色几乎不受影响
- 中场角色/职责变更同步触发 `state._simNeedsResync`，确保下半场空间 agent 使用新角色/职责
- `resyncSimAfterHalfTime` 补齐 `getSlotDuty` 与习惯同步
- `save-schema.js` 新增 `tactics.roles[]`/`duties[]`/`coachRoleIdentityId`/`coachRoleIdentityVersion` 结构校验
- 新增 `scripts/player-roles-audit.mjs` 并接入统一验证，覆盖角色细分位置合法性、职责合法性、教练理念差异、习惯冲突提示、防守因果
- 浏览器 E2E 覆盖战术角色面板交互；三后卫 DEF 槽统一识别为 CB，五后卫只有最外侧槽为 LB/RB
- 缓存 `vcfm-v209`
- 已验证：`node scripts/verify.mjs`、标准档 24 场真实性审计（2.50 球/场、10.1% 转化、75.6% 传球成功率、0.29 点球/场、零停滞）、`node js/sim/_p5_integration.mjs`、`git diff --check`

## v208 球员个人踢球习惯（2026-08-12）

- 球员新增 14 种稳定、持久化的个人踢球习惯，覆盖直塞、二过一、转移、回撤、前插、边路宽度、内切、带球、远射、巧射、盘过门将、积极下脚与两类门将出球偏好
- 习惯只改变空间引擎已有动作的选择倾向、跑位目标和风险偏好，不提高传射抢断成功率，也不写入球队胜率；首发、换人与发展队比赛读取同一份球员档案
- 旧球员按 ID、细分位置和真实属性稳定派生初始习惯；互斥习惯与位置资格统一校验，稳定态不会反复把存档标脏
- 球员资料支持培养或纠正一项个人习惯，按现有教练能力、训练强度、年龄、伤病和决策属性逐周结算；培养委托给教练团队时玩家入口只读
- 外队习惯随持久化球探观察逐步揭示，旧报告不会因球员后来改变习惯而自动刷新；存档结构同步校验习惯、训练进度和已观察事实
- 新增 `js/player-habits.js` 与 `scripts/player-habits-audit.mjs`；缓存 `vcfm-v209`
- 已验证：`node scripts/verify.mjs`、标准与 background 档各 24 场真实性样本、`node js/sim/_p5_integration.mjs`、`npm run test:browser` 与 `git diff --check`；标准档 2.63 球/场、10.3% 转化、76.1% 传球成功率、零停滞，后台档 3.04 球/场、11.3% 转化、80.6% 传球成功率、零停滞

## v207 比赛传球节奏与真实性基准（2026-08-11）

- 接球后的首次处理与连续持球决策读取比赛节奏和球员决策属性，减少空间引擎中过快、过密的无意义传球，不通过控球率或比分回写结果
- 明确目标的普通传球按传球速度、接球队员盘带与决策属性结算停球；传中、高球、线路拦截和压迫风险继续走原有空间因果
- 真实性审计新增完成传球、传球成功率、传中与真实非传中直塞统计，并把传球量、成功率和传中占比纳入发布门槛
- 门将统一基础扑救常数按完整样本小幅校准，仍由射门轨迹、反应、处理、距离、覆盖和落点共同结算，不增加球队或界面隐藏修正
- 缓存 `vcfm-v207`
- 已验证：`node scripts/verify.mjs`、24 场标准档真实性样本、8 场 background 样本、400 场五联赛强弱平衡、`node js/sim/_p5_integration.mjs`、`npm run test:browser` 与 `git diff --check`；标准档 2.63 球/场、10.6% 射门转化、1144.5 次传球、75.5% 成功率、7.5% 传中、41.1% 禁区外射门、零停滞

## v206 发展队比赛与统一比赛锐度（2026-08-10）

- 青训球员与一线队边缘球员组成真实发展队首发，按固定种子直接运行同一个空间比赛引擎；比分、射手、助攻、伤病、体能和评分都来自空间事件
- 发展队比赛不写入联赛积分或正式出场统计，但会把逐场可用状态、首发、真实分钟和伤退写入球员现有出场事实；正式 AI 比赛也同步为双方完整记录这份事实
- 比赛锐度读取最近 28 天所有真实正式/发展队分钟；青训成长、AI 提拔、保留培养和外租优先级共用这份公开事实，发展队分钟不能兑现一线队出场承诺
- 用户队每 28 天固定参加发展队比赛，世界其他同级对阵按稳定轮转抽样运行；赛季末按球员归档发展队出场、分钟、进球、助攻和平均评分
- 新增 `js/development-football.js` 与 `scripts/development-football-audit.mjs`；缓存 `vcfm-v206`

## v205 AI 主教练生态与战术身份（2026-08-08）

- 主教练拥有稳定持久化的足球理念：偏好阵型、风格、压迫、节奏、宽度、防线、青训信任、轮换与应变全部来自同一份公开档案
- AI 球队和受委托球队每场先读取主教练基础理念，再按实力差、近五场与对手风格有限应变；理念只写入现有战术指令，不增加隐藏能力或胜率
- AI 董事会每 14 天按赛季目标、真实积分榜、近况、净胜球与降级区状态复核；连续严重失败才解雇，用户比赛未结算时不读取残缺积分榜
- 解雇补偿进入统一总账，原主帅回到共享市场，看守教练临时接管；招聘读取能力、俱乐部计划、阵容适配、平台、合同与现金
- 任命、离任原因、最终排名、目标与看守期进入俱乐部和职员履历；资料页展示实际使用的理念与帅位状态
- 新增 `js/manager-ecosystem.js` 与 `scripts/manager-ecosystem-audit.mjs`；缓存 `vcfm-v205`
- 已验证：`node scripts/verify.mjs`、24 场标准档真实性样本、`node js/sim/_p5_integration.mjs`、`npm run test:browser` 与 `git diff --check`；标准档 2.63 球/场、9.9% 转化、38.5% 禁区外射门、零停滞，强队 2.00 分/场且总比分 36–16

## v204 禁区决策与门将出击（2026-08-07）

- 空门改为射门决策前即可读取的几何事实：门将是否仍处在射门者与球门之间、横向覆盖、真实可达范围与是否已被过掉共同决定，不再等到出脚后才标记
- 近距离真实空门不受球队射门节奏冷却压制；球员仍按终结、决策、距离与压力决定立即射门或调整一步，射门精度继续允许打偏和封堵
- 修复门将越靠近持球者反而把目标点设到球后方的问题；直塞与持球威胁下的门将目标必须保持在球门侧，射门飞行时按反应属性部分追踪预测落点
- 门将新增独立出击对抗，真实移动到触球距离后按扑救、反应、站位、力量与进攻者控球属性结算收球、身体封堵、扑空或低概率犯规；收球和封堵不冒充射门扑救统计
- 持球者会在门将进入近身封角范围时提前重算射、传、带选择；同一持球阶段限制重复出击，扑空门将保留起身时间
- 新增 `scripts/goalkeeper-open-goal-audit.mjs`，真实性审计新增空门成因与门将出击结果；缓存 `vcfm-v204`
- 已验证：`node scripts/verify.mjs`、24 场标准档等强真实性样本、8 场 background 快速样本、16 场标准档强弱对照、`node js/sim/_p5_integration.mjs`、`npm run test:browser` 与 `git diff --check`；标准档 2.63 球/场、9.9% 转化、38.5% 禁区外射门、0.67 次真实空门、零停滞，强队 2.13 分/场且总比分 30–11

## v203 球员细分位置与多位置适应性（2026-08-06）

- 球员新增 13 个细分位置（GK/LB/CB/RB/DM/CM/LM/RM/AM/LW/RW/CF/ST）的熟悉度档案，由本人真实属性与惯用脚稳定派生；`player.pos` 继续作为赛事与旧档兼容的粗位置字段
- 阵型槽位按左右站位和纵深解析成细分位置，自动阵容、锁定球员安置、半场重同步与换人共用同一份适配度；抢截型中卫不再被塞到边路，左脚边后卫优先落在左路
- 空间比赛的边后卫与边路球员判定改读槽位细分位置，不再只靠横坐标阈值猜测；`detailedPosition` 是槽位属性，换人继承槽位因此保持不变
- 阵容规划新增逐槽覆盖事实与细分位置需求，转会评估、接班人判断与训练重点读取同一份档案；适配度只影响选人与建队决策，不向比赛写入隐藏能力
- 旧档迁移在修正越界熟悉度、剔除非法擅长位置或补齐缺失细分位置时都会回报变更并落盘；稳定态不会反复把存档标脏
- `derivePositionRatings` 对 v199 属性（heading/crossing/decisions）提供方向一致的代理值，外部调用顺序不同时不会塌成平均值；本模块不反向 import `models.js`，避免与 `ensureFootballProfile` 成环
- 新增 `js/player-positions.js` 与 `scripts/player-positions-audit.mjs`；缓存 `vcfm-v203`

## v202 多年阵容规划与 AI 建队（2026-08-06）

- 俱乐部按实际阵型首发位生成一至三年阵容计划，统一记录当前位置深度、下季与两年后已签约人数、同级主力质量、年龄接班、合同断层与报名培养风险
- AI 出售只处理真实过剩且不会破坏位置最低骨架的球员；引援同时读取当前/未来位置需求、候选人能力、潜力、年龄、战术方向、价格与本土培养价值
- AI 合同到期不再按固定总评线续约，青训提拔不再只看潜力；续约、释放、提拔和高潜球员外租全部读取同一份持久化阵容计划
- AI-AI 外租要求母队确有培养对象、接收方确有该位置缺口并能提供现实轮换机会，租借费、工资分摊、球员对象、阵容与总账沿用现有租借因果
- 阵容页新增可解释的多年规划工作台，展示阵型目标、未来合同深度、同级质量差、报名风险和具体人员优先事项；规划不向比赛写入隐藏能力或胜率修正
- 新增 `js/squad-planning.js` 与 `scripts/squad-planning-audit.mjs`；缓存 `vcfm-v202`
- 已验证：`node scripts/verify.mjs`、标准档与 background 档各 24 场 `match-realism-audit`、`node js/sim/_p5_integration.mjs`、`npm run test:browser` 与 `git diff --check`；两档真实性结果保持 v201 发布校准，浏览器端覆盖阵容规划工作台

## v201 后台空间比赛 Worker（2026-08-06）

- 浏览器日历推进改由独立模块 Worker 执行，推进一天、推进到比赛日和推进到赛季末不会再在主线程同步计算整轮 AI 比赛
- AI 后台比赛显式请求与用户比赛相同的空间引擎、阵容、战术、教练、天气、定位球、伤病、纪律、换人、评分和分析因果；直播、快速与纯战报只保留表现差异
- 无画面后台档仍逐步运行同一空间决策、球物理和裁判规则，仅将时间步长降为 0.2 秒并减少非穿透投影轮数；具体 profile、步长与投影轮数随战报持久化，性能取舍可审计
- Worker 在完整克隆世界上完成比赛、积分、球员统计、财政和赛事结算后再原位写回，失败时回退到主线程上的同一空间后台档，不留下半完成日历状态
- 新增 `js/sim/calendar-worker.js`、`js/sim/calendar-worker-client.js` 与 `scripts/background-spatial-worker-audit.mjs`；缓存 `vcfm-v201`
- 已验证：`node scripts/verify.mjs`、标准档与 background 档各 24 场 `match-realism-audit`、`node js/sim/_p5_integration.mjs`、`npm run test:browser` 与 `git diff --check`；标准档 2.83 球/场、11.8% 转化、零停滞，后台档 3.13 球/场、12.7% 转化、零停滞，强弱样本分别 31–15 与 39–28

## v200 职员姓名与国籍因果（2026-08-05）

- 新职员先按俱乐部所在足协、岗位和能力确定本土/外籍身份，再从同一国籍姓名池生成姓名，不再将通用欧美姓名与独立随机国籍拼接
- 旧档保留职员姓名；仅当现有国籍无法由姓名池解释时，按名与姓的文化线索稳定迁移国籍，修复 `Pedro Muller / 中国籍` 等无数据依据的组合
- 临时职员以本土来源为主；普通俱乐部职员保留现实可解释的外籍流动，且姓名、国旗、国家名和头像继续读取同一国籍字段
- 新增 `scripts/staff-identity-audit.mjs`，覆盖 36 个国籍姓名一致性、旧档迁移与俱乐部本土职员占比；缓存 `vcfm-v200`

## v199 球员特征与定位球职责（2026-08-05）

- 球员新增稳定的惯用脚、身高、头球、传中与决策属性；旧档读取时按球员 ID 和既有能力可解释补齐，不重掷已有球员身份
- 战术页新增队长、点球、直接任意球与角球职责；职责必须来自当前首发，自动阵容、换人和开赛前都会校验并在失效时按真实属性补位
- 概率比赛与空间比赛共用同一职责：点球、角球和任意球主罚者读取战术设置，传中质量读取传中/脚法/决策，争顶读取头球、力量与身高
- 球员资料与球探属性展示接入新特征；他队资料继续读取球探知识区间，不暴露真实隐藏值
- 修复当前 schema 存档缺失可修复 `scoutingKnowledge` 时被严格预校验提前拒绝的问题；迁移前预检允许该字段缺失，最终仍执行严格校验
- 修复进攻球员在底线被压迫时持续向边界盘带并触发看门狗解围的问题；现在优先倒三角回做或回撤重组，24 场样本无需强制解围
- 球探能力区间触及 1/20 边界时整体平移并保持可信度对应宽度，后续观察不会因公开估计恰好触边而显示得更宽
- 新增 `scripts/player-traits-set-pieces-audit.mjs`；缓存 `vcfm-v199`
- 已验证：`node scripts/verify.mjs`、`node scripts/match-realism-audit.mjs 24`、`node js/sim/_p5_integration.mjs`、`npm run test:browser` 与 `git diff --check`；24 场等强样本 2.79 球/场、11.7% 射门转化、零停滞、零 30 米外进球，强队对弱队 1.67 分/场且总比分 26–14

## v198 直播因果与定位球表现（2026-08-05）

- 全项目统一模拟秒→比赛分钟换算为 `floor(t/60)+1`；此前进球用 `floor+1`、事件文案用 `round`、播放帧顶栏用 `ceil`，同一时刻会得出三个不同分钟，导致顶栏在 `39′→38′`、`40′→39′` 之间反复并出现"顶栏已到 40′才播 39′点球"
- 顶栏分钟在同一场比赛内只允许单调前进，新比赛与载入历史战报时才显式重置
- 点球改为可见的分阶段死球：法定站位 → 助跑 → 射门飞行 → 结算（判罚后 2.05 秒出脚），除主罚者与门将外双方球员按固定分槽站在禁区和罚球弧外，不再在判罚同一步长内瞬间结算
- 球员非穿透改为带提前退出的 Gauss-Seidel 投影，一次投影补足整段重叠；修正门将与场上球员的权重分配反转（原先门将承担 92.6% 位移，与"门将几乎不动"相反），根治禁区十余人重叠及前锋与门将同坐标
- 定位球文案时长与镜头停顿改读引擎自身的法定间隔，判罚提示会持续到球真正踢出，不再被射门/扑救文案顶掉；倍速同时缩放画面与文案
- 直播数据条按当前画面时刻切片重算，不再从开场就显示整段模拟的最终控球率与射门数（引擎新增控球时间轴采样与 `statsThrough`）
- 空间投影路径不再为角球二次摆拍坐标：真实帧已包含合法角球站位，表现层只保留徽章与镜头，消除高光切换时整队瞬移
- 新增 `js/match-presentation.js`、`scripts/match-presentation-audit.mjs` 与 `scripts/set-piece-presentation-audit.mjs`；缓存 `vcfm-v198`
- 已验证：`node scripts/verify.mjs --full`、`node js/sim/_p5_integration.mjs`、`npm run test:browser` 与 `git diff --check`；24 场真实性样本无 30 米外进球、无无归属进球，射门转化 12.6%

## v197 赛后回放与完整评分（2026-08-05）

- 修复进球回放无法从完场或历史战报暂停状态启动的问题；回放期间临时释放终场冻结与空间投影驱动，播放后恢复原赛后状态
- 一键完赛的进球回放会临时展开并聚焦球场，历史战报不再继承上一场的一键战报折叠布局
- 赛后双方评分展示所有实际出场球员，不再按评分截断为 11 人；替补出场者显示“替补 / SUB”标记
- 扩展状态机、回放界面及出场评分审计；缓存 `vcfm-v197`

## v196 真实临场换人（2026-08-05）

- 修复俱乐部经营模式观赛时双方可能全场不换人的问题；对手和代管用户队现在都在 60′、75′进行同一套临场复核
- 换人依据比分、体能、黄牌风险、位置与替补实力，正常轮换 2–4 人；普通执教模式仍只允许玩家控制自己的球队
- 直播、快速模拟、一键完赛与 AI 后台比赛共用同一决策，换人会立即同步到空间引擎、阵容、事件流和赛后出场记录
- 扩展 `scripts/delegation-audit.mjs`，覆盖经营模式双方换人、直播事件派发及纯 AI 比赛双方换人；缓存 `vcfm-v196`

## v195 自然俱乐部简称（2026-08-04）

- 删除所有俱乐部内部码与队徽文字的国家代码强制前缀；首字母直接表达俱乐部身份，国家和联赛继续由既有字段与界面上下文承载
- 简称统一为 3–4 位：优先保留 `FC`、`CF`、`AC`、`AS`、`SV`、`VfR` 等真实组织字母块和多词首字母，两字母结果使用核心地名三字码
- 自动候选冲突时按组织类型和地名继续区分；机械或含混结果由稳定俱乐部 ID 的显式映射校准，不使用数字、国家前缀或任意字母兜底
- 198 队简称全局唯一，其中 179 个三字码、19 个四字码；旧档加载会同步刷新 `shortName`、`shortCode` 和队徽文字，不改变俱乐部 ID、赛程或历史引用
- 新增 `scripts/club-short-name-audit.mjs`，覆盖唯一性、长度、首字母多样性、代表性自然简称、队徽一致性与旧档品牌迁移；缓存 `vcfm-v195`
- 已验证：`node scripts/verify.mjs --full`、`node js/sim/_p5_integration.mjs`、`npm run test:browser` 与 `git diff --check`；1440×1000 和 390×844 实际截图确认 `SVF`、`FCE` 等新队徽文字清晰且无布局溢出

## v194 程序化俱乐部队徽（2026-08-04）

- 198 家虚构俱乐部按既有品牌形状、图腾、球衣纹样、颜色与内部短码生成唯一、稳定的离线 SVG 队徽，不引入现实俱乐部资产或外部图片
- 开档选队、顶栏、俱乐部链接与资料、全局搜索和比赛计分板统一显示同一枚队徽，球衣与号码仍读取同一品牌颜色
- 队徽通过安全的 SVG data URI 渲染；装饰性实例不重复朗读队名，小尺寸链接延迟解码且保持固定尺寸
- 新增 `scripts/club-crest-audit.mjs`，覆盖 198 枚图形唯一性、确定性、安全标记、关键界面接入与离线缓存；缓存 `vcfm-v194`
- 已验证：`node scripts/verify.mjs --full`、`node js/sim/_p5_integration.mjs`、`npm run test:browser` 与 `git diff --check`；1440×1000 和 390×844 实际截图无页面级横向溢出或元素重叠

## v193 持久化球探知识体系（2026-08-04）

- 球员、俱乐部、联赛与国家知识按观察持久化，旧报告随时间衰减；未观察的成长不会自动刷新旧估计
- 球探任务支持位置、培养潜力/即战力/合同将尽和预算条件，任务候选、被动推荐与市场排序只读取球探估计
- 球员资料、全局搜索、俱乐部与国家队名单、关注列表、转会市场、赛前危险球员和初始报价共用同一能力、潜力及估值区间
- 旧档以公开信息起步；schema v3 校验知识结构，`scripts/scouting-knowledge-audit.mjs` 覆盖持久化、衰减、筛选与隐藏能力泄漏
- 缓存 `vcfm-v193`
- 已验证：`node scripts/verify.mjs --full`、`node js/sim/_p5_integration.mjs`、`npm run test:browser` 与 `git diff --check`

## v192 真实俱乐部财政体系（2026-08-04）

- 国内杯与洲际赛事按参赛、赛果和晋级结算奖金；比赛日收入拆分为门票、零售与接待，并继续读取同一赛前上座因果
- 永久转会支持首付、分期、出场奖金、二次转会分成及 15–21 岁培养履历驱动的青训补偿，所有未来付款进入统一财政义务与预算占款
- 商业赞助改为可选择的多年稳定、平衡与绩效合同；升降级会产生升级支持、两季降级保护并同步改变下一份赞助报价
- 俱乐部融资显式记录本金、利率、期限、周利息、赛季还本和提前还款；工资/收入与债务/收入异常时限制永久引援、自由签约和租入
- 财政工作台展示赞助、比赛日分项、未来应收应付、债务设施和合规状态；旧档按现有公开数据补齐，导入校验覆盖新结构；缓存 `vcfm-v192`
- 已验证：`node scripts/verify.mjs --full`、`node js/sim/_p5_integration.mjs`、`npm run test:browser` 与 `git diff --check`

## v191 职员国籍与任职履历（2026-08-03）

- 主教练、球探和队医拥有稳定国籍，资料页与球员相同显示国旗及双语国家名；旧存档按职员 ID 稳定补齐，不会每次读取变化
- 职员资料新增俱乐部、职位与任职时期记录；解约、合同到期、自由身签约和俱乐部间流动会闭合原履历并建立新履历
- 旧档自动补记当前任职，保留现有姓名、能力、合同与俱乐部；缓存 `vcfm-v191`

## v190 统一比赛因果、真实临场指挥与长期经营可靠性（2026-08-03）

- 空间比赛统一消费教练、天气、备战、队内讲话、真实阵容情境及定位球训练；直播改为 46–60、61–75、76–90 分段计算，临场战术和换人会影响尚未模拟区间
- 分项职责完整约束训练、战术和培养操作；教练为年轻球员生成真实专项培养计划，俱乐部经营与主教练身份分别记录职业履历
- 修正五国联赛争冠/保级情境、赛前票房强强对话与 xG 精度；新增 AI 债务处置、深层存档引用/数值校验及可访问弹窗焦点管理
- 传球成功率、射门节奏与定位球重新校准；新增比赛因果、AI 债务、存档结构和桌面/手机浏览器端到端审计；缓存 `vcfm-v190`
- 已验证：`node scripts/verify.mjs --full`、`node js/sim/_p5_integration.mjs`、`npm run test:browser` 与 `git diff --check`

## v189 经营模式比赛日全程教练控制（2026-08-03）

- 俱乐部经营模式下，赛前讲话、中场战术/角色/换人、下半场场边战术均由主教练负责，玩家界面改为只读观赛
- 主教练在 60′、75′ 按比分、体能和可用阵容重新评估，决定轮换、攻守风格与比赛节奏；决定写入事件流并影响后续空间模拟
- `applyTeamTalk`、`applyUserHalfTime`、`applyLiveTactics` 统一拒绝经营模式玩家绕过 UI 的临场写入；新增经营模式比赛日权限与连续决策审计
- 缓存 `vcfm-v189`

## v188 职责委托与俱乐部经营模式（2026-08-03）

- 职员页新增职责委托中心，训练、首发、赛前战术、临场换人与年轻球员培养可分别由玩家、建议确认或教练团队持续代管
- 支持锁定当前阵型、关键首发和进攻核心，并可设定体能轮换与“实力接近时优先年轻球员”的培养原则
- 新增俱乐部经营模式：聘用主教练负责训练、阵容、战术和临场，玩家继续管理转会、合同、财政、设施、青训和聘帅
- 自动决策只读取真实球员能力、体能、状态、赛程及职员能力，不写入隐藏胜率或能力修正；新增 `scripts/delegation-audit.mjs`；缓存 `vcfm-v188`

## v187 资料头像脸部精修（2026-08-03）

- 原生 48 格标准眼睛扩大为统一 7×4 外框，瞳孔使用 2×2 结构并保留单像素高光与稳定目光方向
- 四种下颌轮廓改用左右对称的连续阶梯描边，清理脸颊和下巴两侧零碎深色像素
- 普通胡茬由整条下巴色带改为稀疏点状纹理，完整胡须仍保留连续轮廓；状态眼型同步对齐新眼部宽度
- 资料肖像 PNG 缓存加入独立美术版本键，专项审计覆盖瞳孔结构、胡茬色带回归与缓存失效；缓存 `vcfm-v187`

## v186 原生资料肖像修正（2026-08-03）

- 删除 32×32 头像按 1.5 倍取整映射到 48×48 后再叠加五官的拼贴路径，修复眼睛大小不一、五官错位和断裂轮廓
- 资料头像的背景、头型、14 种发型、眼眉鼻嘴、年龄层、伤病与球衣全部在单一原生 48×48 坐标系绘制一次
- 新增像素单元合法性校验，任何越界或无效的高精度肖像都会自动回退到稳定的 32×32 版本
- 专项审计禁止恢复分数缩放函数，并断言原生头脸组件、48 格边界和安全回退；缓存 `vcfm-v186`

## v185 双精度人物肖像（2026-08-03）

- 名单与战术板继续使用原生 32×32 程序脸；96px 球员和职员资料头像改用原生 48×48 高精度像素网格
- 高精度肖像从同一基础脸放大并叠加发丝、眼神高光、鼻梁、脸部明暗、年龄纹理与球衣缝线，不是对小头像做模糊放大
- 两级头像共享外貌种子、肤色、发色、发型、脸型、年龄、状态和伤病诊断，确保名单与资料页仍是同一个人
- 头像预览加入同一人物双精度对照，专项审计断言 32/48 网格与原生细节增量；缓存 `vcfm-v185`

## v184 头像清晰度（2026-08-03）

- 名单与表格程序脸统一使用 32px，避免 26/28/30px 非整数网格缩放；球员与职员资料肖像提升到 96px
- 像素头像使用硬边缩放，非像素正式肖像仍保留平滑缩放；战术板维持原布局密度
- 状态角标按头像实际尺寸缩放并限制上下限，减少小头像遮挡；头像预览加入五档尺寸对照
- `scripts/avatar-audit.mjs` 新增尺寸、资料肖像和像素渲染回归断言；缓存 `vcfm-v184`

## v183 人物头像真实性（2026-08-03）

- 球员生成与头像渲染改为读取同一份外貌画像，消除地区与发型权重长期漂移；旧档已保存的外貌不重置
- 程序脸新增脸型、耳形、眼距、胡须与分年龄皱纹，保持热血像素风并提高名单辨识度
- 伤病头像读取实际诊断：普通肌肉和关节伤只显示状态角标，只有头面部诊断才绘制绷带与淤青
- 新增 `scripts/avatar-audit.mjs` 并接入统一验证；缓存 `vcfm-v183`

## v182 出场定位承诺与可解释成长（2026-07-31）

- 球员拥有培养、阵容、轮换、重要和核心五档出场定位；正式承诺每 28 天按可用比赛与真实分钟占比复核，杯赛、洲际赛、替补、红牌和伤退使用同一逐场记录
- 连续未兑现承诺会降低士气与关系并触发离队意愿，阵容表、球员资料、信箱与周期复核统一读取同一定位和分钟数据
- 一线队训练、青训成长、赛季年龄曲线与青训提拔记录具体属性前后值、总评变化及可解释原因；旧存档通过 schema v2 分阶段迁移
- 修复比赛结算只统计终场 11 人的问题，被换下首发与替补上场球员均获得真实出场、赛事分账和赛后评分
- 新增 `scripts/player-pathway-audit.mjs` 与 `scripts/match-appearance-audit.mjs` 并接入统一验证；缓存 `vcfm-v182`
- 已验证：`node scripts/verify.mjs --full`、`node js/sim/_p5_integration.mjs`、`git diff --check` 与本地 HTTP 入口/新模块均通过

## v181 存档可靠性与统一验证（2026-07-31）

- 比赛视图 FSM 记录前态并恢复原 `FREE_PLAY` / `SCRIPTED` / `SIM_DRIVEN` 子状态，暂停画面分支与导演锁不再失效
- 存档 Worker 失败后终止并同步落盘各槽最新快照，退出前冲刷待存队列；新增 `schemaVersion`、旧档迁移封装与导入结构校验
- Service Worker 改为逐资源预缓存；回放列表动态文本使用 `textContent`；`matchview.js` 从首屏静态模块图移出并在比赛入口懒加载
- `scripts/verify.mjs` 补齐国际赛事、品牌、比赛平衡、存档、回放与懒加载审计，`--full` 追加 24 场真实性校准；新增 GitHub Actions
- 缓存 `vcfm-v181`
- 已验证：`node scripts/verify.mjs --full`、`node js/sim/_p5_integration.mjs`、`git diff --check` 与本地 HTTP 入口/懒加载模块均通过

## v180 空间事件比赛分析（2026-07-31）

- 空间引擎事件补齐坐标、真实接球完成事件与压迫尝试；完成传球、推进和压迫数据不由 UI 估算或美化
- 从同一事件流派生出脚前 xG、射门图、推进、压迫、行动热区和完成传球网络，分析随 `fixture.matchReport` 保存并可回看
- 赛后报告新增复盘、射门图、推进、压迫、行动热区和传球网络六个分栏；桌面与移动端使用同一份分析数据
- 新增 `scripts/match-analysis-audit.mjs` 并接入统一验证；缓存 `vcfm-v180`
- 已验证：`node scripts/verify.mjs`、`node js/sim/_p5_integration.mjs`、`git diff --check` 全部通过；Playwright 实际开局并生成战报，1440×1000 与 390×844 下六个分栏无横向溢出或网络标签重叠
- 审计如实显示当前空间引擎传球成功率约 45%–49%；不要在 UI 层修饰。如要校准，须直接调整空间引擎并重跑比赛真实性审计

## v179 报名名单、本土培养与赛事资格（2026-07-31）

- 球员按 15–21 岁实际所属俱乐部和足协逐季记录培养履历；旧档一次性生成稳定且可见的青训背景，转会后既有培养年限继续保留
- 国内联赛采用 25 人名单、最多 17 名非本足协培养和 U21 免报名；洲际 A 名单额外预留 4 个本俱乐部培养名额，在队连续培养满 2 年的 U21 可走 B 名单；国内杯无需报名
- 报名名单仅在转会窗开放时可调整，AI 自动提交合规名单；首发、替补、AI 换人、空间引擎伤退换人和用户换人统一读取赛事资格；新增 `scripts/squad-registration-audit.mjs`；缓存 `vcfm-v179`

## v178 续约与租借分阶段谈判（2026-07-31）

- 用户续约改为「提交年限与周薪 → 球员/经纪人审核 → 接受、还价或拒绝」，签约奖在谈判期间占款，正式谈判替代财政页的到期合同估算
- 租入经过母队条款审核与球员出场机会审核；外租经过市场寻价、用户接受/还价、租入方复核与球员审核，完成前球员始终留在母队
- 永久转会、续约与租借对同一球员互斥，所有成交前复核窗口、阵容、未承诺现金及 AI 8 周运营储备；新增 `scripts/deal-negotiations-audit.mjs`；缓存 `vcfm-v178`

## v177 出售分阶段谈判（2026-07-31）

- 用户主动出售改为「挂牌寻找买家 → 买方正式报价 → 接受/还价/拒绝 → 球员审核新合同 → 成交」，各阶段按日推进并可撤牌
- AI 后台转会不再即时卖走用户球员；既有挖角报价接受后也进入球员审核，成交前复核窗口、阵容、买方 8 周运营储备及其他谈判占款
- 双方财务、合同、号码、阵容、新闻与信箱仍使用同一球员对象和总账；新增 `scripts/sale-negotiations-audit.mjs`；缓存 `vcfm-v177`

## 后续待启动路线

1. 球员个人踢球习惯：以持久化公开特征改变现有空间动作选择、跑位和风险偏好；支持训练学习/淡化、球探认知、AI 战术适配和同种子方向审计，不写入隐藏能力或胜率
2. 完整发展队赛季：周赛制、轮换对手、真实替补与行动评分，并让世界其他俱乐部获得一致的发展比赛机会
3. 空间比赛与存档性能：在保持确定性与同源因果的前提下缩短批量比赛耗时，并把长期存档迁移到更适合大对象的持久层

## v176 全局未承诺现金（2026-07-31）

- 新增统一 `cash-reservations.js`，设施、合同、租借、职员、球探及旧买入入口只可动用扣除转会谈判占款后的现金
- 主动操作在资金不足时不扣款、不改状态；取消谈判后额度即时释放，工资、维护和罚款等刚性义务仍正常结算
- 职员市场不再在余额不足时免费刷新，职员成交复核纳入自由职员签约费；新增 `scripts/cash-reservations-audit.mjs`；缓存 `vcfm-v176`

## v175 转会现金锁定（2026-07-31）

- 进行中的用户买入谈判会真实占用转会费与签约奖，新报价只能使用扣除既有谈判后的未承诺现金
- 俱乐部/球员还价接受与最终成交再次校验其他谈判占款；撤回、拒绝、取消或完成后额度自动释放
- 财政预测与转会成交复用同一成本函数；扩展 `scripts/transfer-negotiations-audit.mjs`；缓存 `vcfm-v175`

## v174 工资承诺与真实季末预测（2026-07-31）

- 进行中的买入谈判和待续约合同同时形成一次性现金与每周工资承诺，续约只计算相对现薪的增量
- 现金储备、工资占比与预计季末余额统一纳入承诺工资，季末预测不再漏扣尚未落账的转会费、签约奖和剩余赛季工资
- 财政工作台新增已承诺周薪与本季工资影响；扩展 `scripts/finance-commitments-audit.mjs`；缓存 `vcfm-v174`

## v173 财政承诺与真实可用预算（2026-07-31）

- 安全转会上限扣除进行中的用户转会谈判（转会费 + 签约奖）与即将到期球员的续约准备金
- 财政工作台显示已承诺现金，预算模型只使用稳定的合同估算，不因打开页面而重新掷随机报价
- 新增 `scripts/finance-commitments-audit.mjs`；缓存 `vcfm-v173`

## v172 财政工作台与赛季预算（2026-07-31）

- 总览分组新增独立财政页，展示现金、精确赛季净额、周运营、资金续航、分类收支与逐笔流水
- 可持久化设置 4–20 周现金储备和 25%–100% 转会分配；据剩余联赛主场、公开比赛日收入、周运营和上季联赛分红计算安全转会上限与预计季末余额
- 工资占比、财务状态和预算结果均由总账与现有公开数据驱动；新增 `scripts/finance-budget-audit.mjs`；缓存 `vcfm-v172`

## v171 统一财务总账（2026-07-31）

- 新增 `club.finance.financeLedger[]` 签名流水；周结算、门票、转会、租借、职员、球探、董事会、设施和合同操作统一记录有符号现金变动
- 旧 `club.finance` 赛季累计字段继续同步，旧存档首次读取时自动迁移为只读历史流水，不改变余额
- 新增 `scripts/finance-ledger-audit.mjs`；缓存 `vcfm-v171`

## v170 比赛可复现种子（2026-07-31）

- 每场 fixture 自动保存 `matchSeed`；概率模拟、空间模拟、天气、伤病、纪律、门票和高光共用同一确定性随机流
- 同一存档、同一 fixture、同一 seed 可复现完整比分、事件、球员和报告；显式修改 `matchSeed` 可生成可控对照样本
- 新增 `scripts/match-seed-audit.mjs`；统一验证入口覆盖固定种子审计；缓存 `vcfm-v170`
> 本地：`pwsh -File start-local.ps1` → http://127.0.0.1:8765/

## v169 世界财政公平与长期生态（2026-07-31）

- 所有俱乐部统一结算一线/青训/职员工资、三类设施维护和按联赛层级/实力生成的商业收入，不再只有用户俱乐部承担运营成本
- 所有主场比赛读取同一球场、上座和赛事情境生成门票收入；AI 转会预留 8 周现金消耗且不会替用户主动买人
- 赛季末补员同时保证各位置骨架，青训自动提拔和 AI 转会收紧一线队上限，控制长期人口膨胀
- 新增 `scripts/ecosystem-audit.mjs` 固定种子推进 5 个完整赛季，审计偿付能力、财富分布、工资/身价通胀、年龄与阵容完整性
- 新增 `scripts/verify.mjs` 统一执行语法与核心审计；缓存 `vcfm-v169`

## v168 游戏 UI 与排版优化（2026-07-30）

- 顶部导航由 15 个平铺入口重组为 5 个主分组，各分组保留二级页签并记忆最近页面；主题、语言、导出和菜单收进更多菜单
- 阵容表新增精简 / 完整视图并持久化偏好，关键号码与姓名列横向滚动时保持可见
- 概览重排为比赛指挥、赛季快照、俱乐部运营、信箱、经理生涯与新闻组成的 12 栏管理工作台
- 宽屏内容区、移动端导航与全屏弹窗统一优化，补充键盘焦点状态和辅助文字可读性
- 新增 `scripts/ui-layout-audit.mjs`；缓存 `vcfm-v168`

## v167 长期真实性（2026-07-29）

- 球员老化按位置拆分身体、技术和心智曲线；前锋更早失去爆发力，门将与后卫更晚，成熟期心智能力仍可增长
- AI 按真实首发负荷、休息天数与比赛重要性进行密集赛程轮换；首发历史由比赛实际阵容记录
- 伤病改为具体诊断、恢复期与复出观察期，伤病史和过早复出会提高复发风险；训练、比赛和界面读取同一伤病状态
- 非晴天天气按球员现有属性产生适应差异，并同步影响攻防、体能与伤病；俱乐部赛季计划同时驱动董事会展示和 AI 转会
- 新增 `scripts/long-term-reality-audit.mjs`；缓存 `vcfm-v167`

## v166 赛前票房因果 / 伤停天数（2026-07-29）

- 上座与门票系数在写入本场赛果前锁定，不再读取本场胜负或赛后积分榜；连胜改为真正连续胜场
- 删除赛后获胜使“门票收入”额外上涨 8% 的倒置因果；所有系数统一受 2.5 倍上限约束
- 战报与财政概览显示杯赛、阶段、德比、争冠/保级、状态、冲刺期及联赛层级系数
- 球员资料、阵容、训练和战术替补席显示预计伤停天数；新增 `scripts/finance-audit.mjs`
- 缓存 `vcfm-v166`

## v165 比赛真实性校准（2026-07-29）

- 远射决策改为随距离衰减的真实出脚意愿；射门加入垂直落点、门线插值与门将反应时间，普通传球不再直接算进球
- 门将托救/后卫封堵正确越过横梁形成角球；角球落点降到可争顶高度并拥有独立二次进攻窗口；快速高光按空间威胁排序
- 抢断与拦截采用球队级节奏冷却，点球、犯规和强弱队差距重新校准；新增固定种子批量审计 `scripts/match-realism-audit.mjs`
- 24 场等强校准样本：3.04 球/场、10.5% 射门转化、37.5% 禁区外射门、0 次 30+ 进球；缓存 `vcfm-v165`

## v164 分阶段买入谈判（2026-07-29）

- 用户买入由即时成交改为「卖方俱乐部审核转会费 → 球员审核合同 → 成交」，每阶段 1–2 天；卖方估价读取球员价值、阵容地位、位置深度与剩余合同，球员要求读取现薪、市场工资、合同年限与两队竞技水平
- 俱乐部/球员可接受、拒绝或还价；还价进入信箱并可在信箱或转会页处理，审核中可撤回；同球员只允许一项进行中谈判，关窗、资金/阵容或任职条件变化会取消交易
- 成交沿用同一球员对象，转会费、签约奖、工资、合同、号码、自动阵容、新闻、媒体及 `finance.seasonTransferNet` 同步落账；新增确定性审计脚本覆盖完整状态机
- 缓存 `vcfm-v164`

## v163 快速高光动态 / 一键纯战报（2026-07-28）

- 快速模拟与直播共用高光录帧：进球/扑救等时段真空间投影，平淡跳过；倍速旋钮生效；快速不跑进球后 FMM 自动重播
- 一键完赛改为纯战报：同步算完 → 灌评论 → 直接赛后报告，不播球场动画；UI `match-report-only`
- 顶栏比分/分钟在快速模式随事件更新（v162 修复延续）
- 缓存 `vcfm-v163`

## v162 转播奖金 / form 阵容 / 换帅新闻（2026-07-28）

- 赛季末按最终积分榜结算转播分成（50% 均分 + 50% 名次）与名次奖金；写入 `club.money` 与 `finance.seasonBroadcastIncome` / `seasonPrizeIncome`；概览财政可见
- 自动阵容 `xiSortScore` 纳入 `playerForm`（近况评分，温和 ±12%，OVR 仍为主轴）
- 主教练成交写「📢 换帅」世界新闻（来源/原帅/卖方临时工）
- 缓存 `vcfm-v162`

## v161 高价值体验打磨（2026-07-28）

- 财政：本季门票/转会净额/薪资与设施支出、净额估算；赛后上座人数与上座率
- 经理：名望档位文案、请辞冷却提示、上任/离队新闻
- 职员挖角：短合同/豪门惜售等标签；拒绝原因可解释
- `start-local.ps1` 一键起服（清占用端口、自动打开浏览器、no-store）
- 当时缓存为 v161（已被页首当前缓存取代）

## v160 门票展示与经理职业流动（2026-07-27）

- 门票展示、经理待业/邀请/再就业；当时缓存为 v160（已被页首取代）

## v159 职员流动（2026-07-27）

- 职员合同、自由身、挖角接触、AI 换帅；当时缓存为 v159（已被页首当前缓存取代）

## v158 俱乐部教练组与职员实力（2026-07-27）

- 俱乐部详情展示主教练 / 球探 / 队医；按实力生成职员（power/stature/资金）
- 当时缓存为 v158（已被页首当前缓存取代）

## v157 球员状态 form（2026-07-27）

- 赛后 `applyMatchRatings` 经 `pushRecentRating` 写入 `stats.recentRatings`（最多 5 场滚动）
- 联赛/杯赛/洲际凡出场均计入状态；联赛场均与 `lastRating` 仍只记联赛
- 阵容表新增「状态」列；球员资料展示状态档位与近况评分条
- 国家队征召分读取同一 form（≥2 场时优先于单场 lastRating）
- 旧档用 `lastRating` 垫一条；跨赛季归档保留 recentRatings
- 当时缓存与入口资源版本为 v157（已被页首当前缓存取代）

## v156 健康修复（2026-07-27）

- 审计脚本对齐 v152 赛制：11 联赛 × 18 队 = **198** 俱乐部、联赛赛程 3366、能力配额按一线队人口相对 3572 基准缩放
- `competition-audit` 门将榜改为 limit 内非空断言；SW 预缓存 matchview 子模块；金岭短名去重
- 清除 matchview DEBUG；存档槽位钳制 1–3；门将大脚改中场安全通道
- 当时缓存与入口资源版本为 v156（已被页首当前缓存取代）

## v155 职员与训练委托（2026-07-27）

- 现任职员与职员市场候选人的姓名/资料按钮均可打开资料页，展示岗位、年龄、能力、薪资/签约费及由同一能力值驱动的实际效果
- 训练页新增“委托助理教练安排”：根据体能、伤病、士气、下场比赛间隔、年轻球员数量与阵容属性短板安排训练重点、强度及赛前备战
- 助教能力影响建议深度：基础能力优先处理体能和赛程，高能力助教会识别阵容短板、青训周期和安全的高强度训练窗口
- 当时缓存与入口资源版本为 v155（已被页首当前缓存取代）

## v152 游戏平衡改进（2026-07-27）

- 本轮修改：10 项核心平衡与体验改进，详见 `IMPROVEMENTS.md`
- 修改文件：`js/models.js`, `js/facilities.js`, `js/transfers.js`, `js/board.js`, `js/poaching.js`, `js/relations.js`, `sw.js`, `AGENTS.md`
- 主要改进：
  - 转会估值公式平滑化（指数 2.4→1.8），降低顶级球员价差
  - 工资增加位置系数（GK 0.85x, ATT 1.1x）
  - 退役曲线平滑化（32岁 1% → 37岁 60% → 38+岁 85%）
  - 冬窗提前至 D90-125（赛季中段），延长至 35 天
  - 董事会警告从 3 次提升到 4 次，增加连续达标减免机制
  - 球员状态追踪系统（form: 最近 5 场评分平均）
  - 球场经济平衡（5级收入 600k→480k，维护 140k→180k）
  - 设施升级成本降低 30%（4-5 级）
  - 挖角频率提升 50%（12%→18%）
  - 球员对话冷却差异化（表扬/批评 4 天，承诺/续约 10 天）
- 已验证：所有修改文件通过 `node --check` 语法检查
- 向后兼容：旧档自动适配新平衡公式
- 续接时先执行 `git status --short --branch` 与 `git fetch --prune origin`，以实际 Git 状态为准

## v149 发布节点（2026-07-26）

- 前序 Git 基线：`26316bd fix: add nation search and sync avatar kits`
- 发布范围：`AGENTS.md`, `css/style.css`, `index.html`, `js/branding.js`, `js/clubs.js`, `js/main.js`, `scripts/branding-audit.mjs`, `sw.js`
- 本轮已完成：v147 俱乐部/国家队球员连续浏览；v148 球探属性标签浅色主题辨识度；v149 将 `EWEW` 等机器代码替换为随语言切换的可读球队短名；v195 起内部与队徽 `shortName` 改为无国家前缀的 3–4 位自然简称
- 已验证：相关 JS 全部通过 `node --check`；`git diff --check` 通过；`node _smoke_p1.mjs` 通过；`node scripts/branding-audit.mjs` 通过（5 国、11 联赛、188 队、旧档迁移 188 队）；本地 v149 的 index/main/branding/clubs 均 HTTP 200
- 本地预览：`http://127.0.0.1:8765/` 已运行，静态服务会直接读取当前工作区
- 续接时先执行 `git status --short --branch` 与 `git fetch --prune origin`，以实际 Git 状态为准

## 已完成路线

### FM ①–⑤
角色 · 讲话 · Inbox · 球探雾/对手报告 · 中场换阵+角色复盘

### ABCD 扩展（v59）

| 线 | 内容 | 文件 |
|----|------|------|
| **A** | 关系 -2..+2、约谈、氛围、对立发信 | `relations.js` |
| **B** | 紧急信箱门闸、球探任务、关注过滤 | `worldpulse.js`, `main.js` |
| **C** | 战术板 pointer 拖拽、Harbourgate/Steelborough/Millford 队服 | `main.js`, `clubs.js`, `models.js`, `avatar.js` |
| **D** | 财政简报、青训周报、世界新闻、成就徽章 | `worldpulse.js`, 概览 UI |

### 比赛引擎 v2（P0–P5）

| 阶段 | 内容 | 文件 |
|------|------|------|
| P0–P4 | 空间模拟、决策、防守、裁判、平衡缩放 | `js/sim/engine.js`, `sim-viewer.html` |
| **P5** | 当时仅用户场接入主游戏；v201 起 AI 后台也由 Worker 运行空间引擎 | `js/sim/adapt.js`, `js/match.js` |
| **直播投影** | 录帧 + `applySimSnapshot`，关导演 AI | `matchview.js`, `main.js` driveMatchEvent |

- 用户场判定：`shouldUseSim = !!state?.userSide`（P6 起永久默认 v2，无总开关）
- 用户场：`SimEngine` 跑半场 → `directResult` → 现有 event/报告/积分（比分与直播帧同源）
- **直播**：用户场 10Hz 录帧 → 高光窗切片真投影；进球只走一次横幅/底栏文案
- AI 场：v201 起浏览器日历推进显式请求 `spatial/background`；旧同步 API 的概率路径只保留兼容与故障隔离用途
- 预览手感：`sim-viewer.html`（raw 手感）
- 当时发布缓存为 v151，当前已统一升级到页首所列版本
- **欧洲赛事中心 v151**：“世界赛事”内分为俱乐部赛事与国家队赛事；欧冠、欧联和欧协联可查看联赛阶段积分、赛程赛果及独立的射手/助攻/评分/门将榜，球员使用 `competitionStats[competitionId]` 分赛事记账且不污染国内联赛；五国顶级联赛按排名分流三档资格，新赛季采用 8 个比赛日、每队 8 场、严格 4 主 4 客、同国回避且不重复对手的精简联赛阶段，前 8 名进入淘汰赛；旧档保留既有赛果并迁移赛事名称；国家队赛事中文统一使用“欧洲杯”，英文使用“European Championship”，并展示射手/助攻/出场/门将四榜
- **全部联赛数据中心 v150**：顶部将原“积分榜 / 数据榜”合并为“联赛中心”，内部用“积分榜 / 球员榜”子页保持内容分离；具体联赛选择双向同步，“全部联赛”仅用于球员榜；球员榜支持汇总五国 11 个国内联赛或逐联赛筛选，并增加紧凑联赛标识；杯赛、洲际赛与国家队赛事仍按真实赛事口径独立；球员新增赛季联赛分账 `leagueStats[division]`，跨联赛转会前后分别归属原联赛与新联赛，总赛季数据继续汇总，旧档首次使用时按当时所在联赛迁移
- **球队可读短名 v149 / v157 / v195**：198 队（v152 起 11×18）中英文城市/地区短名分别全局唯一；界面主短名由机器代码改为本地化可读名称（如 `西湾 / Westbay`），随语言切换同步更新并用于赛程、榜单、比赛事件、新闻及转会；内部唯一代码与队徽文字在 v195 改为无国家前缀的 3–4 位自然简称，窄列悬停仍可查看完整队名
- **球探标签辨识度 v148**：球员详情中的球探属性标签提高字号、字重、内边距和边框对比；浅色主题使用独立的浅灰底与深色文字，不再沿用深色半透明底导致灰字难辨
- **球员连续浏览 v147**：从俱乐部主力名单或国家队 23 人名单打开球员资料时，顶部提供返回名单、上一位/下一位与当前位置；关闭按钮 / `Esc` 返回原名单并恢复滚动位置，`←` / `→` 可连续切换；国家队切换时继续使用对应国家队球衣与 1–23 号，搜索和转会市场等独立入口仍按原逻辑直接关闭
- **头像球衣配色 v146**：删除 `sunset` / `harbor` / `steel` / `mill` 四个旧 ID 的历史头像主题覆盖；俱乐部与国家队头像统一只读取当前 `kit.primary` / `kit.secondary`，全队自动审计保证与号码徽章一致
- **全局搜索国家队 v145**：国家中英文名与三字代码进入全局搜索；结果显示本地国旗、人才池和首发能力，点击直接打开现有 23 人国家队名单
- **五国现实映射 v144 / v152**：198 支球队保留完全虚构的双语名称、队徽和球衣；每队通过唯一匿名席位（如 `ENG-T1-01`）映射现实竞争层级，不保存或展示现实俱乐部身份；五国顶级联赛使用不同的非线性实力/财政曲线，档案同步决定开局青训、训练和球场层级，旧档只补档案而不重置经营成果；v152 起各联赛统一 18 队双循环 34 轮
- **能力稀缺标尺 v144 / v157**：以 3572 人一线队为基准约 2 名 OVR 20、24 名 19、110 名 18；人口变化时按比例缩放（198 队约 3762 人 → 约 2/25/116）；位置生成已归一化，潜力、身价和工资同步更新，国家队与俱乐部继续共享同一球员对象与能力
- **后台强弱响应 v144**：防反改为低控球、低频但具转换质量的策略，不再无代价同时增强攻防；AI 后台比赛依据双方实际首发、体能和士气温和放大 1–20 标尺下的相对差距（上下限 ±14%），不读取名望或现实映射档案
- **国家队视觉上下文 v143**：36 队拥有独立主场球衣；每期 23 人名单按位置分配唯一 1–23 号（门将优先 1/12/23），不覆盖俱乐部号码；从国家队名单打开球员时资料页保持国家队球衣/号码，所属俱乐部与现役赛季历史均可点击进入俱乐部详情
- **现实国家队征召 v142**：先按位置选 23 人、再从名单排 `1-4-3-3` 首发；能力为主体，赛季场均/上一场评分、俱乐部出场率、位置数据、体能、士气、潜力及国家队连续性共同形成可见征召分；伤病硬性排除，俱乐部停赛不跨赛事；国家队页显示状态、体能与征召分
- **国家队共享能力 v141**：现实人才层级进入球员生成；一线队按俱乐部级别形成约 45%–68% 本土比例、青训约 72% 本土；国家队排行、资格与比赛只读取同一俱乐部球员的首发 `OVR`，不再叠加国家队隐藏修正；旧档一次性同步校准球员属性、总评、潜力、身价与工资，不改姓名/国籍
- **头像 v5.4**：状态表情与天生五官分离；眉/眼/目光/鼻/嘴按种子稳定变化，默认由统一怒眉坏笑改为平静、专注或友善
- **青训详情**：青训名单的姓名/详情按钮可打开完整资料；不显示仅适用于一线队的约谈、续约与外租操作
- **全局搜索**：顶部放大镜或 `Ctrl+K` / `/` 搜索一线队球员、俱乐部与本队青训；最多 10 条分组结果，对手能力继续受球探雾限制
- **netHit**：仅脉冲一帧 + 球门线附近才播网效
- **角球**：高光窗半场≤3；禁区约 5v5 不糊；**主罚人显示层钉在角旗球旁**；徽章开出后 ~2s 清掉（不粘整段）
- 战术涌现：前锋回撤、边锋内切、边卫套边、核心绝对权（`ensureCorePlayer` 主客都有）
- 表现层：`compactSimFrame.ball.z` → 空中球阴影/缩放；simDrive **软跟镜**；球轨迹丝带
- **持球光环**：只认 ball.owner 且贴球；飞行中绝不亮；sim 驱动不用 touchUntil 拖影
- **进球直播**：真实射门/入网帧 → 短 hold → 引擎真实庆祝；`_beginGoalBeat` 仅留给旧编舞路径
- **画面分离**：sim `_separateAgents` 双轮；Canvas 圆点半径 6–8px，不再二次改逻辑坐标
- 导演：高光慢镜；**FMM 全场稳镜**；进球 hold 中不灌后续 sim 帧冲散庆祝
- FMM UI：横条棋盘草坪 + 两侧看台；底栏 **解说 ticker ↔ 控球条**；自动重播+跳过
- 门架：端线外侧；入网撞击加强
- 庆祝：最多 5 人分槽自然围拢；无半瞬移；高光在开球复位前结束
- 进球/慢镜：`mp-goal-flash` 与 `mp-replay-slow::after` 同时存在时重置徽章边界，禁止全场紫色遮罩
- 越位：按出脚瞬间快照所有越位位置球员；回到线上接球仍吹；角球/界外球/门球首脚豁免
- 边界判罚：防守方最后触球→角球给进攻方，进攻方触球→门球给防守方（`_resolveBounds` 曾判反已修）；角球真实来源＝门将托救 ~40% 托过底线 + 后卫封堵 ~30% 挡过底线，频率约 0.7/场（归属已正确，未追真实量级 8-12，属设计取舍）
- 纪律空间化（P3）：犯规从**真实抢断失败+贴身接触**涌现（`_commitFoul`）→ 禁区内点球 / 其余任意球；黄红牌按严重度掷，第二黄有谨慎抑制；真红牌 `a.sentOff` 罚下实现 11v10；点球 `_penaltyKick` 罚球点单挑门将（finishing vs reflexes），进球走 `_goal(penalty)`；人墙任意球 AI 已完成（direct/cross/simple）。量级：犯规 ~22/场、黄 ~3/场、红 ~1/6-7 场、点球 ~1/2-3 场。`adapt.js` 全量翻译 foul→card/red/penalty（不采样，喂 `discipline.js`）；`match.js` sim 路径已停调旧 `tryCardOrFoul`
- 伤病空间化（P3 收尾）：接触伤从犯规涌现（严重度 none 1% / 黄 6% / 直红 20%），疲劳伤每模拟分钟抽查体能最低者（引擎体能是开场快照——带伤/低体能上阵才有风险）；伤员真实退场（复用 `sentOff` 减员）→ ~40s 后**热替换**替补从中线进场恢复 11v11（`substituteAgent` + `onInjurySub` 回调），无名额/无人选则少人作战；门将不受伤。`match.js` `wireSimInjuries`：队医×训练×天气 → `eng.injuryMul`；替补自动选人（同位置优先；**用户队也自动**——半场预跑无法中途询问，事件可见、中场可调；AI 静默）；伤情落账复用旧通道（天数 contact 2-6/fatigue 1-3、`injuredOut`、`applySubstitution`）。量级 ~0.45-0.5/场。探针 `js/sim/_injury.mjs`
- 防死锁看门狗 `_antiDeadlock`：球权/球位 20s 零进展 → 强制大脚解围（emit `stall_clear`）。僵持根因已专项修（队射门冷却豁免/press 迟滞/松球 clamp/sentOff 接管等）；看门狗保留作保险（~0 次/场）。连带修：`_resolvePossession` 自由球接管、死球摆位全部过滤 `sentOff`
- **A 批修复（v107）**：`directResult` 透传 `penalty`；`addSimGoal` 禁止 `pickAssister` 回退、点球文案「点球破门」；SW 预缓存补 media/staff/board/training 等；`index.html` 保护键与 CACHE 对齐；autosave 失败 toast
- **乌龙记账**：`_goal` 标 `ownGoal`（lastKicker 属失球方）；`directResult` / `addSimGoal` 透传；比分始终给得分方，文案「乌龙」，触球者不进个人进球榜；找不到球员也涨分。探针 `js/sim/_owngoal.mjs`
- 传球：直接回传降权；直塞只在最后防线附近生成；门将主动处理低平身后球
- 抢断：仅球队指定 presser 下脚；球权转换后 4s 组织窗；个人/全队抢断冷却
- 进攻站位：边锋保持左右宽度，中场按固定 lane 接应；最后三区仅一名中场前插
- 门将：轨迹扑救；空门降扑救；抱稳/托出；圆点侧移残影；压迫必大脚
- 助攻：lastPasser → assistId；高光窗提前
- 球：落地弹跳 + 射门橙黄轨迹

## 关键 API

- `applyPlayerTalk` / `clubAtmosphere` / `relationLabel`
- `startScoutMission(region)` region: `div3`|`div2`|`intl`
- `financeSnapshot(world)`
- `checkManagerBadges` / `noteUserMatchResult`
- `world.scoutMissions[]` · `managerCareer.badges[]`
- `shouldUseSim` / `ensureSimEngine` / `SimEngine.directResult({tMin,tMax})`
- （P6 已删）`scaledResult` / `USE_SIM_ENGINE` — 勿再调用

## 注意

- 勿用 PowerShell `Set-Content` 写中文源码
- 主目录 `F:\VCFM`；说推 GitHub 再 push
- 用户场使用 0.1 秒标准档；AI 后台普通决策使用 0.3 秒步长，关键球路与门将威胁窗口局部使用 0.1 秒自适应积分并移入 Worker。修改性能档后必须分别重跑标准与 background 真实性审计
- **头像**：全场景统一热血程序脸 2.1（`js/avatar.js`）；正式肖像资产池已弃用删除，同队球衣色由 kit 直接绘制。
